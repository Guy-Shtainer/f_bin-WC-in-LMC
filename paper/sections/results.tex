\section{Results}
\label{sec:results}

\subsection{Binary detections}
\label{sec:detections}

Applying the classification criteria described in
Sect.~\ref{sec:criteria} to the \primaryline radial velocities, we detect
\Ndetected new spectroscopic binaries among the \Nstars apparently single
targets. Combined with the \Nknown previously known binaries from
\citet{Bartzakos2001}, the total number of confirmed spectroscopic
binaries in the LMC WC/WO sample is \Nbin out of \Ntotal, yielding an
observed binary fraction of $\fbinobs$.

\begin{figure}
\centering
\includegraphics[width=\columnwidth]{plots/peak_drv_per_star.pdf}
\caption{Peak-to-peak radial-velocity excursion $\Delta\mathrm{RV}_{\max}$
  for each apparently single target, sorted in decreasing order. Error
  bars represent the $\sigthresh\sigma$ combined uncertainty
  (Sect.~\ref{sec:criteria}). The vertical line marks the
  binary-detection threshold $T = \drvthresh$\,\kms; stars satisfying
  both criteria (the threshold cut and the $\sigthresh\sigma$
  significance) are highlighted in red.}
\label{fig:peak_drv_per_star}
\end{figure}

Table~\ref{tab:classification} lists the binary classification for each
star, including the maximum $\Delta\mathrm{RV}$, the associated
significance, and the epoch pair that triggered the detection.

\subsection{Radial-velocity measurements}
\label{sec:rv_results}

Figure~\ref{fig:binary_examples} illustrates the classification by way of
three representative cases drawn from the sample: (a) a clearly detected
SB1, (b) a marginal case lying near the detection threshold, and (c) an
apparently single star. The SB1 panel shows an unambiguous red/blue shift
of the \primaryline emission profile across epochs, with a peak-to-peak
RV excursion comfortably exceeding both the $\drvthresh$\,\kms threshold
and the $\sigthresh\sigma$ significance requirement. The marginal case
exhibits a shift that is comparable in magnitude to the per-epoch
combined uncertainty, illustrating why the joint threshold-plus-significance
criterion of Sect.~\ref{sec:criteria} is needed: without the
$\sigthresh\sigma$ cut, such borderline candidates would inflate the
detected binary fraction with statistically marginal claims. The
apparently single star shows no measurable variation between epochs, with
the line profile reproducing to within the noise. Per-star
radial-velocity time series for the full sample are tabulated in
Appendix~\ref{app:rv_tables}.

\begin{figure*}
\centering
% \includegraphics[width=\textwidth]{plots/binary_examples.pdf}
\caption{Three representative cases drawn from the sample. \textit{Top
  row:} radial-velocity time series for (a) a clearly detected SB1, (b) a
  marginal case near the detection threshold, and (c) an apparently
  single star. \textit{Bottom row:} corresponding \primaryline
  emission-line profiles at the two epochs of maximum and minimum radial
  velocity, illustrating the red/blue shift signature in panel (a), the
  marginal shift in panel (b), and the absence of measurable variation
  in panel (c).}
\label{fig:binary_examples}
\end{figure*}

\subsection{Observed binary fraction}
\label{sec:obs_fbin}

The observed binary fraction of $\fbinobs$ represents a lower limit on
the intrinsic binary fraction, as unfavourable orbital configurations
(long periods, low inclinations, low radial-velocity semi-amplitudes)
prevent detection for a fraction of the binary population. We address
this detection bias through Monte-Carlo simulations in
Sect.~\ref{sec:bias}.

\subsection{Threshold-curve fit results}
\label{sec:threshold_results}

The threshold-curve fit described in Sect.~\ref{sec:threshold_fit} is
applied to the observed cumulative binary fraction as a function of the
$\Delta\mathrm{RV}_\mathrm{max}$ detection threshold. The best-fit
parameters yield an intrinsic binary fraction
$f_\mathrm{bin} = \fbinThresh^{+\fbinThreshErrUp}_{-\fbinThreshErrDn}$,
with fitted single-star scatter $\sigma_\mathrm{intrinsic} = \sigmaIntrinsic$\,\kms
and per-epoch measurement uncertainty $\sigma_\mathrm{meas} = \sigmaMeasFit$\,\kms.
The threshold-curve estimate is compared to the grid-based likelihood
result of Sect.~\ref{sec:bias} as an internal consistency check on the
recovered intrinsic binary fraction.
