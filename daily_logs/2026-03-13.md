# Daily Log — 2026-03-13

---
## 11:03 — Variance-weighted KS scoring bugfix (3 attempts, still broken)
**Topics:** bias correction, variance-weighted KS score, chi-squared goodness-of-fit, cadence-aware simulation scoring
**Work done:**
- Diagnosed why weighted KS scoring showed all p-values ≈ 1.0: `ks_weighted_D()` computed a weighted **average** of |diff|, which is much smaller than the max → Kolmogorov p-value formula (designed for supremum) gave p≈1 everywhere
- Attempt 1: Changed p-value to `1.0 - D_w` (linear score). Result: still all ≈ 1.0 because weighted average D is tiny (~0.01-0.05)
- Attempt 2: Changed `ks_weighted_D()` from weighted average to **weighted max** (`max(|diff| * w_norm)`). Result: still nearly identical to standard KS because the max-diff bin also has low variance
- Attempt 3: Added new `chi2_weighted_score()` function using χ² = Σ(diff²/σ²) with scipy chi-squared p-value. Result: p-values now 10⁻⁴⁴ — way too sensitive because σ² from 10,000 repetitions is tiny (~10⁻⁴), making chi² values enormous
- Searched web for weighted KS implementations (scipy issue #12315, hep_ml, Ecume R package, Monahan 2011)
**Decisions:**
- Chi-squared approach is conceptually right but needs different normalization — σ² from N_sets repetitions is too small to divide by directly
- Need to scale variance appropriately, perhaps by N_sets or use a different variance estimate
**Files modified:**
- `wr_bias_simulation.py` — modified `ks_weighted_D()` (weighted max), added `chi2_weighted_score()`, modified `_evaluate_cadence_point()` weighted branch
- `app/pages/05_bias_correction.py` — updated scoring method explanation text (lines 761-766)
**Open questions:**
- How to properly normalize the chi-squared statistic so p-values are in a useful range? Options: (a) multiply σ² by N_sets to get per-observation variance, (b) use standard deviation instead of variance, (c) use a reduced chi-squared (χ²/n_dof) with a different p-value mapping, (d) entirely different approach
- The fundamental issue: CDF variance from 10,000 repetitions is ~10⁻⁴, so any diff²/var explodes
**Memory updates:** None saved this session — issue still unresolved
