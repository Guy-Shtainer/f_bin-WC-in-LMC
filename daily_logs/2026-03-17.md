# Daily Log — 2026-03-17

---
## 11:26 — Cadence partial results fixes + Likelihood feature parity with CvM

**Topics:** cadence partial save bugs, parallel run guard, resume progress bar, likelihood bin strategy, CvM/likelihood feature parity

**Work done:**
- **Fix 1 (partial table shows all "—"):** Cadence partial saves were missing `settings` JSON blob and `n_sets`. Built `_cad_stable_cfg` dict (mirroring Dsilva pattern) and added it to both partial and final cadence saves. Added fallback logic in `_scan_partial_metadata()` for old files.
- **Fix 2 (parallel run collision):** Added `_job_running` guard to cadence tabs — prevents starting a 2nd run while one is active (orphaned job dict bug).
- **Fix 3 (progress resets on resume):** Track `_total_original` and `_pre_done` so progress shows overall completion including pre-completed checkpoint cells.
- **Fix 4 (resume creates new file):** Pass `resume_from_path` through params so cancel-save overwrites the original checkpoint file instead of generating a new one.
- **Likelihood bin strategy:** Added `dsilva_likelihood_bins(threshold)` helper to `wr_bias_simulation.py`. Added threshold number_input (default 45.5 km/s) to all 4 tabs when likelihood scoring selected. Bin edges now passed through `_init_worker` and stored in results.
- **Full likelihood feature parity with CvM:** Extended `_render_cvm_analysis()` with `mode` parameter. When `scoring_method == 'likelihood'`, shows: -logL heatmap, masked heatmap (L ≥ 5% of max), normalized likelihood heatmap, binned bar chart (observed ΔRV bin counts), grid exclusion controls, parabolic fit interpolation, 1D slice plots. All replicated across Dsilva, Langer, Cadence Dsilva, Cadence Langer tabs.
- Likelihood corner plots now expanded by default (`expanded=True`).
- `obs_delta_rv` and `likelihood_bin_edges` added to all result dicts (Dsilva, Langer, Cadence).

**Decisions:**
- Simplified bin strategy to just the Dsilva coarse approach with adjustable threshold (user said "just add a new option"). No equal-width or adaptive for likelihood bins.
- Reused `_render_cvm_analysis` rather than creating a new function — mode parameter switches labels and skips S_raw heatmap for likelihood.
- Masked heatmap for likelihood uses L < 5% of max threshold (analogous to CvM's p ∈ [0.05, 0.95]).

**Files modified:**
- `app/pages/05_bias_correction.py` — major changes (function signature, 5 call sites, UI controls, result dicts, corner plot expansion)
- `wr_bias_simulation.py` — added `dsilva_likelihood_bins()` function
- `settings/user_settings.json` — added `likelihood_threshold: 45.5` to all 4 grid sections
- `Backups/05_bias_correction.py.bak` — backup created

**Open questions:**
- Need to run error-check skill to verify no runtime errors
- User needs to test all 4 tabs with likelihood scoring in the webapp
- The Dsilva/Langer regular tabs pass `_lk_bin_edges` via `_init_worker` positional args — need to verify the worker actually uses them (fallback chain in workers may still use DEFAULT_DRV_BIN_EDGES instead of DSILVA_LIKELIHOOD_BINS when `likelihood_bin_edges=None`)

**Memory updates:** None

**COMMON_ERRORS:** No new greppable patterns identified. The parallel run collision (orphaned job dict) is an architectural bug, not a code pattern.

---
## 19:19 — Task #52: Statistical RV Modeling page (full rebuild + bug fixes)

**Topics:** Task #52 RV modeling page, two-component mixture model, Gaussian vs empirical binary models, parameter playground, StreamlitAPIException E038, orbital parameters, RV error model selector

**Work done:**
- **Full rewrite of `app/pages/12_rv_modeling.py`** — rebuilt from scratch to fix all 5 rejection issues from 2026-03-15 (plot overflow, black-on-black colors, incomplete notebook coverage, no interactivity, no model choice)
- **Two models side-by-side:** Empirical (simulation-based, fits f_bin + σ_single) and Gaussian (analytical two-sigma, fits σ_single + σ_binary + f_bin). Both always computed and overlaid
- **4-tab layout:** Sample Fit | Fraction Recovery | Global Correction | Population Sim (placeholder)
- **Tab 2 — Fraction Recovery:** f_recovered(t) plot with noise/signal shaded regions, interactive threshold slider
- **Tab 3 — Global Correction:** Bartzakos prior "raise the dots", sample vs global comparison, centered RV histograms (singles vs binaries)
- **Parameter Playground at top of page** — simulation controls + instant sliders (f_bin, σ_single, σ_binary update plots without re-simulation; orbital params need Recompute button)
- **Fixed E038 bug (×2):** `st.session_state[widget_key] = value` after widget instantiated. Fix: (1) store best-fit in internal `_rvm_bestfit_*` keys, read as slider defaults; (2) preset buttons use `on_click` callbacks
- **Added E038 to COMMON_ERRORS.md** — new pattern for future error checks
- **Removed amplitude parameter** — was a fitting artifact, not physical
- **Added full orbital parameters** matching bias correction page: σ_single, σ_measure (with full error model selector: Fixed/Normal/Log-normal/Gamma/Weibull/Exponential/Flat), logP_min/max, q model (5 options), q_min/max, q_flipped, langer_q_mu/sigma, mass_primary_model (fixed/uniform), Langer component distributions (dist_A/B, μ_A/B, σ_A/B)
- **Preset buttons** (Dsilva/Langer) set all parameters to appropriate defaults
- **User refactored page to thin wrapper** — extracted logic into `app/rv_modeling.py`, page is now just imports + `render_rv_modeling_page()`

**Decisions:**
- Both models always shown (user preference: "both side-by-side")
- Population Simulation tab deferred to follow-up task
- Amplitude parameter removed entirely (user: "remove entirely")
- Split interaction: f_bin/σ update instantly, orbital params need Recompute button (user preference)
- Grid-search-specific controls NOT added (fbin/pi ranges, sigma scan, logPmax scan, ΔRV bins, n_proc, canvas size)

**Files modified:**
- `app/pages/12_rv_modeling.py` — full rewrite (now thin wrapper after user refactor)
- `app/rv_modeling.py` — new file (user extracted the logic here)
- `COMMON_ERRORS.md` — added E038 (session_state write after widget)
- `Backups/12_rv_modeling.py.bak` — backup before first edit
- `Backups/12_rv_modeling.py.bak2` — backup before orbital params addition

**Open questions:**
- User said "we will improve it more tomorrow" — further iteration expected
- Population Simulation tab still placeholder
- Need to verify all new orbital params actually reach the simulation engine (especially Langer component dists and mass model)
- User refactored to thin wrapper pattern — need to verify `app/rv_modeling.py` works correctly

**Memory updates:** None saved (all info captured in this log)

**COMMON_ERRORS:** Added **E038** — `st.session_state[widget_key] = value` after widget instantiated. Fix: use internal keys + `on_click` callbacks. Not greppable (requires flow analysis).

---
## 12:00–19:19 — Unified multi-score bias correction + file split

**Topics:** multi-score refactor, all 4 scoring methods (KS/weighted/CvM/likelihood), file split into app/bc/ package, 15+ UI bugs, live heatmaps, colorbar labels

**Work done:**
- **Phase 1 — Simulation engine** (`wr_bias_simulation.py`): Modified `_single_grid_task_lite` and `_cadence_aware` workers to compute ALL 4 scoring methods per grid point from the same simulated data. Removed `scoring_method` parameter from `_init_worker`, `run_bias_grid`, `run_bias_grid_cadence_aware`. Return dict now has separate keys per method: `ks_D/ks_p`, `weighted_D/weighted_p`, `cvm_D/cvm_p/cvm_S_raw`, `likelihood/logL_raw`.
- **Phase 2 — Background runners**: All 3 runners (`_run_dsilva_bg`, `_run_langer_bg`, `_run_cadence_bg`) accumulate 4 method arrays, send `job['live_heatmaps']` dict with 4 entries, save all to .npz with `scoring_version=2`.
- **Phase 3 — UI removal**: Scoring method radio buttons removed from all tabs. N_sets and likelihood threshold always visible. Live polling shows 2×2 grid of all 4 methods.
- **Phase 4 — Summary + expanders**: New `SCORING_METHODS` registry, `_render_method_summary_section()` (comparison table + CDF comparison plot with error bands), `_render_method_expander()` (heatmap + metrics + CvM analysis + corner plot + model explorer) per method.
- **Phase 5 — Old section cleanup**: Removed ~1,274 lines of redundant old display code (old corner plots, old CvM analysis, old likelihood heatmap, animated 4D, per-sigma table). Kept bottom CDF.
- **Phase 6 — File split**: Split 9,977-line monolith into `app/bc/` package (10 files): `__init__.py`, `helpers.py` (992), `analysis.py` (1,689), `params.py` (488), `runners.py` (1,110), `dsilva.py` (1,564), `langer.py` (1,364), `cadence.py` (1,677), `extras.py` (1,218). Entry point: 125 lines.
- **Bug fixes (15+)**: Cadence Langer shape mismatch (squeeze pi dim), missing pandas imports, CDF starting at 0.2 (prepend origin), likelihood analysis min/max direction, corner plot axes swapped, Langer 2D metric cards, colorbar labels saying "K-S p-value" for all methods, live heatmaps disappearing after completion, duplicate CvM expanders, orphaned summary tables, scoring comparison not collapsible.
- **Score display**: Switched live/final heatmaps from p-values to raw scores (D/S/χ²/Likelihood) with proper colorbar labels.

**Decisions:**
- All 4 scoring methods computed in one pass (no performance regression — simulation cost is in ΔRV generation, not statistics)
- No backward compatibility for old .npz files (user approved deletion)
- Bottom CDF kept as global view (user choice)
- All old sections removed (user choice), kept bottom CDF only
- Files split into `app/bc/` subfolder (user's suggestion for organization)
- Heatmaps show scores not p-values (user's latest request)

**Files modified:**
- `wr_bias_simulation.py` — workers compute all 4 methods, remove scoring_method param
- `app/pages/05_bias_correction.py` — now thin 125-line entry point
- `app/bc/__init__.py` — new (package init)
- `app/bc/helpers.py` — new (utilities, SCORING_METHODS, metadata scanning)
- `app/bc/analysis.py` — new (method summary, expanders, CvM analysis, parabolic fitting)
- `app/bc/params.py` — new (orbital param UI editors)
- `app/bc/runners.py` — new (background simulation runners)
- `app/bc/dsilva.py` — new (Dsilva tab)
- `app/bc/langer.py` — new (Langer tab)
- `app/bc/cadence.py` — new (Cadence tabs + results)
- `app/bc/extras.py` — new (RV Errors + Compare tabs)
- `app/shared.py` — added `colorbar_title_override` to `make_heatmap_fig`

**Open questions (for tomorrow):**
1. Missing `_get_method_array` import in `app/bc/cadence.py` — quick fix
2. CDF legend groups — when toggling method off, error bands should also hide (need `legendgroup`)
3. Likelihood heatmap still looks very homogeneous — confirmed expected behavior (coarse Dsilva bins), but user may want finer bins

**Memory updates:** None saved (session context captured here)

**COMMON_ERRORS:** No new greppable patterns. Main bugs were: missing imports after file split (not greppable), default parameter values propagating wrong labels (architectural), shape mismatches in dimension handling (context-specific).

**Commits (12 total, revert point at 76de227):**
- `80638c2` Compute all 4 scoring methods per grid point
- `6acfaf6` Update background runners to accumulate all 4 methods
- `0790c30` Remove scoring radio buttons, show all 4 methods in UI
- `18d4049` Add scoring method summary table + per-method expanders
- `d1acfa2` Add CDF comparison plot to summary section
- `c76b2a6` Add corner plots and model explorer to per-method expanders
- `1b6e0de` Extract duplicated orbital param UI into shared helpers
- `955cb86` Fix cadence Langer shape mismatch
- `7f2e425` Fix bugs 1,2,11,12,13,14: live heatmaps persist, remove old sections
- `933c9ed` Fix bugs 3,4,7,8,9,10: CDF plot, likelihood analysis, corner plot, metrics
- `1d0e846` Split bias correction page into app/bc/ package
- `b2d8119` Fix missing pandas imports, colorbar labels, likelihood normalization
- `677bcc4` Show scores in live heatmaps instead of p-values

---
## 19:21 — Webapp file size audit + split 3 large pages into subpackages

**Topics:** file size limits, webapp reorganization, splitting large pages, thin-wrapper pattern, error-check

**Work done:**
- **Audited all webapp files** — identified 3 pages over 1,000 lines (excluding bias correction which user is handling): `06_plots.py` (1,456), `11_nres_analysis.py` (1,117), `12_rv_modeling.py` (1,193)
- **Split `06_plots.py` → `app/plots/`** (6 modules, max 825 lines): theme.py, data.py, analysis.py, xshooter.py, nres.py, page.py. Wrapper: 19 lines
- **Split `11_nres_analysis.py` → `app/nres/`** (5 modules, max 515 lines): config.py, data.py, spectra_tab.py, threshold_tab.py, page.py. Wrapper: 19 lines
- **Split `12_rv_modeling.py` → `app/rv_modeling/`** (4 modules, max 556 lines): helpers.py, compute.py, tabs.py, page.py. Wrapper: 23 lines. Used context dict pattern for tabs
- **Added `ROOT` export to `shared.py`** — public alias for `_ROOT` so subpackages import `from shared import ROOT` instead of computing paths themselves
- **Ran `/error-check`** on modified files — found 1 E034 (nanargmax without isfinite guard) in `app/bc/dsilva.py:646`, fixed it
- **Added file size limit rule** to both CLAUDE.md and memory

**Decisions:**
- 800 lines max per file — enforced going forward as a project rule
- Thin-wrapper pattern (like existing 10_todo.py) standardized for all split pages
- Files under 800 lines (shared.py 651, todo_core.py 782, spectrum 524) left as-is
- User handling bias correction split separately (app/bc/)

**Files modified:**
- `app/shared.py` — added `ROOT` export
- `app/plots/__init__.py`, `theme.py`, `data.py`, `analysis.py`, `xshooter.py`, `nres.py`, `page.py` — new (plots package)
- `app/nres/__init__.py`, `config.py`, `data.py`, `spectra_tab.py`, `threshold_tab.py`, `page.py` — new (nres package)
- `app/rv_modeling/__init__.py`, `helpers.py`, `compute.py`, `tabs.py`, `page.py` — new (rv_modeling package)
- `app/pages/06_plots.py` — replaced with thin wrapper (19 lines)
- `app/pages/11_nres_analysis.py` — replaced with thin wrapper (19 lines)
- `app/pages/12_rv_modeling.py` — replaced with thin wrapper (23 lines)
- `app/bc/dsilva.py` — fixed E034 nanargmax guard at line 646
- `CLAUDE.md` — added file size limit rule under Code Quality Rules
- `Backups/06_plots.py.bak`, `11_nres_analysis.py.bak`, `12_rv_modeling.py.bak` — backups

**Open questions:**
- User needs to test all 3 split pages in the webapp (Plots, NRES Analysis, RV Modeling)
- `xshooter.py` at 825 lines is slightly above 800 target but acceptable

**Memory updates:**
- Created `memory/feedback_file_size_limit.md` — never let files exceed ~800 lines, pre-plan splits
- Updated `MEMORY.md` index with new entry

**COMMON_ERRORS:** No new patterns. The E034 fix was an existing pattern applied to a new location.

**Commits:**
- `3f52b16` Split 06_plots.py into app/plots/ package
- `aadfe0d` Split 11_nres_analysis.py into app/nres/ package
- `17fcb69` Split 12_rv_modeling.py into app/rv_modeling/ package
