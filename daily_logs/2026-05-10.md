# Daily Log — 2026-05-10

---
## 15:56 — Validation Model Explorer: smooth pooled CDF + 16/84 band

**Topics:** bias correction validation tab, Model Explorer CDF, uncertainty-band shading, smooth empirical CDF from pooled ΔRVs, reuse of existing `smooth_pooled_cdf` helper, Dsilva/Langer parity

**Work done:**
- User reported: Model Explorer CDF in the Validation tab shows no std/uncertainty shadow despite running `n_sets` MC iterations, and the step CDF is unnecessarily coarse — wants a band AND smoothing using all `n_sets × n_stars` pooled ΔRVs.
- Explore agent traced the code: `_render_lk_model_explorer` lives in two variants — [app/bc/render_lk_explorer.py:1124](app/bc/render_lk_explorer.py#L1124) (Dsilva) and [app/bc/render_lk_explorer_langer.py:698](app/bc/render_lk_explorer_langer.py#L698) (Langer). The CDF trace block in each had an explicit comment `# Explorer median CDF (no error band)` — confirmed missing band.
- Discovered the exact helper needed already exists: `smooth_pooled_cdf(pooled, n_sets, n_fine=500)` at [app/bc/helpers.py:118](app/bc/helpers.py#L118), already used by [render_shared.py:637-659](app/bc/render_shared.py#L637) for the validation tab's other CDF panel. Returns smooth median + 16/84-percentile band on a fine 500-point grid.
- Asked 3 clarifying questions via AskUserQuestion (band type, overlay scope, variant scope) — all answered Recommended (16/84 percentile, both Explorer + best-fit overlay, both Dsilva + Langer variants).
- Wrote plan to [~/.claude/plans/in-the-model-explorer-generic-hamster.md](.claude/plans/in-the-model-explorer-generic-hamster.md). User approved without edits.
- Coder agent applied the patch: 3 hunks per file (import + Explorer trace block + best-fit overlay block). Reused the LOCAL `_hex_to_rgba` already defined at the top of each file instead of re-importing from `bc.helpers` — flagged as deliberate deviation (functions are byte-equivalent, avoids shadowing/redefinition warning).
- `/error-check` PASS — pyflakes clean, py_compile clean, real-data render test 5/5 PASS, all 6 downstream consumers (render_lk, render_lk_langer, render_validation, render_shared, render_shared_langer, validation) re-import cleanly. Helper unit-smoke confirms shapes/monotonicity/None edge cases.
- Visual sign-off NOT taken (per `feedback_no_self_approve.md`). User must reload the webapp and confirm: smooth dashed red line + translucent red band on Explorer (current); dotted red + lighter band on best-fit overlay when checkbox ticked; both bands compose readably; band tracks slider movement; vrect bin overlay still visible behind new traces; Langer page identical.

**Decisions:**
- **Band convention: 16/84 percentile** — matches project standard (Panel B, render_shared.py marginal/grid CDFs, `feedback_honest_labels.md` 68% HDI rule).
- **Apply to both current AND best-fit overlay traces** for visual consistency. Current = dashed red @ 0.20 fill opacity; best-fit = dotted red @ 0.12 fill opacity (so the two bands compose without illegibility).
- **Apply to both Dsilva and Langer variants** to maintain UI parity.
- **Reuse `smooth_pooled_cdf` instead of writing new code** — existing helper does exactly the right thing, validated visually elsewhere in the app.
- **Drop `shape='hv'` on the median line** — that was the source of the staircase look. Smooth pool sort makes it natural.
- **Keep the original step trace as the empty-pool fallback** — `if _scdf is None: <original trace>`. Never silently lose the line; never show false data.
- **Do NOT touch the WORKING flag** above `_render_lk_model_explorer` — user explicitly authorised the change so the FLAG WORKING CODE rule (#5) is satisfied; the change is documented in this log + plan file + commit message will reference both.

**Files modified:**
- [app/bc/render_lk_explorer.py](app/bc/render_lk_explorer.py) — 3 hunks (import + 2 trace blocks)
- [app/bc/render_lk_explorer_langer.py](app/bc/render_lk_explorer_langer.py) — 3 hunks (import + 2 trace blocks)
- [~/.claude/plans/in-the-model-explorer-generic-hamster.md](.claude/plans/in-the-model-explorer-generic-hamster.md) — created
- [daily_logs/2026-05-10.md](daily_logs/2026-05-10.md) — created (this file)

**Open questions:**
- Visual sign-off in the actual webapp still pending. User should reload Bias Correction → Validation tab → expand Model Explorer (Dsilva and Langer pages independently).
- TODO.md not yet updated with this entry — should be added at to-test status before commit. (No commit pending until visual sign-off confirms.)

**Memory updates:** None this session.

**No new COMMON_ERRORS patterns** — this was a feature addition, not a bugfix; no recurring greppable code pattern surfaced.

**No new learnings** — the workflow (Explore → AskUserQuestion → Plan → coder agent → /error-check → wait for visual sign-off) executed cleanly with no corrections; no new mistakes; no surprising successes worth promoting. Existing `learnings.md` rule "ALWAYS plan before non-trivial code changes" was followed (plan mode used end-to-end).

---
## 17:42 — Built mock_inspector_app standalone webapp + 8 iteration rounds + cross-tab divergence diagnosis

**Topics:** new standalone Streamlit webapp (separate from app/), 3x3 orbital-params overlay grid, Mock vs Model Explorer pipelines, multiprocessing experiment + revert, single-run feature with red/green per-star dots, multi-seed accumulation, std band controls, mock-data divergence between new GUI and validation tab

**Work done:**
- Designed + built `mock_inspector_app/` — a NEW standalone Streamlit webapp (not a page in app/), launched on port 8503. Files: `__init__.py`, `settings.py`, `runner.py`, `inspector.py`, `app.py`. Imports from existing project (`bc.helpers`, `wr_bias_simulation`, `pipeline.load_observations`) but writes to its own `mock_inspector_app/settings.json`.
- Compares two pipelines side-by-side: **Mock Data** (`rng.choice` for binary indices) vs **Model Explorer** (`rng.permutation`). Both call same inner `simulate_delta_rv_cadence_aware` → byte-identical orbital draws; only binary-index assignment differs.
- Per pipeline: ΔRV CDF (smooth pooled empirical + 16-84 band via `bc.helpers.smooth_pooled_cdf`), f_bin-vs-threshold curve, 3x3 orbital histograms grid (logP, e, q, K1, M1, M2, i_deg, ω_deg, T0_rad — matching `render_shared_langer.py:1014` `render_orbital_histograms` convention).
- Iteration rounds (chronological): (1) initial side-by-side columns build → (2) restructured to 3x3 overlay grid with Mock blue + Explorer red overlaid in each panel → (3) tried `multiprocessing.Pool` parallelization but spawn overhead on macOS BLOCKED Streamlit thread; reverted to pure serial loop with inline 100-call progress throttle → (4) allowed N_iter=1 + removed observed-CDF trace → (5) added "🎯 Add single run" button with red/green per-star dots → (6) added step LINE through dots on CDF + same dots+step line on f_bin panel → (7) added seed input + accumulate multiple single runs + "🧹 Clear single runs" button → (8) added 16-84 std band on f_bin panel + user-controlled "Std band bins" number_input above both panels.
- **Cross-tab divergence investigation**: user reported new GUI gives different mock data than validation tab at same seed/params. First bisect was wrong (compared GUI defaults to GUI defaults, missed the actual validation-tab UI defaults). Re-bisect found root cause: σ_meas default in new GUI = 5.0 km/s, in validation tab (extras.py:578) = 1.622 km/s. Empirical proof: ΔRV is peak-to-peak of noisy measurements, so σ_meas DIRECTLY inflates each star's ΔRV. User pushed back claiming σ_meas shouldn't matter; ran live bisect showing σ_meas=5.0 vs 1.622 produces completely different sorted ΔRV arrays at same seed.
- Audit revealed FIVE different binary-index sampling methods across the codebase (`rng.choice`, two `rng.permutation` sites, two `rng.random < f_bin` Bernoulli sites at `wr_bias_simulation.py:1073` and `:2132`). The Bernoulli sites violate the `feedback_mock_deterministic.md` rule (n_bin should be exactly `round(N × f_bin)`, not Bernoulli). Surfaced as a follow-up worth standardising.
- All edits dispatched via the coder agent per project rule "coder does all code edits"; orchestrator never wrote code directly. plots + qa + scientist agents jointly verified plot fidelity (titles/legends/axes match data) at multiple checkpoints. /error-check ran clean after every edit.

**Decisions:**
- 3x3 grid uses `make_subplots(rows=3, cols=3)` despite the project's "no make_subplots" preference, because the existing bias correction page (`render_shared_langer.py:1072`) uses the same pattern for the same use case. Project rule wasn't absolute — applies when subplots cause cross-figure interference.
- Multiprocessing reverted at user request after spawn overhead made the UI freeze at N_iter=5000. Spec was correct (byte-identical to serial, throttle to 100 callbacks max), but `mp.Pool(spawn)` blocks the Streamlit main thread for ~5-10s during worker startup — no workaround short of `concurrent.futures.ProcessPoolExecutor` in a background thread + `st.fragment` polling, which is significant complexity for marginal gain.
- Single-run dots/step lines accumulate across multiple seed clicks; aggregated into one red + one green trace; per-run step lines share `legendgroup='single_runs'` so they collapse to ONE legend entry. Run ▶ clears them; Clear button wipes them manually; parameter changes leave them (user can clear via either button).
- σ_meas default mismatch is REAL but user has not yet decided how to fix (left at 5.0; can be changed manually). Discussion ended at the empirical-proof step.

**Files modified:**
- NEW (this session): `mock_inspector_app/__init__.py`, `mock_inspector_app/settings.py`, `mock_inspector_app/runner.py`, `mock_inspector_app/inspector.py`, `mock_inspector_app/app.py` — total ~2200 lines across the 5 files.
- Auto-created on first save: `mock_inspector_app/settings.json`
- No changes to existing project files (`app/**`, `wr_bias_simulation.py`, `pipeline/**`, `bc/**` all untouched per "don't touch working code" rule).

**Open questions:**
- **σ_meas default**: change new GUI default from 5.0 → 1.622 to match validation tab? (User asked; awaiting decision.) Two options: (a) change default + leave knob, (b) keep at 5.0 with caption noting validation-tab default. User has not picked.
- **n_iter default**: validation tab uses n_iter=1 by design; new GUI uses 500 for the smoothed pooled band. If aligning defaults, also change to 1? Or expose both modes?
- **Bernoulli f_bin sites in `wr_bias_simulation.py:1073, :2132`** — violate the deterministic-mock rule. Not addressed this session; raised as follow-up.
- **Multiprocessing for very large N_iter** — `concurrent.futures.ProcessPoolExecutor` + `st.fragment` background polling pattern would work but wasn't implemented. Reverted to serial.
- **Visual sign-off pending** on the multi-seed accumulation + band bins control + single-run line on CDF + f_bin band.

**Memory updates:** None this session — the memory rules already cover the relevant patterns (no-false-data, no-self-approval, coder-does-edits, surgical-edits, ui-persist-on-change, mock-deterministic). The "GUI for X = dedicated interactive inspector, not generic CLI wrapper" rule from 2026-05-07 was vindicated this session — the user wanted exactly the kind of dedicated webapp that rule describes.

**No new COMMON_ERRORS patterns** — the `mp.Pool(spawn)` blocking Streamlit issue is architecture-specific, not a greppable code pattern. The σ_meas-default-mismatch is a defaults problem, also not greppable.

**New learnings (2):**
1. **Don't dismiss a user-reported divergence after one incomplete bisect.** Cost: 1 round-trip where I confidently told the user "the two paths are byte-identical, your screenshots must have different f_bin" — the user replied "no no dont gaslight me, its all 100% the same parameters". Re-investigation found a real σ_meas default mismatch I had missed because my first bisect script passed `sigma_meas=5.0` to both paths (comparing GUI defaults to GUI defaults). The fix: before claiming "no divergence", trace the OTHER tab's actual UI defaults from its source code, don't assume. Will add to learnings.md under Context & Memory section.
2. **σ_meas affects ΔRV directly** — not just an errorbar. ΔRV = peak-to-peak of `orbital_signal + N(0, σ_meas)` per epoch. So changing σ_meas from 1.622 → 5.0 makes the noise floor of single-star ΔRVs jump from ~3-7 km/s to ~9-20 km/s. This is documented in the validation tab's hidden defaults (extras.py:578). When troubleshooting cross-pipeline ΔRV divergences, σ_meas is the first thing to check.

---
## 17:45 — Validation Model Explorer mock-vs-grid CDF mismatch — FOUND: q_range default mismatch (E048 recurrence)

**Topics:** bias-correction Validation tab Model Explorer, mock vs grid generative-model divergence, BinaryParameterConfig partial-field construction bug, dataclasses.replace fix, my own debugging mistakes

**Work done:**
- User reported: Model Explorer dashed CDF (at user-set truth params) visibly fits the mock observation worse than the algorithm best-fit dotted CDF, even though those exact truth params generated the mock. Suspected "obvious bug, must be resolved".
- Launched 3 parallel investigators (coder, scientist, qa) to attack from different angles. First hypothesis: logP_max plumbing bug in cadence.py:1230-1236. User pushed back: "you could have checked from the saved file" — I had assumed instead of reading the .npz. Direct inspection of `mock_results/validation_dsilva_fbT0.70_piT3.00_sigT10.0_logPT3.50_seed42_*.npz` confirmed `logPmax_grid = [3.5]`. logP_max was correct in both paths; my first hypothesis was wrong.
- Then chased "1.0-logL truth-cell reproducibility gap" between stored grid logL and deterministic recomputation. Couldn't reproduce -39.15 from any seed in 0..1000 — fresh code gave -38.12 ± 0.04 across 200 seeds. Looked like older-code drift but couldn't confirm.
- Then chased "the saved mock cannot be reproduced from its metadata" — `_sample_delta_rv_mock`/`simulate_delta_rv_cadence_aware`/`generate_mock_observations` at seed=42 with the saved cadence/bin_cfg/sigma_meas all gave DIFFERENT mocks (max=223/244/244 vs saved max=186). User clarified "I just clicked Generate, didn't run a full grid; mock looked same in app vs saved file" — confirming the live UI reproduces the saved mock byte-identically, but my standalone scripts don't.
- That mismatch was the breadcrumb: read [render_validation.py:351-357](app/bc/render_validation.py#L351-L357) and found the smoking gun — UI builds `BinaryParameterConfig(logP_min=0.15, logP_max=true_logPmax, period_model=period_model, e_model=e_model, e_max=0.9)` with only 5 fields explicitly set, leaving `q_range` at dataclass default `(0.1, 2.0)` per [wr_bias_simulation.py:318](wr_bias_simulation.py#L318). The grid uses the FULL `bin_cfg` from the orbital-params widget, which is `q_range=(0.1, 4.0)` for this user's settings. Higher q ⇒ heavier companion ⇒ larger K1 ⇒ larger ΔRV. Mock samples narrower ΔRV distribution than grid evaluates against.
- Numerical confirmation: at truth (0.7, 3.0, 10.0, logP_max=3.5), mock smooth (q≤2) vs grid smooth (q≤4) differ by 0.05–0.09 in CDF across the entire ΔRV range — exactly the offset the user kept pointing at. Saved mock empirical CDF is consistent with q≤2 model (within 1σ for N=25); appears as +2σ outlier vs the q≤4 model the Explorer plots.
- Plan file written, user approved. Coder agent applied the fix at `render_validation.py:347-404`. Since `bin_cfg` is not in scope at that call site (it's `_render_single_point` which receives only `settings` + `cadence_*`), the coder built `_base_bin_cfg` from `settings['grid_cadence_dsilva']['orbital']` (Dsilva) or `settings['grid_cadence_langer']` (Langer) — same source `cadence.py` reads — then `dataclasses.replace(_base_bin_cfg, logP_min=0.15, logP_max=true_logPmax, period_model=period_model, e_model=e_model, e_max=0.9)` for the truth-specific overrides. /error-check passed all 15 checks.

**Decisions:**
- **Build base bin_cfg from settings sub-dict, not from any in-scope `bin_cfg` variable** — because `bin_cfg` is not in scope at the call site (function only receives `settings`). The settings dict is the single source of truth that both `cadence.py` and the orbital-params widgets read.
- **Override only truth-specific fields with `dataclasses.replace`** — `logP_min`, `logP_max`, `period_model`, `e_model`, `e_max`. Everything else (q_range, mass_primary_*, langer_*, q_flipped) inherits from the user's orbital-params widget.
- **Do NOT modify the grid worker side** — the fix is one-sided (mock generator only). Grid is the source of truth for physics knobs; the mock just needs to inherit them.
- **Visual sign-off NOT taken** — per `feedback_no_self_approve.md`. User must reload the webapp tomorrow, set truth + q_range=(0.1, 4.0), Generate, and verify the mock empirical CDF max ΔRV is now in typical range for q≤4 (often >200; fresh code at seed=42 gives max=244). Validation grid run should land Explorer-at-truth logL within single-mock noise of the algorithm's best.

**Files modified:**
- [app/bc/render_validation.py](app/bc/render_validation.py) lines 347-404 — replaced 11-line `BinaryParameterConfig(...)` construction with `_base_bin_cfg` from settings + `dataclasses.replace(...)` overrides. Both Dsilva and Langer branches handled.
- [COMMON_ERRORS.md](COMMON_ERRORS.md) — appended "Recurrence 2026-05-10" note to E048 (Re-sim helper builds fresh `BinaryParameterConfig`) — same root cause at a new call site (the mock generator, not a re-sim helper). Rule extends.
- [.claude/references/learnings.md](.claude/references/learnings.md) — updated `last_triggered` to 2026-05-10 on the two existing rules about full bin_cfg / period_model threading; added new entry under Data & Debugging about running empirical comparison BEFORE repeating a "single-mock noise" explanation when user pushes back.
- [~/.claude/plans/in-the-model-explorer-replicated-tower.md](.claude/plans/in-the-model-explorer-replicated-tower.md) — investigation plan + final fix; user approved.
- [daily_logs/2026-05-10.md](daily_logs/2026-05-10.md) — this entry.

**Open questions:**
- **Visual sign-off pending tomorrow** — user said "we will test tomorrow". TODO entry stays at to-test.
- **Does the fix also resolve the ~1.0 logL stored-vs-recomputed truth-cell gap?** — Likely yes (stored grid scored against a mock generated under a different model than the grid's own physics), but won't know until a fresh validation run is done. If it persists, separate investigation.
- **Other call sites of `BinaryParameterConfig(...)` with subset-of-fields construction** — E048 grep is `BinaryParameterConfig\(logP_max=`; today's instance was caught only by visual review. Worth a broader audit grep `BinaryParameterConfig\(` and triaging each construction site. Deferred.

**Memory updates:** None saved — the existing memory rules (`feedback_no_false_data.md`, `feedback_no_self_approve.md`, `feedback_coder_does_edits.md`, `feedback_surgical_edits.md`) all already covered the workflow.

**COMMON_ERRORS update:** E048 (existing) extended with 2026-05-10 recurrence note — `app/bc/render_validation.py:351-357` was a new instance of the same partial-`BinaryParameterConfig`-construction pattern. No new E-number minted (same root cause).

**New learnings (1):**
1. **When the user pushes back on "this is statistical noise", run the empirical comparison BEFORE re-presenting the argument.** Cost ~90 minutes today: I had a coherent statistical story (Wilks Δχ²=4.6 → 88% joint confidence → "expected for 25-star draws") and presented it ~5 times in different framings as the user kept saying "no, 5 seeds all cluster, this isn't noise". The user was right; the actual bug was a 5-line UI mock-config construction that any direct distribution-comparison test would have caught immediately. Rule: when user reports systematic divergence and I have a "noise" explanation, FIRST move is `python -c "compare distribution path A vs path B numerically"`. If empirical agrees with noise → present numbers. If empirical disagrees → find the real bug. Added to learnings.md under Data & Debugging at top of section. (Note: this is an instance of the existing "verify explore-agent hypotheses against the code before accepting them" rule at line 37 — but generalized to my OWN hypotheses, not just sub-agents'.)
