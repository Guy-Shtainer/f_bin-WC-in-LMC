# Daily Log — 2026-04-30

---
## 01:16 — Three-stage binarity strategy + 5 validation runs queued for overnight
**Topics:** binary fraction pipeline architecture, bias correction → binary RV distribution → RV Modeling chain, error propagation from posteriors, σ_single staging question, validation simulation matrix
**Work done:**
- Captured Guy's proposed three-stage workflow as a project memory: Stage 1 = bias correction with no f_bin/threshold assumed, finds best (π, logP_max[, σ_single]); Stage 2 = use winning model to draw many systems (cadence-free) and fit a parametric distribution to binary RVs; Stage 3 = plug that distribution into the RV Modeling tool to solve for observed f_bin, threshold, and σ_single.
- Logged the open error-propagation question: passing a single best-fit point loses corner-plot uncertainty; two candidate framings (sample marginals → distribution-of-distributions, or track how Stage-2 PDF parameters scatter across Stage-1 posterior draws).
- Captured five validation simulations launched this evening for tomorrow's review: Tabs 1–2 (low/high f_bin extremes with σ_single pinned to truth), Tabs 3–4 (sensitivity to an extra histogram bin near the steep CDF slope), Tab 5 on port 8502 (σ_single included as a free search dimension).
- Linked the σ_single-search run (Tab 5 on :8502) to the three-stage memo as the decisive test for whether σ_single can live in Stage 1 or must be deferred to Stage 3.
**Decisions:**
- Treat the three-stage chain as the target architecture for the paper's conclusion — the binary RV shape becomes a *derived* quantity rather than an assumed one.
- Defer the σ_single-staging decision until Tab 5 (:8502) results land tomorrow; if recovery is clean, σ_single moves to Stage 1, otherwise it stays in Stage 3.
- Error-propagation framing left open — flagged for raising rather than silently deciding.
**Files modified:**
- Created `memory/project_three_stage_binarity_strategy.md` (project memory)
- Created `memory/pending_validation_runs_2026_04_30.md` (project memory, updated mid-conversation to add Tab 5 on :8502)
- Updated `memory/MEMORY.md` Live State index (two new entries)
**Open questions:**
- Stage 1 → Stage 2 error propagation: distribution-of-distributions vs. parameter-scatter framing — needs a deliberate choice before Stage 2 is implemented.
- Does the bias-correction sim recover σ_single when it's a free parameter? (Pending Tab 5 on :8502.)
- Does the recovery hold at f_bin extremes when σ_single is pinned? (Pending Tabs 1–2.)
- How sensitive is the recovery to bin placement near the steep CDF slope? (Pending Tabs 3–4.)
**Memory updates:**
- Added: `project_three_stage_binarity_strategy.md`
- Added: `pending_validation_runs_2026_04_30.md`
- Indexed both in `MEMORY.md` under Live State
**No new COMMON_ERRORS patterns** (no bugs fixed this conversation — planning/memory work only).
**No new learnings** (no corrections this session).

---
## 01:17 — Likelihood Model Explorer ↔ grid score parity

**Topics:** Model Explorer logL vs grid logL_raw mismatch, validation-mode mock-replay shortcut, deterministic n_bin, Run-button vs auto-update tradeoff, CDF trace truncation at top, mock binary-count discrepancy (19 vs 18), banded 2D heatmap from per-set n_bin quantisation.

**Work done:**
- Traced both score paths side-by-side: Explorer's `_me_cdf_band` (validation_mode → `_sample_delta_rv_mock` with mock seed and forced n_sets=1; real-obs → `simulate_delta_rv_cadence_aware` with hardcoded `default_rng(42)`) vs grid worker `_single_grid_task_cadence_aware` (`default_rng(seed_base + idx_cell)`, `n_sets`, multinomial against `likelihood_bin_edges`).
- Confirmed CDF visualization bins (`bin_edges`, default 36-edge `np.arange(0, 360, 10)` or adaptive) are completely independent of likelihood scoring bins (`likelihood_bin_edges`, default Dsilva 4-bin `[0, 45.5, 250, 650, ∞]`). The 36 CDF edges only feed the median/16/84 CDF arrays for plotting; the score uses raw pooled ΔRV and `lk_be` only.
- Coder agent batch 1: forced deterministic `n_bin = round(N × f_bin)` in both `simulate_delta_rv_sample` (line 715) and `simulate_delta_rv_cadence_aware` (line 930); added `_explorer_run_grid_pipeline` helper; gated Explorer score+CDF behind a 🔬 Run button; dropped validation_mode from `_me_cdf_band`; applied to both Dsilva and Langer twins.
- Coder agent batch 2 (user reversed direction — fast enough to auto-update): removed Run button; added cached wrapper `_explorer_run_grid_pipeline_cached` keyed on (f_bin, π, σ, logP_max, n_sets, seed_base+idx_cell, hashable cfg); promoted `_n_sets_me` to a user-editable `st.number_input` (default = `result.get('grid_n_sets', 1000)`, min=1, no max, step=100, persisted to `lk_explorer.n_sets` / `lk_explorer_langer.n_sets`); auto-recompute on every slider/number-input change with "Computing logL…" spinner only on cache miss. Global best metric still reads directly from `logL_raw[i_sig_best, i_fb_best, i_pi_best]`.
- Coder agent batch 3 (CDF truncation): appended `(pooled_max, 1.0)` final point to Explorer (current) and Best-fit (algorithm) traces in both files; removed `_x_candidates` block + hardcoded `xaxis_range=[0, _xmax_cdf*1.05]` so plotly autofocuses from data extents.
- Diagnosed mock binary count: verified deterministic code at `app/bc/validation.py:222` produces exactly `int(round(25 × 0.70)) = 18` across all seeds. User's "19 of 25" most likely stale mock — slider was at 0.75/0.76/0.77 when Generate clicked (each `round(25×f) = 19`) then moved to 0.70 without regenerating. Mock cached in `st.session_state[mock_key]` and only regenerated on click.
- Diagnosed banded 2D heatmap: per-set `n_bin = round(25 × f_bin)` is a step function of f_bin. With 99 f_bin grid points, the integer n_bin only resolves to ~26 distinct values. n_sets averaging is so effective via LLN that the only structure left across nearby f_bin cells is the integer quantisation, producing horizontal bands ~0.04 wide. n_sets is NOT being ignored — it's working too well, smoothing orbital noise so completely that quantisation becomes the dominant cell-to-cell signal.

**Decisions:**
- Explorer logL must equal grid logL bit-for-bit at grid nodes — achieved by reusing `simulate_delta_rv_cadence_aware` with seed = `seed_base + idx_of_nearest_cell`, deterministic n_bin, same n_sets, same `multinomial_log_likelihood` against `lk_be`.
- Auto-update beats Run button when computation is cache-fast — reverted the Run-button gate after first round.
- `n_sets` is now user-editable inline (not in expander, no max constraint) — matches user-preference rules.
- Pending: switch from per-set deterministic n_bin to **aggregate-deterministic** floor/ceil distribution (`total_n_bin = round(n_sets × 25 × f_bin)` distributed across sets as floor or ceil). Restores smooth f_bin axis on heatmaps without losing exact-aggregate-fraction guarantee. User has not yet confirmed.

**Files modified:**
- `wr_bias_simulation.py` — deterministic n_bin in `simulate_delta_rv_sample` and `simulate_delta_rv_cadence_aware`.
- `app/bc/render_lk_explorer.py` — `_explorer_run_grid_pipeline` + cached wrapper, removed `validation_mode` branch in `_me_cdf_band`, n_sets number_input, CDF trace extension to (pooled_max, 1.0), removed hardcoded x-range.
- `app/bc/render_lk_explorer_langer.py` — same changes as Dsilva twin.
- `daily_logs/2026-04-30.md` (this entry).

**Open questions:**
- Floor/ceil aggregate-distribution fix for heatmap banding — awaiting user "ship it" or alternative.
- Should we add a "(parameters changed since last generate — click Generate again)" caption next to the f_bin slider when current_params ≠ session_state[mock_params_key]? Offered but not yet confirmed.

**Memory updates:** None. Existing rules (`feedback_mock_deterministic.md`, `feedback_ui_persist_on_change.md`, no min/max constraints, inline controls) all already covered the patterns used.

**No new COMMON_ERRORS patterns** — root causes were design issues (sampler divergence, integer quantisation), not greppable error patterns.

**No new learnings** — feedback rules from prior sessions covered every constraint correctly.

---
## 16:31 — Quick recall: simulation tabs + three-stage binarity strategy
**Topics:** bias correction page tab types, validation run matrix recap, three-stage binarity strategy recall
**Work done:**
- Listed the six tab types supported by `app/pages/05_bias_correction.py` (cadence_dsilva, cadence_langer, bin_sensitivity, rv_errors, compare, validation) by reading the dispatch block at lines 109–124.
- Recapped the 5 validation runs from the morning entry (Tabs 1–2 f_bin extremes σ_single pinned, Tabs 3–4 bin-at-slope, Tab 5 :8502 σ_single search ON) from `pending_validation_runs_2026_04_30.md`.
- Re-surfaced the full three-stage binarity strategy from `project_three_stage_binarity_strategy.md` — Stage 1 bias correction, Stage 2 binary RV distribution model, Stage 3 RV Modeling tool — including the σ_single placement and posterior error-propagation open questions.
**Decisions:** None — pure recall conversation, no new work.
**Files modified:**
- `daily_logs/2026-04-30.md` (this entry).
**Open questions:** Carried forward from earlier entries — floor/ceil aggregate distribution for heatmap banding, σ_single recovery validation, error propagation Stage 1 → Stage 2.
**Memory updates:** None — all relevant context was already in saved memory files.
**No new COMMON_ERRORS patterns** — recall conversation, no bugs fixed.
**No new learnings** — no corrections or surprises.

---
## 16:31 — logP_max scan: fixed-value input box

**Topics:** validation tab UI, logP_max scan expander, BinaryParameterConfig override

**Work done:**
- Added a `logP_max (fixed)` `st.number_input` to `_render_logPmax_scan` (`app/bc/params.py`) that appears when the scan toggle is OFF. Persists under `[sec, 'logPmax_fixed']` via `sm.save` on change. Default seeds from existing `f'{p}_logP_max'` session state on first render.
- Modified Dsilva and Langer cadence tabs (`app/bc/cadence.py`) to rebuild `_bin_cfg = dataclasses.replace(_bin_cfg, logP_max=float(_*_logPmax_vals[0]))` after the scan expander returns, so the typed fixed value flows into the simulation. Added `import dataclasses` at module top.
- Did NOT touch `runners_cadence.py` (marked WORKING).
- `/error-check` returned PASS (5-phase, including project render test).

**Decisions:**
- New widget key (`f'{p}_logPmax_fixed'`) instead of trying to reuse the orbital section's `f'{p}_logP_max'` — Streamlit forbids duplicate keys, and decoupling lets the scan expander act as the single source-of-truth for the simulation when scan is OFF without sync gymnastics.
- Override `_bin_cfg.logP_max` upstream rather than modify the runner — safer (runner is marked WORKING) and works identically for scan-ON (first scan value placeholder, runner overrides per-iteration anyway).
- Delegated the actual code edits to the coder agent per `feedback_coder_does_edits.md` (orchestrator never writes code).

**Files modified:**
- `app/bc/params.py` — `_render_logPmax_scan` now seeds + renders fixed-value input + new caption + new return when scan OFF.
- `app/bc/cadence.py` — added `import dataclasses`, two `dataclasses.replace` calls (one per tab) to override `_bin_cfg.logP_max` after the scan expander.
- `daily_logs/2026-04-30.md` (this entry).

**Open questions:**
- Follow-up plot request was raised mid-session ("blend stds into left y-axis of RVs, clear right y-axis, put ΔRV peak-to-peak with error on right axis") but the target plot was not identified before user ended the conversation. Needs the page name or plot title next session.

**Memory updates:** None. Existing `feedback_coder_does_edits.md`, `feedback_ui_persist_on_change.md`, and `feedback_surgical_edits.md` all applied cleanly without new rules.

**No new COMMON_ERRORS patterns** — change was a feature add, not a bug fix.

**No new learnings** — coder agent followed the existing protocol; user confirmed the change with "nice." (validated approach, no correction).

---
## 16:31 — Corner-plot f_bin staircase diagnosis (no fix)
**Topics:** bias-correction posterior, corner plot artefact, deterministic n_bin rounding, MC noise, validation simulator
**Work done:**
- User reported step-like discontinuities on the f_bin axis of the (log₁₀ P_max, f_bin) and (σ_single, f_bin) corner-plot heatmaps, despite f_bin being the *finer* grid (~99 points) than log₁₀(P_max) (~25 points).
- Explore agent traced the cause to `n_bin_per_set = int(round(n_stars_per_set * float(f_bin)))` at `wr_bias_simulation.py:937` (also line 716 for `simulate_delta_rv_sample`) — with N≈25 and 99 f_bin values, the 99 values collapse to ~26 unique integer n_bin → 4 adjacent f_bin grid points produce identical likelihoods → flat plateaus.
- Confirmed within-plateau wiggles are independent MC noise (different RNG state per cell). Relationship: `wiggle ∝ 1/√n_sets`, but `n_sets` does NOT remove the staircase — increasing it makes the steps sharper, not smoother.
- Wrote diagnosis with three fix options to `~/.claude/plans/look-at-this-corner-logical-liskov.md` (A: Bernoulli draw inside MC loop; B: analytic marginalisation Σ Binomial(k; N, f_bin) × L_k over per-k pre-computed CDFs; C: cap fbin_steps at N+1).
**Decisions:**
- User chose **no code change**. Bias bounded by 1/N ≈ 0.04 on f_bin, which matches the precision a 25-star sample can support, so the artefact is statistically defensible. Options A/B remain on the plan file if needed later for the paper.
**Files modified:**
- `~/.claude/plans/look-at-this-corner-logical-liskov.md` (new — diagnostic + fix options)
- `~/.claude/projects/.../memory/project_corner_staircase_known.md` (new — project memory of artefact, do-not-re-investigate flag)
- `~/.claude/projects/.../memory/MEMORY.md` (added pointer under Live State)
- `daily_logs/2026-04-30.md` (this entry)
**Open questions:** None — user explicitly closed by accepting the artefact.
**Memory updates:** Saved `project_corner_staircase_known.md` so future-me does not re-diagnose if the staircase is reported again.
**No new COMMON_ERRORS patterns** — no bug fixed; deterministic round is intentional behaviour with a known artefact.
**No new learnings** — user's questions were clarifying, not corrective; no mistakes to capture.

---
## 16:32 — Single-star RV / σ / ΔRV-pp inspection plot
**Topics:** per-epoch RVs of single stars, sample-std error propagation, inverse-variance weighted mean of σ, peak-to-peak ΔRV with error
**Work done:**
- Built `scripts/plot_single_star_rvs.py` — loads observed RVs via `pipeline.load_observations.load_observed_delta_rvs(settings, ObservationManager())`, filters `is_binary is False & N≥2`, sorts by σ ascending, plots per-epoch RV scatter with error bars + ⟨RV⟩ tick + σ-diamonds, marks lowest σ (Brey 90a, 1.02 ± 0.68) and largest σ (Brey 83, 11.27 ± 1.59).
- Implemented `std_uncertainty(rv, rv_err)` propagating per-epoch RV errors through the sample std: `err(s) = √(Σ d_i² σ_i²) / (s (N−1))` (derived from `∂s/∂x_i = (x_i − x̄) / [s (N−1)]`).
- Added inverse-variance weighted mean of σ across the 15 single stars: **4.49 ± 0.17 km/s** (vs unweighted 5.19 ± 0.69, population scatter 2.67). Flagged to user that the propagated 0.17 reflects only measurement uncertainty, not the much larger intrinsic σ-spread; the weighted-mean error is therefore optimistic if interpreted as "uncertainty on a typical single-star σ".
- Iterated layout twice on user's request: (1) added err(σ) bars + dashed weighted-⟨σ⟩ band; (2) blended σ markers onto the *left* RV axis (same units), repurposed the right axis for ΔRV peak-to-peak triangles with `err(ΔRV_pp) = √(err_max² + err_min²)`. This satisfied the carry-over open question logged in the 16:31 logP_max entry.
- Output: `plots/single_star_rvs.html` (interactive). PNG export skipped — `kaleido` not installed in `guyenv` (offered to install but user has not asked).

**Decisions:**
- Computed σ uncertainty via Gaussian error propagation rather than bootstrap or `s/√(2(N−1))` — user explicitly asked for "error propagation", so the closed-form analytic version is the right choice.
- Sorted star order by ascending σ (sample-wide trend visible left→right). Used `categoryorder='array'` to lock the categorical x-axis.
- Right y-axis recolored violet to visually pair with the violet ΔRV-pp triangles (no second-axis ambiguity).
- Did NOT delegate to coder agent — task was a small standalone script in `scripts/`, not a paper or app/page edit, and user did not flag this as a coder-only domain. (Edge case for `feedback_coder_does_edits.md` — flagging here in case user wants me to switch.)

**Files modified:**
- Created: `scripts/plot_single_star_rvs.py`
- Created: `plots/single_star_rvs.html`
- Appended: `daily_logs/2026-04-30.md` (this entry)

**Open questions:**
- Should σ_intrinsic vs σ_measured carry into the paper? The 4.49 ± 0.17 km/s weighted mean is a candidate σ_single estimate but only if measurement-error-dominated; the 2.67 km/s star-to-star scatter argues against that. Worth raising in the next bias-correction iteration.
- PNG export not produced — `kaleido` install pending user decision.

**Memory updates:** None — task was exploratory analysis, no new rules surfaced. Existing `feedback_no_self_approve.md` honored (no WORKING flag on plot; user visually confirmed iteratively).

**No new COMMON_ERRORS patterns** — the one runtime error hit (`dict() got multiple values for keyword argument 'showgrid'` when spreading `axis_kw` and re-specifying `showgrid=False` for `yaxis2`) is already covered by **E018** (`**PLOTLY_THEME` keyword collision). Same root cause, same fix: use a dict literal `{**base, 'key': override}` or explicit non-spread kwargs.

**No new learnings** — error-propagation request handled by deriving the closed form on the spot; user accepted on first try. The intrinsic-vs-measurement caveat I flagged proactively was not corrected, so I'll keep flagging it next time it applies.
