# Daily Log — 2026-04-14

---
## 14:30 — WR RV variability literature review + bias simulation parameter walkthrough
**Topics:** intrinsic ΔRV of single WR stars, logP_max justification, minimum RV scatter, bias simulation parameters, sigma_measure code path, likelihood bin computation
**Work done:**
- Two rounds of literature search on intrinsic ΔRV variability of single WR stars (WC-specific papers, LMC/metallicity effects, CIR mechanisms, pulsation-driven RV, statistical frameworks). Compiled ~40 citations with quantitative σ_w values and detection thresholds.
- Added new Section 4b to `DOCUMENTATION.md` ("Intrinsic RV Variability of Single WR Stars and Period Range Justification") — 6 subsections covering wind-induced scatter by subtype, line dependence, metallicity effects, minimum floor, period range justification, and threshold implications.
- Added 6 new rows to §5 Key Numbers table (σ_w per subtype, min observed σ_RV, theoretical floor, logP_max constraints, longest spectroscopic binary).
- Expanded §6 References from 5 to 22 citations (Chené, Crowther, Deshmukh, Dsilva I/II/III, Grassitelli, Kar, Koesterke, Langer, Moe & Di Stefano, Nazé, Sana 2012/2025, Sander & Vink, Schnurr, Shenar, Simón-Díaz, St-Louis, Vink & de Koter).
- Walked through bias simulation parameters with user: orbital distributions (logP, e, q, M₁, i), noise model (σ_single + σ_measure with 7-distribution error model selector), detection threshold, likelihood bins, cadence library.
- Traced sigma_measure code path through the stack and showed user that it is NOT fixed — flows from UI error model selector (`extras.py:556-612`) through `_WORKER_GLOBALS` to `SimulationConfig`. Confirmed user is running Weibull error model.
- Traced likelihood bin code path: `params.py:_render_likelihood_bin_config` → `cadence.py:1076,1202` → `runners_cadence.py:100` → `_init_worker` (`wr_bias_simulation.py:1363`) → `_single_grid_task_*` → `multinomial_log_likelihood` (`wr_bias_simulation.py:1202-1244`). Explained `logL = Σ n_i · ln(p_i)` with epsilon floor = 1/N_total.

**Decisions:**
- logP ∈ [0.15, 5.0] is well-justified for WC stars (Dsilva WC best-fit logP_max = 4.00; RV detection drops to 0% beyond logP ~4.5; Deshmukh binary desert at intermediate periods).
- No minimum σ_RV = 0 for single WR stars — observed floor is 1.8 km/s (WR 3), theoretical floor ~2 km/s (Grassitelli+2016).
- WC wind variability (0–6 km/s) is much lower than WN (5–15 km/s); the 45.5 km/s threshold is very conservative.

**Files modified:**
- `DOCUMENTATION.md` — new §4b, updated §5 table, expanded §6 references
- `.claude/plans/mossy-marinating-hickey.md` — plan file (used for plan mode)

**Open questions:**
- Continue parameter-by-parameter review of bias simulation (eccentricity, mass ratio, primary mass, inclination remain — only period and noise covered).
- Whether to address the fixed M₁ = 10 M⊙ assumption and the q_max = 2.0 choice in the paper methods.

**Memory updates:** None (session memory was read, not written).

**COMMON_ERRORS scan:** No new patterns — no bugs were fixed this session.

**Learnings:** No new learnings — session was research/review, no corrections from user.

---
## 16:20 — Add AIC/BIC to Compare tab for Dsilva vs Langer model selection
**Topics:** comparing unnormalized logL across period models, free-parameter counting, AIC/BIC formulas, Compare tab extension
**Work done:**
- Discussed methodology: raw max-logL is biased toward Dsilva (extra π DOF) → penalize with AIC/BIC
- Confirmed via npz inspection that `logL_raw` and `obs_delta_rv` (N=25) are stored in every result file
- Planned implementation in plan mode (giggly-percolating-lampson.md); user-edited to enforce dynamic k and scope guardrail
- Coder agent implemented: new columns `logL, k, AIC, ΔAIC, BIC, ΔBIC` + caption in Compare best-fit table
- `k` derived dynamically from grid axes with size > 1 (not hard-coded)
- `/error-check` PASS (pyflakes + render test)
**Decisions:**
- Use AIC/BIC (not fixed-π nested comparison) as primary model-selection signal; flat-π variant deferred
- Keep existing normalized `Likelihood` column for continuity; add raw `logL` alongside
- N for BIC = len(obs_delta_rv) = 25 (observed stars)
- k computed per-run from grid shape so single-σ/logP_max runs get correct penalty
**Files modified:**
- app/bc/extras.py (inside `_render_compare_tab()` only; Dsilva/Langer tabs untouched)
- /Users/guyshtainer/.claude/plans/giggly-percolating-lampson.md (plan file)
**Open questions:** Visual verification in the webapp still pending; not yet committed
**Memory updates:** None
**COMMON_ERRORS:** No new patterns
**Learnings:** No new learnings

---
## 16:40 — Standalone spectrum_app + rv_modeling_app extractions with agent team
**Topics:** page extraction, standalone Streamlit apps, agent orchestration, A&A plot style, matplotlib theme, diagnostic line audit, distribution fitting, orbital histograms, dict contract bugs, plots agent role

**Work done:**
- Extracted `app/pages/02_spectrum.py` → new `spectrum_app/` (8 files, ~1500 lines); committed to main
- Extracted `app/rv_modeling/` → new `rv_modeling_app/` with isolated copy (import-path bug fixed so standalone uses its own copy, not main app)
- Spectrum app features added: peak-to-peak epoch in banner + toggle, quick-zoom buttons per diagnostic line group (type-aware Y-range: abs [0.5,1.3], em data-driven, ISM/Telluric [0,1.2]), ISE anchor/interp toggles on unnormalized chart, unnormalized COMBINED plot below main, raw FITS fallback for non-COMBINED bands, LMC correction, multi-line view (later removed per user)
- Matplotlib/academic Plotly theme (white bg, serif, black axes) for all plots; caught white-axis-title bug by adding `title.font.color=black` to `_ACADEMIC_AXIS` and `theme=None` on all `st.plotly_chart()` calls
- Scientific audit of `DIAGNOSTIC_LINES` in `app/spectrum_helpers.py` (opus scientist + general-purpose second opinion): removed misidentified `O V 5590`, moved `[O III] 5007` to new `Nebular / Circumstellar` group, split `He II (hot companion)` into `OB companion absorption` + `WR emission`, split Oxygen into `WC` + `WO` diagnostic groups
- RV modeling: added Laplace + Gen. Normal distributions per scientist recommendation for double-exp RV histogram; enhanced auto-fit-all (Rank column, best-fit banner, positive-only x-axis clamping, AIC/BIC info expander); fixed KeyError 'n_epochs' from producer/consumer dict contract mismatch (both-sided fix); added 9-panel orbital histogram grid to Tab A "Simulate Binary RVs" after dist fitting
- Strengthened plots agent definition (`.claude/agents/plots.md`) with HARD RULE #1 Contrast + A&A Journal Standards + mandatory 6-step Review Protocol
- Removed `min_value`/`max_value` from distribution `st.number_input` calls (feedback_no_hard_limits)
- Fixed 12 locations missing `theme=None` on plotly_chart + 7 bare-string axis titles + 12 update_xaxes/yaxes missing title_font

**Decisions:**
- Standalone apps share `settings/user_settings.json` with main app via namespaced keys — accepted for dev use (no race conditions expected)
- Coder agent's phantom "plan mode" bug in its subagent context handled by applying its verified plans directly; user prefers coder for edits but this is a harness-level issue
- Spectrum histograms NOT ported from BC — RV modeling already had them (helpers.py:431); gap was: only shown in physics-based Tab B/C, not Tab A; fix was adding the call to Tab A
- Dict contracts: `.get(key, default)` over `p[key]` for cross-module shared dicts (new feedback memory)

**Files modified:**
- Created: `spectrum_app/` (9 files), `rv_modeling_app/` (11 files), `memory/feedback_no_collapsing_controls.md`, `memory/feedback_matplotlib_style.md`, `memory/feedback_delegate_to_agents.md`, `memory/feedback_coder_does_edits.md`, `memory/feedback_agent_roles_strict.md`, `memory/feedback_dict_contract_bugs.md`, `memory/feedback_aa_journal_style.md`
- Modified: `app/spectrum_helpers.py` (DIAGNOSTIC_LINES reorg, `theme=None`, font_size bumps, legend bgcolor), `.claude/agents/plots.md` (new A&A Standards section), `memory/MEMORY.md` (7 new feedback entries)
- Git commit: `spectrum_app` extraction (12 files, +1469 lines)

**Open questions:**
- rv_modeling_app not yet committed to git
- Scientist flagged possible bug: `simulate_binary_rvs_raw` in `wr_bias_simulation.py:817` may skip `_draw_measurement_noise` (Tab A raw-RV path) — not confirmed
- Future: Case A/B view mode for orbital histograms, expanded caption with all model params, constant-parameter handling (vertical-line fallback from render_shared_langer.py)
- Future: auto-fit-all enhancement already done; user noted to apply same pattern elsewhere if needed

**Memory updates:**
- 7 new feedback memories saved (listed above); MEMORY.md index updated

**COMMON_ERRORS:** No new patterns added (the dict-contract bug is captured in `memory/feedback_dict_contract_bugs.md` rather than COMMON_ERRORS since it's a design pattern, not a specific greppable typo)

**Learnings:**
- Agent role boundaries: coder writes, QA reviews, plots advises on charts (saved as `feedback_agent_roles_strict.md`)
- Plots agent needs explicit Review Protocol — "background first, then every text element's contrast" — caught recurring white-text-on-white bug after strengthening
- Model override `model: "sonnet"` consistently fails with 500 errors in this harness; switched to general-purpose without override for second opinions
- User wants matplotlib/A&A journal style for every plot; dark Streamlit UI + white Plotly charts is the intentional pattern (needs `theme=None` to override Streamlit's dark theme overlay)
