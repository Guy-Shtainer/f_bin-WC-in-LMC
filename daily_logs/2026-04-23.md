# Daily Log — 2026-04-23

---
## 14:05 — RV modeling app: histogram fix, parametric summary, error-model port
**Topics:** rv_modeling_app Simulate Binary RVs tab, orbital histogram bug, Model Fitting tab Parametric mode, best-fit parameter summary, measurement error model port from bias-correction, 2-test detection rule on model curve

**Work done:**
- Diagnosed orbital-parameter 3×3 histogram grid on "Simulate Binary RVs" tab: histograms looked sparse/spiky (logP cut at 0.9, K₁ spikes, e/q patchy). Root cause: `compute_physics_diagnostics` called with `n_sets=1` → only ~25 binaries drawn for diagnostics while the main RV generator uses `n_sim=100 000`. Fixed by computing `n_sets_hist = ceil(n_sim / n_per_set)` so histogram population matches main sim.
- Added best-fit Parameter Summary for Parametric mode on Model Fitting tab: distribution names + per-parameter labels (from `_PARAM_META`) for both single and binary, plus N_epochs and Seed. Physics mode unchanged.
- Ported single-star and binary-star measurement error models from bias-correction webapp into Model Fitting tab Parametric mode:
  - Added `render_error_model_pair` call after the distribution selectors.
  - Extended `compute_model_fraction_curve` with 6 new error kwargs (all safe defaults).
  - Implemented per-star Bernoulli assignment + per-class intrinsic draws (singles: one tiled draw; binaries: per-epoch draws) + independent per-epoch noise via `_draw_measurement_noise`.
  - Implemented full 2-test detection on simulated ΔRV: `(dRV > t) AND (dRV − 4·σ_pair > 0)`, σ_pair = √2·σ_measure per class. Matches observed-curve construction at page.py:49-59.
  - Session state and Parameter Summary now include the error-model fields.
- QA + scientist fact-checks: all 7 core scientific correctness points ✓ (singles intrinsic tiled, binaries per-epoch, per-class noise, σ_pair convention, 4σ detection, per-star Bernoulli, no double-counting). Functional tests pass.

**Decisions:**
- Singles get ONE intrinsic draw per star tiled across epochs (behavior change from prior per-epoch draws). Rationale: single stars have no intrinsic epoch-to-epoch variation; ΔRV should come from noise alone. Scientist-approved.
- Binaries keep per-epoch independent intrinsic draws. Orbital marginal.
- σ_pair = √2·σ_measure per class (homoscedastic by class on model side; observed side uses true per-star `sqrt(σ_i²+σ_j²)`). For fixed/normal error models this is exact.
- New kwargs all default to `fixed / () / 0.0` so existing callers are unaffected.
- Strict agent delegation followed: ALL code edits went through the `coder` agent after one early slip where I edited directly (reverted it). QA + scientist reviewed every change.

**Files modified:**
- rv_modeling_app/rv_modeling/tab_simulation.py — lines 748-754 (n_sets_hist fix)
- rv_modeling_app/rv_modeling/tab_fitting.py — lines 227-258 (parametric summary rows), 308-316 (error-model UI), 356-394 (grid + final `compute_model_fraction_curve` calls with error kwargs), 395-408 (session_state error fields)
- rv_modeling_app/rv_modeling/compute.py — lines 298-360 (rewritten `compute_model_fraction_curve` with 6 error kwargs, per-class intrinsic+noise logic, 2-test detection)

**Open questions:**
- For non-Gaussian error distributions (gamma, weibull, lognormal, exponential, uniform), `sigma_measure` is NOT the actual 1σ of the noise drawn by `_draw_measurement_noise` (the code applies `abs(X)` then random sign). So σ_pair=√2·σ_measure and the 4σ criterion are miscalibrated for those models. Exact for fixed/normal. User to decide: (a) accept caveat and stick to fixed/normal, or (b) compute σ_eff=√E[X²] per distribution. Not patched yet.
- Three pre-existing unused imports flagged by pyflakes in tab_fitting.py (scipy.stats, scipy.optimize.minimize, shared_lite.COLOR_SINGLE). Pre-existing, not introduced by today's work. Clean-up candidate.

**Memory updates:**
- User re-emphasized: only the coder agent is allowed to edit code, no exceptions. `memory/feedback_coder_does_edits.md` already covers this — reinforced by a direct correction during this session.
- Also: user wants scientist fact-check on any scientifically-sensitive code changes, not just coder+QA.

**COMMON_ERRORS:** No new patterns discovered.

**Learnings:** No new learnings beyond the reinforced coder-only-edits rule (already in `.claude/references/learnings.md`).

---
## 17:27 — Standalone To-Do webapp: how-to-launch + rendering/parsing bugfixes
**Topics:** standalone todo_app.py launch command, zsh `!` history-expansion gotcha, green code-span styling in task descriptions, markdown-table pipe corruption, task #185 overflow into priority column

**Work done:**
- Explained how to launch standalone To-Do webapp: `conda run -n guyenv streamlit run todo_app.py --server.port 8502` (standalone entry shares all logic with embedded page via `app/todo_core.py`).
- Diagnosed zsh error "event not found: Thesis/Thesis" — zsh was doing history expansion on the `!` in the path `תואר שני!/`. Fix: use single quotes (double quotes don't block `!` expansion) or `cd` first and use a relative path.
- Identified two bugs from user screenshots of the task list:
  - **Bug 1:** Descriptions rendered with bright green `<code>` spans (unreadable) because user-authored descriptions contain many backticks (e.g., `` `_me_cdf_band` ``) which `st.markdown` turns into code elements.
  - **Bug 2:** Task #185 showed priority as literal `"none"` plus description text bleeding into the narrow 0.5-width priority column. Root cause: the description contained `int \ | none` — the embedded `|` broke the naive `line.split('|')[1:-1]` parser at `todo_core.py:72`, shifting all subsequent columns.
- Delegated both fixes to the `coder` agent (per repo rule: only coder edits code). Coder:
  - In `col_title` rendering: HTML-escaped the description and stripped backticks before passing to `st.markdown`, so code styling no longer applies.
  - Hardened `_parse_table_rows` with a regex that splits on `|` only when NOT preceded by `\` (`re.compile(r'(?<!\\)\|')`), then unescapes `\|` → `|` in each cell.
  - Added `_escape_cell()` helper in `save_todos` to pre-escape every string cell in the Open / Done / Deleted tables.
  - Repaired the corrupted row in `TODO.md` surgically: `int \ | none` → `int or None` (only in row #185).
- Coder ran `/error-check` — all phases PASS (pyflakes, imports, parse test on real `TODO.md` parses 61/105/4 tasks, task #185 priority now correctly parses as `high` instead of `none`, backtick-strip + HTML-escape verified). Smoke test on port 8504 returned HTTP 200.
- Confirmed to user: the changes are scoped to the To-Do app only. Grepped — `app/todo_core.py` has no importers outside `todo_app.py` and `app/pages/10_todo.py`. Bias correction page (`app/pages/05_bias_correction.py` + `app/bc/*`) is untouched and has no dependency on `todo_core`. `TODO.md` is a data file not consumed by the bias pipeline.

**Decisions:**
- Strip backticks from description display rather than overriding global `<code>` CSS — less invasive, doesn't affect any other page's code styling.
- Escape pipes with `\|` (markdown convention) rather than HTML entities — readable in the raw `TODO.md` file and round-trips cleanly through the hardened parser.
- Kept the data repair on task #185 surgical (single substring replace) rather than rewriting the whole row.
- Did NOT flag the changes as WORKING — visual sign-off on the rendered page still pending per `feedback_no_self_approve`.

**Files modified:**
- app/todo_core.py — added `import html`, `_CELL_SPLIT` regex + `_escape_cell` helper, pipe-aware parser, pipe-escaping writer, HTML-escape + backtick-strip on description render
- TODO.md — one substring replace in row #185 (`int \ | none` → `int or None`)

**Open questions:**
- User has not yet visually confirmed the rendered fix (pending — needs to reload the standalone app and check that the green code-spans are gone and task #185 lays out normally).

**Memory updates:** None (no new preferences or corrections this session).

**COMMON_ERRORS:** No new patterns. The `split('|')` bug is localized to one parser (not a codebase-wide recurring pattern), so it doesn't warrant an E-entry.

**Learnings:** No new learnings. Delegation to coder + surgical-edits rules were followed from the start.

---
## 19:06 — Bin Sensitivity sub-tab: revived save/load, fixed hang, persistence, plots
**Topics:** bin_sensitivity tab, save/load UX, autosave partials, promote_partial, schemes persistence, mock params persistence, @st.cache_data deadlock, CDF overlay annotations, posterior HDI68 shading, readable text rule, saved-runs UI visibility

**Work done:**
- Revived a prior chat's work on Bin Sensitivity save/load. Storage layer (`bin_sensitivity_storage.py`, 696 lines with self-tests) was already done; tab-side UI was half-wired with two broken call sites (`_maybe_autosave_partial` undefined, `_render_results` signature mismatch).
- Round 1 wiring: coder built custom `_maybe_autosave_partial` + `_scan_bs_metadata` + `_render_saved_runs_panel`. User rejected: "look it up, do the same but simply with slightly different path as I told you, there should be no errors after that since its basically code copying and path changes".
- Round 2 rebuild: rewrote to mirror cadence `_render_partial_table` / `_scan_partial_metadata` / `_scan_result_metadata` from `file_ops.py` verbatim — added `list_bs_partials()` to storage + `_scan_bs_partial_metadata` + `_scan_bs_result_metadata` + `_render_bs_partial_table` + `_render_bs_results_table` + shared `_hydrate_loaded_bs_run`. Partial/final separation, expander for partials, Load/Delete/Resume buttons matching cadence icons.
- Added schemes list persistence to `user_settings.json` under `bin_sensitivity.schemes` — initial signature-gated approach, later replaced (see below).
- Diagnosed and fixed run hang: `_run_all_schemes_bg` was calling `rescore_scheme_cached` (`@st.cache_data`-decorated) from a `threading.Thread`; Streamlit's cache deadlocked on internal locks without a ScriptRunContext. Bypassed by calling uncached `rescore_scheme` directly, passing the already-loaded `ctx` to avoid re-opening the .npz per scheme.
- Added mock-truth parameters persistence (f_bin, π, σ_single, log P_max, seed) to `bin_sensitivity.mock_params`.
- Plot improvements: CDF Overlay now shows two-line subplot titles with best-fit + HDI68 per scheme (Option A, U+2212 minus); Posterior Shapes shades 68% HDI region per scheme with matching legendgroup (line+shadow toggle together); all 6 plots got readable-text bump (ticks ≥12pt, titles ≥14pt, legend ≥14pt for 3+ subplots).
- Post-user-correction fixes (two course corrections near end of session):
  - Moved saved-runs UI out of `_render_results` (which only fires on active completed job) into `_render_bs_saved_runs_panel` called unconditionally at top of tab inside "Saved runs" expander — so user can load prior partials after a browser refresh.
  - Replaced signature-gated schemes persist with unconditional `_persist_schemes(rows)` on every render of `_render_schemes_list` (bulletproof, eliminates signature-cache edge cases).
  - Removed silent `except: pass` from `_persist_schemes` + `_persist_mock_params`; errors now surface via `st.toast(..., icon='⚠️')` + `print()` fallback.
  - Added first-call-per-session debug `print('[bsn] _persist_schemes first call, N rows')` so Guy can verify in the streamlit terminal.
- Added "Readable text size" rule to `memory/plot_preferences.md` — minimums for ticks (≥12pt), axis titles (≥14pt), subplot titles (≥14pt), legend (≥12pt, ≥14pt for grids), bar labels (≥12pt), figure titles (≥16pt).

**Decisions:**
- Keep `.partial_` leading-dot filename convention in `bin_sensitivity_results/` (hides partials on Unix) — cosmetic only; cadence uses `_partial_` (no dot); don't "fix" what user chose.
- Resume button on partials routes to Load (bin-sensitivity runs are atomic, not incrementally resumable like cadence cell-grids).
- Bypass `@st.cache_data` entirely on the bg-thread path rather than attempt a ScriptRunContext workaround — result caching is already handled by the autosave layer (`bin_sensitivity_storage`).
- Schemes persistence uses unconditional write on every render (not signature-gated) — 1-2KB JSON is cheap, eliminates a whole class of cache-skip bugs.

**Files modified:**
- `app/bc/bin_sensitivity.py` — main tab file, grew from 792 → ~1270 lines across multiple rounds (persistence helpers, saved-runs panel extracted to top-of-tab, autosave, plot wiring)
- `app/bc/bin_sensitivity_storage.py` — added `list_bs_partials()` + self-test step
- `app/bc/bin_sensitivity_scorer.py` — non-mock branch in `_run_all_schemes_bg` bypasses cache; NOTE comment on `rescore_scheme_cached`
- `app/bc/bin_sensitivity_plots.py` — `_apply_readable_text()` helper; CDF subplot title annotations with best-fit + HDI68; HDI68 shaded bands on posterior marginals
- `memory/plot_preferences.md` — added "Readable text size" rule (§2026-04-23)
- `.claude/references/learnings.md` — 3 new User Interaction rules (don't dismiss user-reported issues based on code review; no silent except pass in persistence; always-visible saved-results UI)
- `COMMON_ERRORS.md` — added E050 (@st.cache_data from threading.Thread → deadlock)

**Open questions:**
- Awaiting Guy's verification of both fixes after next app launch: (1) 3 existing `.partial_*.npz` in `bin_sensitivity_results/` should appear in top-of-tab Saved runs expander; Load → render, Save result → promote. (2) Custom schemes should survive a hard browser refresh and the `[bsn] _persist_schemes` debug line should appear in the streamlit terminal on first tab interaction.
- If schemes STILL don't stick after the bulletproof rewrite, the toast error will reveal the underlying exception.

**Memory updates:**
- Added `memory/plot_preferences.md §2026-04-23` — readable text size minimums (app-wide, not just Bin Sensitivity).
- Added 3 learnings in `.claude/references/learnings.md` User Interaction section (see Learnings below).

**COMMON_ERRORS:** Added E050 — `@st.cache_data` invoked from a background `threading.Thread` → silent deadlock on cache-write after bg compute finishes. Grep pattern + fix documented.

**Learnings:**
- **Don't dismiss user-reported issues based on code review alone** (new, last_triggered 2026-04-23). Guy explicitly called this out — "you feel free skipping my commands. No you do not get to choose what you listen to". The code for schemes persistence LOOKED correct but wasn't actually sticking; I trusted the read over his lived experience.
- **Silent `except: pass` in persistence is a trap** (new, last_triggered 2026-04-23). Swallowed exceptions hide persistence failures. Surface via `st.toast` + `print()`.
- **Always-visible UI for saved-results tables** (new, last_triggered 2026-04-23). Load/Save UI must not be nested behind an "active job" gate — browser refresh wipes session_state and makes the UI unreachable. Mirror cadence pattern: render at top of tab unconditionally.

---
## 21:38 — Validation tab: A&A polish, mock/Explorer parity, mock_results persistence, TODO 187 (6 bugs)
**Topics:** validation tab home-button zoom, A&A theming leak, mock vs Explorer CDF byte-identical invariant, mock_results/ persistence, error distribution chooser port from Dsilva, sigma_meas as noise vs as reported error, TODO 187 all 6 sub-items, marginal mode removal, corner-plot honest labels, Explorer x-axis truncation, CDF sanity check rewrite, honest labels rule

**Work done:**
- **Bug 1 (home button / y-axis on "Simulated Binary Fraction vs Threshold"):** First attempt wrong-axis (I fixed x-axis trimming); user corrected; reverted. Second attempt correct: full x-range from `max(gap_drv, obs)` + explicit `yaxis.range=[0,1]` + `xaxis.range=[0, x_max]` so home button zooms all the way out. Three files: `render_shared.py`, `render_shared_langer.py`, `sim_plots.py`.
- **Bug 2 (mock vs Explorer CDF mismatch):** Extracted the inlined mock sampler into canonical helper `_sample_delta_rv_mock` in `validation.py`. `_me_cdf_band` got `validation_mode=True, validation_seed` kwargs; routes to the same helper with n_sets=1 when validation context is detected via session_state `{p}_val_mock_params`. Byte-identical ΔRV arrays verified in both Dsilva and Langer (new tests `scripts/test_explorer_mock_equal*.py` with fixed + Log-normal cases + σ_meas regression guard).
- **A&A theming sweep:** `_AA_OVERRIDES` was missing top-level `title.font.color` → figure titles stayed light-grey on plots that DID apply the override. Added that key. Then applied `_AA_OVERRIDES` to 7 plots that skipped it: mock preview CDF/BF, Model Explorer CDF, CDF Sanity Check, analysis `_render_all_methods_cdf`, both `_render_likelihood_cdf` twins. Flipped `color='white'` traces to `#000000` where visible on white bg.
- **mock_results/ persistence:** New `app/bc/validation_io.py` (567 lines): `save_validation_result`, `save_mock_stars` (pickled dict keyed by star index with rvs/times/errs/is_binary), `list_validation_results`, `load_validation_result`, `scan_validation_metadata`. Runner routes validation runs via `save_backend='mock_results'` gate; cadence.py injects context from session_state; render_validation.py got a saved-runs table at top of Single-Point Recovery with Load/Delete/Resume mirroring cadence. Auto-saves partial every 120s + final on completion.
- **Error distribution chooser port:** `_render_one_error_model` from `extras.py` now appears in validation mock UI. `_sample_delta_rv_mock` draws per-RV noise via `_draw_measurement_noise` from `wr_bias_simulation` (same helper Dsilva grid uses), adds to v, stores per-epoch `errs = sigma_measure` broadcast in `errs_per_star`. Explorer validation_mode reads `(error_model, error_params)` from the expanded 8-tuple `_val_mock_params` so byte-identical invariant survives distribution choice.
- **TODO 187 Stages A-D (six validation-tab bugs) + cleanup sprint:**
  - Stage A: best-fit CDF reads stored `best_median_cdf` instead of re-simulating (no more flat line); Explorer CDF x-axis extends across all traces; `_obs_label(result)` helper renames "Observed" → "Mock Observation" when `is_validation=True` across 15+ plot files.
  - Stage B: runners_cadence stores all 4 argmax params (`argmax_fbin/pi/sigma/logPmax`), `logP_max=NaN` bug fixed; marginal mode (`mode_*`) stripped from result dict and all readers (extras, file_ops, validation_io, bin_sensitivity_plots); summary table column renamed to "Joint argmax".
  - Stage C: grid's `n_sets` + `grid_seed` stored in result dict; Explorer uses them (no more seed/n_sets drift); new regression test `scripts/test_grid_vs_explorer_score.py` — |ΔlogL| = 0.103 within 5.0 tolerance.
  - Stage D: CDF Sanity Check rewritten — N=500 per draw (configurable), 16/84 percentile band from 50 draws, mock in black inside band.
  - Cleanup: corner-plot vline source = joint argmax (not marginal mode); caption + annotation honest; pre-existing `xv` undefined bug at render_lk_explorer_langer.py:230 patched to use `sig` + added `logP_max` to title.
- **Plots agent hardened:** `.claude/agents/plots.md` got CRITICAL RULES #1-3 rewritten (A&A always; never auto-zoom; always high-contrast) + concrete exit-check list.

**Decisions:**
- Mock noise addition behavior matches Dsilva grid (noise IS added to v, drawn from chosen distribution) — NOT "store only, no noise". Took two wrong turns to read this correctly; second revert cost a coder sprint. Regression guard (σ_meas=1 vs σ_meas=10 must produce different ΔRV arrays) added to both byte-identical tests so this specific regression can't silently recur.
- "Honest labels" rule codified in `memory/feedback_honest_labels.md`: only joint argmax + 68% HDI allowed as summary stats anywhere in the app. Marginal mode purged from result dict and every reader.
- Conditional "Mock Observation" label via `_obs_label(result)` helper — only validation-flow callers flip; normal Cadence tabs keep "Observed".
- mock_stars.npz uses pickled-dict format (per user spec) — allow_pickle=True, top-level key `stars` → dict keyed by int star index.
- `_AA_OVERRIDES` expansion happens in the validation module to keep the canonical recipe there; other files use deferred import to avoid circular deps.
- Byte-exact grid↔Explorer reproducibility NOT pursued (0.1 logL tolerance is acceptable for this project).
- Three white-on-white lines inside WORKING-flagged `render_lk_explorer` blocks left untouched pending explicit user authorization.
- Plan-mode workflow was used ONCE (user invoked it explicitly for the A&A audit + Langer parity sprint) — plan file at `/Users/guyshtainer/.claude/plans/look-through-validation-tab-fluttering-codd.md`.

**Files modified:**
- NEW: `app/bc/validation_io.py` (567 lines), `mock_results/` + `mock_results/mock_observations/` directories, `scripts/test_explorer_mock_equal.py` (expanded), `scripts/test_explorer_mock_equal_langer.py`, `scripts/test_grid_vs_explorer_score.py`
- `app/bc/validation.py` — `_sample_delta_rv_mock` helper, `_draw_mock_rv_errors` (added then removed), `generate_mock_observations_detail` signature extended with error_model/error_params, noise addition restored via `_draw_measurement_noise`, `errs_per_star` broadcast
- `app/bc/render_validation.py` — `_AA_OVERRIDES` + top-level `title` font, saved-runs table, error-model chooser replacing sigma_meas number_input, 8-tuple `_val_mock_params`, summary table honest labels, `logP_max=nan` fix, HDI from `_method_best_and_hdi`
- `app/bc/render_lk_explorer.py` + `render_lk_explorer_langer.py` — `_me_cdf_band` validation_mode+seed+error_model kwargs, x-axis union across all traces, grid_n_sets/grid_seed propagation, observed label helper, A&A theme, CDF Sanity Check rewritten
- `app/bc/runners_cadence.py` — 4-param argmax storage, grid_n_sets/grid_seed in result, marginal-mode strip, validation-lane save_backend routing, partial-save lane
- `app/bc/cadence.py` — 13-line validation-context injection reading session_state `{p}_val_save_backend`
- `app/bc/corner_plots.py` — vline from joint argmax with "Joint argmax" annotation
- `app/bc/analysis.py` — `_render_all_methods_cdf` reads stored best_median_cdf, Mock Observation labels
- `app/bc/render_shared.py`, `render_shared_langer.py`, `sim_plots.py` — y-axis [0,1], x-axis full range, `_AA_OVERRIDES` tail
- `app/bc/helpers.py` — new `_obs_label` helper + unified `_render_lk_cdf_sanity_check` delegator
- `app/bc/file_ops.py`, `app/bc/validation_io.py`, `app/bc/bin_sensitivity_plots.py`, `app/bc/extras.py`, `app/bc/likelihood_viz.py`, `app/bc/render_lk_fit.py`, `app/bc/render_lk_fit_langer.py` — argmax/honest-label readers + conditional observed-label plumbing
- `.claude/agents/plots.md` — CRITICAL RULES #1-3 rewritten
- `memory/feedback_honest_labels.md` (new), `memory/MEMORY.md` (index updated)
- `.claude/plans/mock_results_persistence.md` (new), `.claude/plans/look-through-validation-tab-fluttering-codd.md` (new)

**Open questions:**
- ALL of the above is uncommitted. Awaiting Guy's visual sign-off before `/git`. Specifically he needs to verify: (a) every validation-tab plot is white-bg A&A with black titles, (b) summary-table values match heatmap argmax at a real truth-on-grid run and logP_max is not NaN, (c) mock and Explorer CDFs overlay byte-for-byte when sliders match truth + seed, (d) saved validation run appears under `mock_results/` and reloads, (e) home button on binary-fraction plot zooms out to full data range.
- Three white-on-white trace lines inside WORKING-flagged blocks in `render_lk_explorer*.py` — `:973, :995` (detection-fraction panel) and Langer `:284` (sanity check). Left untouched.
- The Dsilva/Langer GRID simulator still adds measurement noise to RVs (as designed), whereas the validation mock does too after the revert. But the user's earlier phrasing "sigma_meas is just the error" suggested a deeper conceptual model I didn't implement. If he comes back asking for a noise-free grid, that's a much bigger refactor.

**Memory updates:**
- NEW: `memory/feedback_honest_labels.md` — CRITICAL rule. 100% honest labels on every stat. Only joint argmax + 68% HDI. Applies everywhere, not just validation. MEMORY.md index updated.

**COMMON_ERRORS:** No new greppable patterns discovered. The `mode_*` key removals were a project-specific semantic cleanup, not a recurring language/framework pitfall. The `xv` undefined typo at render_lk_explorer_langer.py:230 is a one-off not worth an E-entry.

**Learnings:**
- **Misreading user intent: always re-read the literal words before delegating** (new, last_triggered 2026-04-23). Cost TWO coder sprints this session — Bug 1 went to x-axis when user meant y-axis; Bug 2 noise went to "remove" when user meant "draw from chosen distribution". When user says "X is the Y and nothing else", that means "keep X doing Y, add nothing" — NOT "remove whatever X is doing now". Use `AskUserQuestion` for any phrase with two plausible readings before delegating.
- **Auto mode is not a slowness cause; misreads are** (new, last_triggered 2026-04-23). Guy asked why I was slow — answer: every misread costs an extra coder sprint to revert. Adding one clarifying `AskUserQuestion` call ≈ 10 seconds; a misread revert ≈ 10 minutes. Always ask when ambiguous.
