# Daily Log — 2026-04-19

---
## 13:09 — Bias correction Langer tab slider crash fix
**Topics:** bias correction, Langer tab, Streamlit slider bug, grid exclusion
**Work done:**
- Fixed `StreamlitAPIException` in `_make_range_slider` when grid axis has single value (σ_single=7.5 fixed in Langer model causes `min_value == max_value`)
- Added guard: when `lo >= hi`, display static text label instead of slider, return `(lo, hi)` tuple
- Added E045 to `COMMON_ERRORS.md` for the `st.slider` min==max pattern
**Decisions:**
- Minimal fix to WORKING-flagged code (user explicitly requested the fix)
- Show "fixed" label with `st.markdown` rather than a disabled slider (simpler, no Streamlit workaround needed)
**Files modified:**
- `app/bc/helpers.py` — `_make_range_slider` function (lines 100-111)
- `COMMON_ERRORS.md` — added E045
**Open questions:** None
**Memory updates:** None
**Learnings:** No new learnings — straightforward single-value guard pattern

---
## 14:38 — Bias-correction resume heatmap discontinuity fix
**Topics:** bias correction, cadence-aware resume, checkpoint validation, Monte-Carlo seeds, simulation-context signature, Streamlit button-flow, sim-context drift guard, plan mode
**Work done:**
- Investigated user's report that resuming a cancel-saved cadence-aware grid (Cadence Dsilva / Cadence Langer) produces a visible horizontal-row kink in the `f_bin × σ_single` likelihood heatmap, potentially faking a best-likelihood cell and biasing the binary-fraction result.
- Ruled out the Explore agent's "seed mismatch" hypothesis: per-cell seed at [app/bc/runners_cadence.py:85-91](app/bc/runners_cadence.py#L85-L91) is a pure function of `(i_lp, i_sig, i_fb, i_pi)`; per-task RNG is freshly seeded in worker via `np.random.default_rng(seed)` at [wr_bias_simulation.py:1617](wr_bias_simulation.py#L1617); `imap_unordered` dispatch order is irrelevant. Seeds are identical in fresh and resumed runs — not the cause.
- Identified real root cause: the checkpoint in `_save_partial_cadence` stores only `logL_raw` + grid axes + n_sets/drv_bin_width/drv_max/adaptive_bins. It does NOT store `bin_cfg`, `sigma_meas`, `bin_edges`, `likelihood_bin_edges`, error-model config, cadence_library, or obs_delta_rv. On resume, those are pulled from current UI / `@st.cache_data` caches without validation. Page refresh or Streamlit-process restart (cache eviction) can recompute them to a slightly different array → systematic logL offset for post-resume cells → horizontal kink.
- User confirmed discontinuity is "along a constant-σ row", matching the σ-major task-build order — consistent with the drift theory.
- Asked clarifying questions via AskUserQuestion; user reported cancel-save → travel to university → page refresh → resume (no deliberate UI change, unclear whether server stayed up).
- Plan approved (`/Users/guyshtainer/.claude/plans/i-have-noticed-that-elegant-crystal.md`).
- Delegated implementation to coder agent:
  - `app/bc/helpers.py`: added `_array_fingerprint`, `_cadence_lib_fingerprint`, `build_sim_context_signature`, `diff_sim_contexts` (lines 99-170).
  - `app/bc/runners_cadence.py`: build `_sim_context` + `_sim_context_hash` once at run start (lines 97-115); persist both as new npz keys in `_save_partial_cadence` (lines 201-202) — additive, backward-compatible.
  - `app/bc/cadence.py`: on resume, parse checkpoint's `sim_context`, recompute live signature, branch on status: `ok` → silent resume; `legacy` (old checkpoints with no signature) → yellow warning; `mismatch` → red `st.error` with field-level diff + "Start fresh" / "Cancel" buttons.
- Caught Streamlit button-flow bug in the first implementation: both buttons were rendered inside the one-shot `if (_run_btn or _cad_auto_resume) and not _job_running:` block, but `_auto_resume` is popped at the top of the function → after click, next rerun's outer `if` is False → button not re-instantiated → click dropped.
- Delegated fix to coder: re-arm `_auto_resume` + `_resume_from` session_state before `st.stop()` in the "wait for click" branch; clear them in the click-handler branches to avoid infinite loop. Verified manual flow trace for all three cases (no click / Start fresh / Cancel).
- Ran `/error-check`: 16/16 checks pass. pyflakes clean, imports clean, signature unit test (identical-inputs, σ_meas drift, cadence drift, npz roundtrip) all pass, `scripts/test_render.py` 5/5 pass.
- Added E046 to `COMMON_ERRORS.md` documenting the "st.button inside conditional block → click dropped on rerun" pattern.
**Decisions:**
- Implemented "validate-and-refuse with field-level diff" (never silently resume with drift), not "auto-rerun on mismatch" — matches user's requirement that the simulation be *consistent*, and surfaces *which* parameter drifted so next reproduction pinpoints the trigger.
- Backward compat: old checkpoints without `sim_context` get a yellow warning and proceed (legacy path) rather than hard-fail — keeps existing partials resumable at user's risk.
- `_save_partial_cadence` npz keys are purely additive; no format break.
- Did NOT touch any `# ── WORKING — do not change this code ──` blocks (seed scheme, filter logic, `imap_unordered`, per-task RNG, partial-save filename builder).
**Files modified:**
- `app/bc/helpers.py` — +68 lines (4 new helpers)
- `app/bc/runners_cadence.py` — ~20 lines added (import, signature build, 2 new npz keys)
- `app/bc/cadence.py` — ~90 lines reworked in the resume block (load → validate → branch; button-flow re-arm)
- `COMMON_ERRORS.md` — added E046
- `.claude/references/learnings.md` — added "Verify explore-agent hypotheses against the code before accepting them"
- `/Users/guyshtainer/.claude/plans/i-have-noticed-that-elegant-crystal.md` — plan file (already existed, iteratively built)
**Open questions:**
- Next time the bug reproduces, the diff message will tell us *which* field drifted (bin_cfg.*, sigma_meas, obs_delta_rv_fp, cadence_lib_fp, adaptive bin edges). That output decides whether the upstream trigger is cache eviction, page-refresh widget re-init, or something deeper in `cached_load_cadence` / `_render_cadence_adaptive_bins`.
- Pending user decision: commit these changes (offered `/git` at end of conversation).
**Memory updates:**
- Added learning: "Verify explore-agent hypotheses against the code before accepting them" (avoided building a fix on a false root cause).
- Added COMMON_ERRORS E046: Streamlit button-in-conditional-block click-drop pattern.

---
## 14:39 — Bias correction CDF alignment + band rendering bugfix
**Topics:** bias correction, CDF plots, Model Explorer, Grid Range Exclusion, Plotly step vs linear, `_me_cdf_band`, cadence-aware simulation
**Work done:**
- Investigated why the top "CDF Comparison" plot and Model Explorer CDF showed different curves for the same best-fit model. Root cause: top plot used `simulate_delta_rv_sample` (non-cadence-aware, n_stars=1000) while the likelihood grid (and Model Explorer) use `simulate_delta_rv_cadence_aware`. Top plot also omitted `shape='hv'` so Plotly cubic-spline-smoothed the empirical CDF.
- Ran scientist + plots agents in parallel to validate. Scientist confirmed grid scoring via `_single_grid_task_cadence_aware` at `wr_bias_simulation.py:1580-1629`, and that the cadence library matters physically (real stars have 2–8 epochs with non-uniform sampling).
- Planned and implemented a fix: top CDF now imports `_me_cdf_band` (and Langer twin `_me_cdf_band_langer`), so both CDFs compute from the same helper. Also preserved `bv = info['best_vals']` so Grid Range Exclusion reactivity stays intact (NaN-masked `_hm_result` flows through `_method_best_and_hdi` → `method_results`).
- User tested the fix and caught a second bug: the dashed median line visibly exited the 16-84 band. Plots + scientist agents confirmed pure rendering artifact — `fill='toself'` polygon edges were linearly interpolated while the median used `shape='hv'` step.
- Fixed the band-vs-step mismatch in 4 files by replacing the single `fill='toself'` polygon with two `shape='hv'` traces using `fill='tonexty'`.
- Added E047 to COMMON_ERRORS.md for the step-line + linear-band pattern.
- Added two entries to `.claude/references/learnings.md` under a new "Plot Rendering" section.
**Decisions:**
- Import `_me_cdf_band` directly between sibling render modules rather than promoting to `helpers.py` — Dsilva and Langer twins have different signatures (π vs. period_model='langer2020') and cross-module imports match the existing pattern.
- Keep `n_sets=50` (matches Model Explorer, enables shared `@st.cache_data` hits) even though it widens the percentile band slightly — scientifically honest given 25 real stars.
- Out-of-scope: `_me_cdf_band_langer` is still non-cadence-aware; flagged as follow-up work.
- Out-of-scope: pre-existing `undefined name 'xv'` at `render_lk_explorer_langer.py:135` (predates this PR) — reported to user, not patched.
- Overrode the `# WORKING — do not change this code` flags with user's explicit permission; updated to `# UPDATED 2026-04-19: …` comments.
**Files modified:**
- `app/bc/render_shared.py` — `_render_all_methods_cdf` rewritten to delegate to `_me_cdf_band`, added `shape='hv'` and fallback warning
- `app/bc/render_shared_langer.py` — parallel rewrite using `_me_cdf_band_langer`; removed dead `pi_v`
- `app/bc/render_lk_explorer.py` — fixed band rendering in `_render_lk_resim_interp`
- `app/bc/render_lk_explorer_langer.py` — fixed band rendering in `_render_lk_resim_interp`
- `COMMON_ERRORS.md` — added E047 (Plotly step-line + linear-band mismatch)
- `.claude/references/learnings.md` — added "Plot Rendering" section (2 rules)
- Backups created: `Backups/render_shared.py.bak`, `Backups/render_shared_langer.py.bak`, `Backups/render_shared.py.bak2`, `Backups/render_shared_langer.py.bak2`, `Backups/render_lk_explorer.py.bak`, `Backups/render_lk_explorer_langer.py.bak`
- Plan file: `/Users/guyshtainer/.claude/plans/at-bias-correction-page-zany-zebra.md`
**Open questions:**
- User should visually verify: (1) top CDF and Model Explorer CDF overlay pixel-perfect at the same best-fit params; (2) Grid Range Exclusion narrowing causes Plot 1 best-fit label and CDF to update; (3) the median dashed line now stays inside the band after the fill='tonexty' fix.
- Langer Model Explorer / top CDF are still NOT cadence-aware (separate issue, deferred).
- Pre-existing `undefined name 'xv'` in `render_lk_explorer_langer.py` Re-sim button path — deferred.
**Memory updates:** None this session.
**Learnings:** Added 2 new rules under a new "Plot Rendering" section in `.claude/references/learnings.md`: (a) step line + step band must match; (b) first-glance plausibility is not enough — zoom into rendering details.

---
## 14:40 — Skill/plugin update audit + agent system overhaul
**Topics:** Claude Code plugins, Anthropic skills, agent system review, comms protocol, designer-coder-QA UI loop, CLAUDE.md minimalism
**Work done:**
- Audited installed plugins/skills: only `ralph-loop@claude-plugins-official` v1.0.0 is registry-tracked (up to date). Other Anthropic-style skills (`/simplify`, `/claude-api`, `/update-config`, `/loop`, `/schedule`, `/keybindings-help`, `/less-permission-prompts`) are bundled with the CLI binary, not marketplace-installable
- Reviewed 7-agent system — 3 parallel Explore agents found it was defined Apr 13-14 but never operationalized: `qa`, `designer`, `writer`, `meta-tools` had 0 invocations in 6 days; comms folder all "(never)" timestamps; CLAUDE.md had zero delegation rules
- Wrote plan v1 (lean: merge designer→plots, retire qa, kill comms). User course-corrected v1; wrote plan v2 (keep all 7, activate comms, reference-based CLAUDE.md, no cleanup). v2 approved
- Created `.claude/references/comms-protocol.md` — canonical briefing + comms write contract, standard chains, QA-FAIL 3-round loop
- Created `.claude/references/agent-delegation.md` — trigger table, hard rules, decision flow, parallel spawn rules
- Created `.claude/agents/comms/briefing.template.md` — fill-in skeleton
- Rewrote `.claude/agents/designer.md` — added UI Loop Role section (first step of designer→coder→QA), reads user-taste feedback memory, output format now includes Acceptance Criteria
- Rewrote `.claude/agents/qa.md` — human-style QA role, always invoked after coder, compares output vs designer spec + memory, PASS/FAIL/BLOCKED verdict with concrete fix suggestions; removed duplicated Validation Checklist
- Added `.claude/references/comms-protocol.md` reference line to all 7 agent files
- Added 3-line `## Agents` section to CLAUDE.md (92→97 lines) pointing to the two reference files
- Added delegation + protocol pointers to MEMORY.md under Agent Team
**Decisions:**
- Keep all 7 agents (user override of v1's merge/retire recommendation)
- QA always runs after coder when change touches UI/user-facing behavior/data display
- Designer is first step of UI loop; reads feedback memory for user taste
- Activate comms (not kill it) — broadcast via `comms/{agent}.md` files, orchestrator writes briefing.md
- CLAUDE.md stays minimal — rules go in `.claude/references/`, only pointed to from CLAUDE.md
- No cleanup performed (skill-creator name collision, 17-deep Streamlit sub-skill tree, etc.) — deferred per user
**Files modified:**
- `.claude/references/comms-protocol.md` (new)
- `.claude/references/agent-delegation.md` (new)
- `.claude/references/learnings.md` (added 2 new rules to Context & Memory section)
- `.claude/agents/comms/briefing.template.md` (new)
- `.claude/agents/designer.md` (UI Loop section + comms reference + richer output format)
- `.claude/agents/qa.md` (rewrote role + procedure, removed duplicated checklist)
- `.claude/agents/coder.md` (comms reference line)
- `.claude/agents/scientist.md` (comms reference line)
- `.claude/agents/plots.md` (comms reference line + added qa.md as upstream)
- `.claude/agents/writer.md` (comms reference line)
- `.claude/agents/meta-tools.md` (comms reference line)
- `CLAUDE.md` (+5 lines: `## Agents` section)
- `~/.claude/projects/.../memory/MEMORY.md` (Agent Team section: added 2 pointers)
- Plan file: `/Users/guyshtainer/.claude/plans/alright-now-that-you-graceful-kite.md`
**Open questions:** None — plan executed as approved
**Memory updates:**
- `learnings.md` new rule: "CLAUDE.md stays minimal — reference, don't inline" (Context & Memory section)
- `learnings.md` new rule: "Don't bulk-cleanup without explicit ask" (Context & Memory section)
**Learnings:** See memory updates above. No new COMMON_ERRORS patterns (all changes were markdown, no runtime code touched).

---
## 23:55 — Corner plot marginalization audit (code vs Dsilva 2022)
**Topics:** bias correction, corner plots, 1D/2D marginalization, normalized likelihood, Dsilva 2022 comparison, flat-prior posterior
**Work done:**
- User asked how the 1D marginal distributions in the corner plots are produced — normalized likelihood? unnormalized? logL? something else? And whether it matches Dsilva.
- Delegated audit to scientist agent (opus, paper-research skill). Research-only, no code edits.
- Scientist traced the data flow:
  - Joint grid = `result['likelihood']` = `exp(logL_raw − logL_max)` defined at [wr_bias_simulation.py:1561-1563](wr_bias_simulation.py#L1561-L1563) and `:1749-1751`. Linear likelihood, max=1.0, NOT integral-normalized.
  - 1D marginals: [`app/bc/corner_plots.py:197-201`](app/bc/corner_plots.py#L197-L201) uses `np.nansum` along all nuisance axes, then `_add_1d_posterior` at `:37-38` renormalizes each 1D curve via `np.trapezoid` so it integrates to 1.
  - 2D marginals: `:204-223` `np.nansum` over nuisance axes; HDI contours at 68%/95% from cumulative mass (`:72-81`).
  - Same marginalization used by `_method_best_and_hdi` at `app/bc/analysis.py:110-113`.
- Compared to Dsilva 2022 §5.2 p.6 col.2: "Assuming flat priors... marginalised posterior likelihood." Figs 7 and 8 captions both state flat priors. Under flat priors, posterior ∝ likelihood, so "marginalizing the posterior" reduces to integrating linear likelihood over nuisance axes — exactly what the code does.
- Verdict: implementation is scientifically correct and matches Dsilva 2022.
**Decisions:** No code changes. Audit only.
**Files modified:** None (research task).
**Open questions:**
- User will review tomorrow.
- Minor caveats flagged (for future reference, not action items):
  1. `nansum` assumes uniform grid spacing — fine currently; would need revisiting if any axis becomes non-uniform (e.g. log-spaced σ).
  2. `exp(logL − logL_max)` underflows to 0 far from max — same white-zone behavior Dsilva notes on Fig. 8 p.8.
  3. Dsilva plots a single 68% HDI contour; we plot 68% + 95%. Cosmetic deviation only.
- Scientist agent still alive (ID `a16505fe1a8846d38`) for follow-ups.
**Memory updates:** None.
**Learnings:** No new learnings. No new COMMON_ERRORS patterns (no code changes).

---
## 23:54 — Bias-correction CDF/score consistency bug (E048) — physics-config drift between grid and re-sim helpers
**Topics:** bias correction, CDF plateau, Model Explorer logL mismatch, `_me_cdf_band`, `BinaryParameterConfig`, `period_model`, `cadence_weights`, result-dict completeness contract, cadence-aware re-simulation, agent delegation (coder + learnings update)
**Work done:**
- User reported two symptoms that survived the 14:39 CDF alignment fix: (1) top "CDF Comparison" simulated curve flatlines at 0.5 from ~27 → 320 km/s (impossible for a valid best cell); (2) heatmap's stored best `logL_raw = -33.467` disagrees with Model Explorer's "Global best logL = -34.39" at the same parameters; (3) user manually found a higher-logL point in Explorer — algorithm "fails to find" best.
- Diagnosed root cause directly (no Explore spawn): `_me_cdf_band` at [app/bc/render_lk_explorer.py:48-93](app/bc/render_lk_explorer.py#L48-L93) builds a fresh `BinaryParameterConfig(logP_max=logPmax)` internally, discarding every other field of the user's `bin_cfg` AND silently defaulting `period_model` to `"powerlaw"`. The grid worker `_single_grid_task_cadence_aware` at [wr_bias_simulation.py:1607-1626](wr_bias_simulation.py#L1607-L1626) uses the FULL user `bin_cfg` and the correct `period_model`. One bug → all three symptoms, because every CDF/logL the helper recomputes lives on a physically different surface than `logL_raw`.
- Supporting bug in [app/bc/runners_cadence.py:480-496](app/bc/runners_cadence.py#L480-L496): result dict didn't persist `bin_cfg`, `period_model`, or `cadence_weights`, so downstream callers couldn't reproduce the grid cell even if they wanted to.
- Wrote detailed briefing to `.claude/agents/comms/briefing.md` (round 1) with validated root cause, recommended fix shape, acceptance criteria. Delegated to coder agent.
- Coder agent implemented end-to-end:
  - `runners_cadence.py:480-514` — result dict now persists `bin_cfg` (via `vars(bin_cfg)`), `period_model`, `cadence_weights`, `cadence_library`, `sigma_meas`.
  - `render_lk_explorer.py:48-119` — `_me_cdf_band` gained `_bin_cfg_dict` + `period_model` params, rebuilds full `BinaryParameterConfig`, only overrides `logP_max` from slider. Added `_bin_cfg_dict_as_hashable`, `_result_bin_cfg_tuple`, `_result_period_model` helpers.
  - All 4 callsites in `render_lk_explorer.py` + `render_shared.py:_render_all_methods_cdf` thread the new params.
  - Langer twins (`render_lk_explorer_langer.py`, `render_shared_langer.py`) mirrored — and `_me_cdf_band_langer` gained the missing cadence-aware branch (previously basic-only — mismatch with the cadence-aware grid).
  - Caption guarded by `np.isfinite(...)` → no more `logP_max=nan` in legend.
  - Backward-compat fallback + `st.info()` notice for legacy .npz.
  - New regression test `scripts/test_explorer_logL_consistency.py`: asserts `|logL_raw[best_idx] − recomputed| < tol`. PASS on the briefing's file (|Δ|=2.0 within legacy tol=8.0; will tighten to ~0.3 once user reruns grid with updated runner).
  - E048 added to `COMMON_ERRORS.md` (grep pattern: `BinaryParameterConfig\(logP_max=`).
- Coder ran `/error-check`: 17 PASS / 1 expected WARN (Streamlit-mock `KeyError`, pre-existing, covered by test_render.py). pyflakes clean, py_compile clean, smoke tests 7/7, test_render.py 5/5, test_explorer_logL_consistency.py PASS.
- Added 3 new rules to `.claude/references/learnings.md` under "Plot Rendering":
  - "A re-simulated plot MUST use the SAME bin_cfg / period_model / cadence_weights as the grid scoring worker"
  - "Helper functions that re-simulate must accept ALL physics-affecting params, not a subset"
  - "Result-dict completeness contract" (lists every field a runner must persist)
**Decisions:**
- One-bug-three-symptoms framing kept the fix surgical: touched only the helper + its callers + the runner's result-dict build step. No restructuring of the WORKING-flagged `_render_lk_model_explorer` body — only the 4 callsite lines inside it.
- `_bin_cfg_dict` kept `_`-prefixed (per E023) so Streamlit cache still hits across re-runs. Trade-off acceptable because cache is invalidated per result-load anyway.
- Coder also persisted `cadence_library` + `sigma_meas` into the result dict (not just the 3 briefing-listed fields) — previously these were injected at render time in `cadence.py:660-663`, which breaks a second-load from .npz. Persisting them makes the result dict fully self-contained. Additive; no breakage.
- Coder added cadence-aware branch to `_me_cdf_band_langer` (was basic-only). Briefing said mirror Dsilva shape; mirror wasn't complete without closing this pre-existing gap.
- Caption fix done at the caption site (guard with `np.isfinite`), not inside `_method_best_and_hdi`, to keep the change localized and keep nan as the correct mathematical answer for a degenerate 1-point HDI.
- Did NOT commit — user needs to rerun grid and visually verify in webapp before `/git`.
**Files modified:**
- `app/bc/runners_cadence.py` — lines 479-514 (result dict additions: `bin_cfg`, `period_model`, `cadence_weights`, `cadence_library`, `sigma_meas`)
- `app/bc/render_lk_explorer.py` — `_me_cdf_band` sig + body (47-119), 3 new helpers (122-180), 4 callsites updated
- `app/bc/render_lk_explorer_langer.py` — mirror: sig + body + new cadence-aware branch, 4 callsites updated
- `app/bc/render_shared.py` — `_render_all_methods_cdf` (242-346), `st.info` notice, caption `np.isfinite` guards
- `app/bc/render_shared_langer.py` — mirror of render_shared.py changes
- `COMMON_ERRORS.md` — E048 entry added
- `scripts/test_explorer_logL_consistency.py` — new regression test (file)
- `.claude/references/learnings.md` — 3 new rules in "Plot Rendering" section
- `.claude/agents/comms/briefing.md` — round-1 briefing written
- `.claude/agents/comms/coder.md` — coder's work log
**Open questions:**
- User must rerun a bias-correction grid (any size) after this fix so the new result-dict fields land in a fresh .npz, then visually verify: (1) top CDF tracks observed curve across full ΔRV range (not flat at 0.5); (2) Explorer's "Global best logL" matches heatmap's stored value within ~0.3 at n_sets≥1000; (3) caption no longer shows `logP_max=nan`; (4) same on Langer tab. After approval → `/git` commit.
- Pre-existing `NameError: xv` in `render_lk_explorer_langer.py:189` inside a `try/except Exception` — deferred (out of scope per "one file only / don't refactor nearby working code").
**Memory updates:**
- `.claude/references/learnings.md` — 3 new rules in Plot Rendering section (see Decisions above)
- No MEMORY.md pointer updates (Plot Rendering subsection already indexed)
**Learnings:** See Memory updates. E048 added to COMMON_ERRORS.md in the same round by the coder agent — it is the pattern the 3 new learnings rules prevent, so "learning" and "pattern" are logged together rather than separately.
