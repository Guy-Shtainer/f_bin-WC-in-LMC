# Project To-Do List


## Open Tasks

| ID | Title | Description | Priority | Tags | Status | Added by | Suggested by | Date added | Urgent | Important | Notes |
|----|-------|-------------|----------|------|--------|----------|-------------|------------|--------|-----------|-------|
| 217 | Validation strategy — representative-vs-outlier mock posterior comparison (3 sub-cases × 2 outlier tails) | Design captured during 16:14 discussion (no code). Plan: per separation regime (easy / medium / hard blend of singles vs binaries CDFs), run **3 sub-cases × 2 outlier mocks**, one outlier from each tail of the σ. Compare to a representative-mock pass with the new batch button (#216). Success criterion: posteriors should be **sharp** on representative mocks (centered on truth) and **wide** on outlier mocks while still containing the true parameters in the corner plot. Failure criterion: if posteriors stay sharp under outlier mocks, the simulation will report a confident wrong answer when real data happens to be an outlier — would force a redesign of the inference layer. The simulation already keeps full per-model likelihoods so the posterior over parameters can express that uncertainty; the experiment will reveal whether it actually does. | high | bias-correction, validation, research | open | Claude | Guy | 2026-05-19T16:14:00 | N | Y | Results expected 2026-05-20. Outcome decides whether current inference is trustworthy or needs revisiting. If posteriors remain sharp under outliers: open design question on how to flag "real data may be an outlier" from data alone (no plan yet). |
| 216 | Validation tab — one-click batch mock generation ("Generate N mocks, ascending seeds") | Restructured the Generate-Mock controls into a 3-column row: (a) single-click "Generate Mock Observations" (existing); (b) N-input + "Generate N mocks (ascending seeds)" button — runs seeds `seed, seed+1, …, seed+(N-1)` through the new `_generate_one_mock` helper with an `st.progress` bar; (c) "Clear stack". N persists to `grid_cadence_{dsilva,langer}.val_batch_n`. The helper does NOT call `st.rerun()` or clear Explorer CDF caches — caller owns both (cache clearing happens once per user action, not per-iteration). "Current mock state" in batch mode reflects the LAST (highest) seed so downstream code sees a consistent realisation. Each generation stacks on the CDF preview the same way as single-click. N has no min/max constraints (per `feedback_index.md`); guard with a warning if `N <= 0`. Implementation in `app/bc/render_validation.py` (~357-line diff includes the helper extraction, layout, batch handler with progress bar, single final `st.rerun()`). | high | bias-correction, validation, feature, ux | to-test | Claude | Guy | 2026-05-19T16:13:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify (a) clicking "Generate N mocks" with N=5 produces 5 stacked CDFs (seeds increment by 1); (b) progress bar advances; (c) Clear stack resets; (d) "Current mock state" matches seed `start+N-1`; (e) N field persists across reruns via settings. Tests against #217 plan. |
| 215 | Validation tab — bin-edges sync now fires from 3 load paths (extends #214) | TODO #214 (yesterday) wired the `f'{p}_is_loaded_result'` one-shot flag from the **validation-tab** saved-runs loader only. User testing surfaced two missing paths: (a) **cadence-tab** "Load saved result" loader did not set the flag, so loading a validation `.npz` through the broader cadence-tab picker left the bin widgets at their stale values; (b) **fresh run-completion** path also needed sync — when a new validation run finishes, the global + Explorer bin widgets should reflect the bins that were actually used in the run, regardless of what the user had typed before clicking Run. Fix: in `app/bc/cadence.py:1062`, the in-tab Load handler now also sets `_is_loaded_result=True` + records `_val_last_seen_result_id`; in `app/bc/render_validation.py`, a new run-completion detector compares `result['timestamp']` against `f'{p}_val_last_seen_result_id'` (with `id(result)` as fallback for older saved files without timestamps) and fires the same flag before cadence delegation. | high | bias-correction, validation, ux | to-test | Claude | Guy | 2026-05-19T16:13:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify (a) loading a validation `.npz` via the cadence-tab picker now syncs both bin widgets (previously broken); (b) running a fresh validation with custom bins → widgets reflect the actual run bins post-completion without needing a manual Load; (c) #214 path (validation-tab Load) still works. Timestamp marker chosen because worker writes it per-run; fallback `id(result)` covers legacy files. |
| 214 | Validation tab — sync likelihood bins on Load via one-shot `is_loaded_result` session flag | When loading a saved validation result, both bin widgets (simulation-params global + Model Explorer) now pre-fill with the saved run's `likelihood_bin_edges` automatically. Approach: Load handler in `render_validation.py:101` sets `f'{p}_is_loaded_result'=True`; both `_render_likelihood_bin_config` and `_render_explorer_lk_bin_config` in `params.py` check the flag at the top and override their session-state keys via the new `_infer_lk_bin_mode(bin_edges)` helper (recognises canonical `[0,T,250,650,inf]` schema as Threshold-based; everything else is Manual with comma-joined finite edges). Flag cleared at end of `_render_single_point` so the override is strictly one-shot — user edits after Load are preserved. Global widget gains a `default_bin_edges` parameter; `cadence.py:1087` threads `result['likelihood_bin_edges']` in at the call site. User explicitly rejected an earlier helper-based design ("you don't need a helper, the code already has a default value"). | high | bias-correction, validation, ux | to-test | Claude | Guy | 2026-05-18T23:55:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify: (a) Threshold result `[0,45.5,250,650,inf]` loads both widgets at Threshold-based + 45.5; (b) Manual result `[0,60,200,800,inf]` loads both widgets at Manual + `0,60,200,800`; (c) override-from-stale: manually set Manual + custom values, then Load a Threshold result → widgets flip to Threshold (one-shot override); (d) user-edit-preserved: after Load, change a widget → click Run → recovery uses new bins (flag cleared); (e) legacy result without `likelihood_bin_edges` → override block skips harmlessly. New learning recorded in `.claude/references/learnings.md` Context & Memory. |
| 213 | Validation tab — stack mock observations on CDF preview (multi-click distribution inspection) | Each click of "Generate Mock Observations" now appends to a per-page session-state stack `f'{p}_val_mock_stack'` instead of overwriting. Prior mocks render as thin dotted colored step CDFs on the Mock Preview chart (no markers, only step lines, to keep the chart readable). Clear-stack button with live counter next to Generate. Active (most-recent) mock still drives the recovery flow. Snapshot stores ONLY `delta_rv` + `is_binary` + seed + truth params (no per-star RV/err arrays) so session-state stays small after 20+ stacked mocks. Colors cycle through `_SNAPSHOT_PALETTE`. Counter caption "📚 N mocks stacked …" appears when ≥2 entries. | high | bias-correction, validation, feature | to-test | Claude | Guy | 2026-05-18T22:45:00 | N | Y | UNCOMMITTED — visual sign-off needed. Restored feature that existed in a prior uncommitted state but never made it to git. Verify multiple clicks (vary seed) produce stacked dotted CDFs in cycled colors; Clear button resets the stack; recovery grid still uses the most-recent mock. |
| 212 | Model Explorer snapshot CDF — resolution + 16/84 band fix | Initial snapshot implementation (#211) stored `median_cdf` (binned step values) and drew a fake shadow filling from the median down to y=0. User noticed two visual bugs: (a) saved CDF looked coarser/lower-resolution than the live Explorer line (because step at `be` vs smooth `smooth_pooled_cdf`), (b) the shadow band went to the x-axis instead of being a symmetric 16/84 band around the median. Fix: at Save time, capture the full `smooth_pooled_cdf(pooled_drv, _n_sets_me)` 5-tuple `(_sp, _yp, _xf, _lo, _hi)` as tuples-of-floats; at render time, replay with the same lo/hi/median pattern as the live Explorer trace at `render_lk_explorer.py:1633-1650`. Saved snapshots now visually match the live Explorer at the same params + n_sets. Applied to both DSilva and Langer twins. Backward-compatible: legacy snapshots without `smooth_cdf` fall back to step rendering. | high | bias-correction, plots, bug | to-test | Claude | Guy | 2026-05-18T20:30:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify saved snapshot CDF lines match the live Explorer in smoothness; shadow band is symmetric around median (not zero-fill). |
| 211 | Model Explorer — tunable likelihood-bin editor + saved-attempts snapshot table + colored CDF overlays | Implemented across `params.py` (`_render_explorer_lk_bin_config` helper, Threshold/Manual modes mirroring the global widget but persisting to `explorer_likelihood_bin_config` namespace so the simulation's saved bins are never overridden), `helpers.py` (`_SNAPSHOT_PALETTE` of 8 distinct colors), `render_lk_explorer.py` (bin editor wired above sliders, snapshot store `f'{prefix}_lk_me_snapshots'`, 💾 Save / 🧹 Clear-all UI, per-row × delete, colored CDF traces + dashed median per snapshot, optional bin-edge vlines in matching colors when "Show likelihood bin edges on CDF" is toggled), and `render_lk_explorer_langer.py` (mirrored twin with independent `_lk_me_langer_snapshots` store). Lets the user investigate whether their grid's logL is genuinely poor or whether bin choices are the cause — tweak bins, hit Save, compare logL values across attempts side-by-side. Snapshots are session-only (not persisted to disk). | high | bias-correction, validation, feature, ux | to-test | Claude | Guy | 2026-05-18T19:00:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify: bin editor visible above sliders, Threshold + Manual modes work, defaults match simulation on first open, hitting Save adds a colored row + a new CDF trace, per-row × delete works, Clear all empties the stack, both DSilva and Langer tabs behave identically, `settings/user_settings.json` shows new `explorer_likelihood_bin_config` block but `likelihood_bin_config` (simulation namespace) is unchanged. |
| 210 | Validation tab — Max ΔRV control removed; auto-derive from observed ΔRVs (Mock Observation CDF was truncated at 460 km/s) | Removed the user-facing "Max ΔRV (km/s)" `number_input` from the Cadence-aware settings panel. New helper `_auto_drv_max(obs_drv, bin_width, headroom_bins=5)` in `app/bc/params.py:442-452` rounds `max(obs)` up to a bin-width multiple plus 5 headroom bins (default 500 km/s fallback when observations aren't loaded). Both `_render_cadence_adaptive_bins` non-adaptive branch (params.py:482-495) and `_cadence_run_and_results` (cadence.py:1015-1028) now call the helper so the displayed bin count matches the simulation bin grid exactly. Caption changed from `N bins` → `N bins · max ΔRV = X km/s (auto)`. Mock Observation CDF step line now extends to the full data range instead of truncating at the user's input. | high | bias-correction, validation, ux, bug | to-test | Claude | Guy | 2026-05-18T16:30:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify "Max ΔRV" input is gone, only "ΔRV bin width" remains, caption shows the auto max; Mock Observation step extends past previous 460 km/s out to the actual data max (~900 km/s). Adaptive mode unchanged. |
| 209 | Validation lane — attach truth fields (`true_fbin`/π/σ/logPmax) to in-memory result on fresh runs (E054) | Bug: in `_run_cadence_bg` mock-backend branch at `runners_cadence.py:672`, `result['is_validation'] = True` was set but the four `true_*` keys were persisted to disk only (via `_vio.save_validation_result(..., _validation_truth, ...)`) — never attached to the in-memory `result` returned to the UI. Consequence: corner-plot `_truth_for(axis_name)` returned None on fresh runs (key missing) → green dashed truth lines invisible; the lines reappeared only after the user reloaded the same `.npz`. Fix: 5 added lines that mirror the disk metadata onto the in-memory dict — `result['true_fbin'] = float(_validation_truth.get('true_fbin', np.nan))` and the three siblings. Pattern recorded as **E054** in COMMON_ERRORS.md ("Validation lane persists metadata to disk but not to the in-memory `result` dict"). | high | bias-correction, validation, bug | to-test | Claude | Guy | 2026-05-18T15:45:00 | N | Y | UNCOMMITTED — visual sign-off needed. Verify: fresh validation run with non-default truth (e.g., fbin=0.5, σ=10, logPmax=3.5) → corner plot green dashed lines appear on 1D diagonals + green × on 2D off-diagonals without reloading from disk. |
| 208 | Repo housekeeping — `.gitignore` exclude large simulation outputs + agent skill restructure | Two infrastructure changes pushed in commits `f40576e` + `5bef671`: (a) `.gitignore` now excludes `results/` (2.3 GB of .npz), `mock_results/` (222 MB), `bin_sensitivity_results/`, `settings/states/`, `bias_app/.cache/` + `.obs_delta_rv_cache.npy`, `mock_inspector_app/__pycache__/`, `scripts/.agent_live_output.txt` + `.agent_work/`, and `papers/*.pdf` — none of these were meant to live in git history; (b) all `.claude/agents/*-skills/` subdirectories relocated to `.claude/skills/<role>/` to match the harness's updated skill discovery layout. Pure renames; agent behaviour unchanged. | medium | infrastructure, repo | done | Claude | Guy | 2026-05-18T17:55:00 | N | N | Committed and pushed (`f40576e`, `5bef671`). |
| 207 | Bulk commit of accumulated validation pipeline + Model Explorer work (commit `faf92da`) | 5 commits pushed to main today during `/git` (commits `f40576e`, `faf92da`, `5bef671`, `a67f51c`, `d462fa5`). The big code commit `faf92da` (23 files, +7432/-820) bundled multi-session work that had been queued since 2026-05-04 awaiting visual sign-off: Max ΔRV removal, truth attachment to in-memory result, Model Explorer bin editor + snapshot table, validation_io module owning mock_results/ schema, refresh of helpers/params/cadence runners/render_validation/render_shared/sim_plots. Refreshed plot PDFs in `plots/` shipped in commit `d462fa5`. Verification pages + diagnostic scripts + standalone `mock_inspector_app/` + `docs/` + `environment.yml` shipped in commit `a67f51c`. | high | bias-correction, infrastructure | done | Claude | Guy | 2026-05-18T17:50:00 | N | Y | Pushed to remote. Code visual sign-off still pending for several features inside this commit — those have separate TODO entries (#209-#214). |
| 185 | Validation tab overhaul: mock-preview + parameter-recovery diagnostics + A&A white-bg styling | Added `generate_mock_observations_detail()` to `app/bc/validation.py` — cadence-aware single-set mock that returns per-star `is_binary`, per-epoch RVs, `rv_min/rv_max`, `n_epochs` alongside ΔRV. Added pre-run `_render_mock_preview` to validation tab: side-by-side CDF + binary-fraction-vs-threshold plots with red (single) / green (binary) dots per star, plus a mock-star table (#, Type, N_ep, RV_min/max, ΔRV p2p, per-epoch RVs) with row-color highlighting. Added clearer "Simulation Parameters & Recovery Run" section header above the delegated Dsilva/Langer run. Added `n_sets_override: int or None` kwarg to both `_render_cadence_dsilva_tab` and `_render_cadence_langer_tab` in `app/bc/cadence.py` (applied as default only when session-state key is unset); validation tab now passes `n_sets_override=500` (previously defaulting to 10 000, which inflated config without need for validation runs). built new **parameter recovery diagnostics** section post-run: `_render_validation_diagnostics`, `_render_panel_a_fbin`, `_render_panel_b_cdf`. panel a = truth-vs-recovered table + `f_bin(t)` overlay with residual panel. panel b = δrv cdf overlay with 68 % hdi band from 200× bootstrap of `gap_sim['delta_rv']` chunks of size `n_mock` + residual panel. `render_validation.py` grew from ~330 → 1281 lines (above claude.md soft cap, split deferred until after visual sign-off). discovered and fixed root-cause "ships-dark-instead-of-white" bug: `plotly_theme` is always dark in this project (`app/shared.py:171` hardcodes `_dark_palette`), so any plot using `plotly_theme` alone is a&a-illegal. converted all new diagnostic figures to explicit white-bg via a module-level `_aa_overrides` dict with wcag-audited colors. rewrote `.claude/agents/plots.md` "plotly theme application" section with the `_aa_overrides` recipe, subplot axis-name gotcha, and a mandatory exit checklist (state bg color, list every text element + its color, grep for 'white'/'gold' before claiming done). rewrote `memory/feedback_aa_journal_style.md` to surface the plotly_theme-is-dark trap. saved `memory/validation_cdf_vs_fbin_diagnostic.md` with scientific reasoning (cdf degeneracy in f_bin/π direction, why f_bin(t) is more informative, mc-noise floor at 100 sets × 25 stars) + `memory/pending_test_validation_diagnostics.md` — visual sign-off checklist for 2026-04-22. | high | bias-correction, validation, plots | to-test | Claude | Guy | N | N | Y |  |
| 183 | Bias-correction CDF/Explorer logL consistency fix (E048) — physics-config drift in re-sim helper | `_me_cdf_band` (Dsilva + Langer) was building a fresh `BinaryParameterConfig(logP_max=...)` and discarding the user's `bin_cfg` AND silently defaulting `period_model='powerlaw'`, while the grid worker `_single_grid_task_cadence_aware` used the full `bin_cfg` + correct `period_model`. One bug → three symptoms: top CDF flatlines at 0.5 from ~27 km/s, heatmap stored `logL_raw` disagrees with Explorer's "Global best logL" at the same params, manual exploration finds higher-logL points the algorithm missed. Fix: result dict at `runners_cadence.py:480-514` now persists `bin_cfg`, `period_model`, `cadence_weights`, `cadence_library`, `sigma_meas`; `_me_cdf_band` (and Langer twin) gain `_bin_cfg_dict` + `period_model` params and rebuild full config; all 4 callsites threaded; Langer twin gained missing cadence-aware branch; caption guarded with `np.isfinite` (no more `logP_max=nan`). New regression test `scripts/test_explorer_logL_consistency.py`. E048 added to COMMON_ERRORS.md. 3 new rules in `.claude/references/learnings.md` Plot Rendering. /error-check 17 PASS / 1 expected WARN. | critical | bias-correction, bug, plots | to-test | Claude | Guy | 2026-04-19T23:54:00 | Y | Y | User must rerun grid (any size) to land new result-dict fields, then verify (1) top CDF tracks observed across full ΔRV range, (2) Explorer "Global best logL" matches stored heatmap logL within ~0.3 at n_sets≥1000, (3) caption no longer shows `logP_max=nan`, (4) same on Langer tab. Pre-existing `NameError xv` at `render_lk_explorer_langer.py:189` deferred. |
| 181 | Skill/plugin audit + agent system overhaul (comms activation, designer/QA rewrite) | Audited installed skills/plugins: only `ralph-loop@claude-plugins-official` v1.0.0 is registry-tracked; bundled CLI skills (`/simplify`, `/claude-api`, `/update-config`, `/loop`, `/schedule`, etc.) are not marketplace-installable. 3 parallel Explore agents found agent system defined Apr 13-14 but never operationalized — 4/7 agents had 0 invocations in 6 days, comms folder all "(never)" timestamps, CLAUDE.md had zero delegation rules. Built v2 plan (keep all 7 agents, activate comms, reference-based CLAUDE.md). Created `.claude/references/comms-protocol.md` (briefing + comms write contract, QA-FAIL 3-round loop) and `.claude/references/agent-delegation.md` (trigger table, hard rules, decision flow). Rewrote `designer.md` (UI Loop role, reads user-taste memory, Acceptance Criteria) and `qa.md` (PASS/FAIL/BLOCKED verdict, removed duplicated checklist). Added comms-protocol pointer to all 7 agent files; added `## Agents` section to CLAUDE.md (92→97 lines); added Agent Team pointers to MEMORY.md. | high | agent, infrastructure | to-test | Claude | Guy | 2026-04-19T14:40:00 | N | Y | No real-task validation yet. No cleanup performed (skill-creator name collision, deep skill trees) — deferred per user. New learnings: "CLAUDE.md stays minimal — reference, don't inline" + "Don't bulk-cleanup without explicit ask". |
| 180 | Bias-correction CDF alignment + step-band rendering fix (E047) | Top "CDF Comparison" plot used `simulate_delta_rv_sample` (non-cadence-aware, n_stars=1000) while the likelihood grid + Model Explorer use `simulate_delta_rv_cadence_aware` — different curves for the same best-fit. Top plot also omitted `shape='hv'` so Plotly cubic-spline-smoothed the empirical CDF. Fix: top CDF now imports `_me_cdf_band` (and Langer twin `_me_cdf_band_langer`); both CDFs compute from the same helper. Preserved `bv = info['best_vals']` so Grid Range Exclusion reactivity stays intact. User caught secondary bug: dashed median visibly exited the 16-84 band — pure rendering artifact, `fill='toself'` polygon edges linearly-interpolated while median used `shape='hv'` step. Replaced single `fill='toself'` polygon with two `shape='hv'` traces using `fill='tonexty'` in 4 files. E047 added to COMMON_ERRORS.md. 2 new rules in `.claude/references/learnings.md` under new "Plot Rendering" section. | high | bias-correction, plots, bug | to-test | Claude | Guy | 2026-04-19T14:39:00 | N | Y | User to visually verify: (1) top CDF and Explorer CDF overlay pixel-perfect at same best-fit; (2) Grid Range Exclusion narrowing updates Plot 1 best-fit + CDF; (3) median dashed line stays inside band. Out of scope: `_me_cdf_band_langer` still non-cadence-aware (deferred); pre-existing `undefined name xv` at `render_lk_explorer_langer.py:135` (predates this PR). Overrode WORKING flags with explicit user permission; updated to `# UPDATED 2026-04-19: …` comments. |
| 179 | Bias-correction resume cadence checkpoint sim-context fingerprint guard (E046) | Cancel-saved cadence-aware grids could resume with a horizontal-row kink in the `f_bin × σ_single` likelihood heatmap, potentially faking a best-likelihood cell. Root cause: `_save_partial_cadence` stored only `logL_raw + grid axes + n_sets/drv_bin_width/drv_max/adaptive_bins`; on resume, `bin_cfg / sigma_meas / bin_edges / likelihood_bin_edges / cadence_library / obs_delta_rv` were re-pulled from current UI + `@st.cache_data` caches without validation. Page refresh or Streamlit-process restart (cache eviction) can recompute these to a slightly different array → systematic logL offset for post-resume cells. Fix: added `_array_fingerprint`, `_cadence_lib_fingerprint`, `build_sim_context_signature`, `diff_sim_contexts` in `helpers.py:99-170`; build + persist `_sim_context` + `_sim_context_hash` as new npz keys at run start in `runners_cadence.py:97-115`; on resume in `cadence.py`, validate signature → branch on status (ok/legacy/mismatch) with field-level diff + Start Fresh/Cancel buttons. Caught Streamlit button-flow bug (`st.button` inside conditional block dropped first click) and re-armed `_auto_resume`+`_resume_from` session_state before `st.stop()`. E046 added to COMMON_ERRORS.md. /error-check 16/16 PASS. | high | bias-correction, simulation, bug | to-test | Claude | Guy | 2026-04-19T14:38:00 | N | Y | Backward compat: legacy checkpoints without `sim_context` get yellow warning and proceed. npz keys purely additive. Added learning: "Verify explore-agent hypotheses against the code before accepting them" — Explore agent's "seed mismatch" hypothesis was wrong (per-cell seed is pure function of grid indices). Next reproduction's diff message will pinpoint which field drifted. |
| 178 | Bias-correction Langer tab slider crash fix (E045) | `StreamlitAPIException: max_value must be >= min_value` raised in `_make_range_slider` when the Langer model's grid axis has a single value (σ_single=7.5 fixed → `min_value == max_value`). Added guard at `app/bc/helpers.py:100-111`: when `lo >= hi`, display static `st.markdown` text label ("σ_single = 7.5 (fixed)") instead of slider, return `(lo, hi)` tuple. Minimal surgical fix to a WORKING-flagged function with explicit user request. E045 added to COMMON_ERRORS.md. | high | bias-correction, bug, langer | to-test | Claude | Guy | 2026-04-19T13:09:00 | Y | Y | Greppable pattern: `st\.slider\(.*min_value=.*max_value=` followed by no min/max guard. |
| 177 | Plots-agent A&A style + contrast review protocol | Strengthened `.claude/agents/plots.md` with HARD RULE #1 (WCAG contrast) and A&A Journal Standards + mandatory 6-step Review Protocol (background → axes → titles → legend → gridlines → annotations). Fixed 12 plotly_chart calls missing `theme=None`, 7 bare-string axis titles, and 12 update_xaxes/yaxes missing `title_font` in `app/spectrum_helpers.py`. Added `title.font.color=black` to `_ACADEMIC_AXIS`. | high | plots, agent, A&A | to-test | Claude | Guy | 2026-04-14T17:00:00 | N | Y | Visual verification still pending per-page. |
| 176 | Standalone spectrum_app + rv_modeling_app extractions | Extracted `app/pages/02_spectrum.py` into `spectrum_app/` (8 files, ~1500 lines, already committed in d53d92d) and `app/rv_modeling/` into `rv_modeling_app/` with isolated copy. Added peak-to-peak epoch banner, quick-zoom diagnostics per line group, ISE anchor/interp toggles, unnormalized COMBINED plot, raw FITS fallback, LMC correction. Reorganised `DIAGNOSTIC_LINES` (removed O V 5590; new Nebular, OB companion absorption, WR emission, WC/WO groups). Added Laplace + Gen. Normal to RV-modeling histogram fit; auto-fit-all ranking banner; 9-panel orbital histogram grid in Tab A. Fixed `KeyError 'n_epochs'` dict-contract bug. | high | webapp, spectrum, rv-modeling | to-test | Claude | Guy | 2026-04-14T16:40:00 | N | Y | `rv_modeling_app/` not yet committed. Scientist flagged possible `simulate_binary_rvs_raw` bug (wr_bias_simulation.py:817 may skip `_draw_measurement_noise`) — unconfirmed. |
| 175 | AIC/BIC model selection in Compare tab | Added `logL, k, AIC, ΔAIC, BIC, ΔBIC` columns + caption to Compare best-fit table in `app/bc/extras.py::_render_compare_tab()`. `k` derived dynamically from grid-axis sizes > 1 so single-σ / single-logP_max runs get correct penalty; N = len(obs_delta_rv) = 25. Dsilva/Langer tabs untouched per scope guardrail. `/error-check` PASS (pyflakes + render test). | high | bias-correction, paper | to-test | Claude | Guy | 2026-04-14T16:20:00 | N | Y | Visual verification in webapp still pending. |
| 174 | DOCUMENTATION §4b: intrinsic RV variability + period range justification | Literature review on intrinsic ΔRV of single WR stars (~40 citations). Added new §4b to `DOCUMENTATION.md` with 6 subsections (wind-induced scatter by subtype, line dependence, metallicity effects, minimum floor, period-range justification, threshold implications). 6 new rows in §5 Key Numbers table. §6 References expanded from 5 to 22 entries. Conclusions: logP ∈ [0.15, 5.0] well-justified for WC; no minimum σ_RV = 0 (observed floor 1.8 km/s, theoretical ~2 km/s); WC wind variability 0–6 km/s vs WN 5–15 km/s. | high | paper, documentation, research | to-test | Claude | Guy | 2026-04-14T14:30:00 | N | Y | Feeds paper methods/discussion sections. |
| 173 | Agent team + role-based skill assignment | Created 7 role-based agents (coder/qa/designer/plots/scientist/writer/meta-tools) in `.claude/agents/` with embedded domain knowledge, file-based inter-agent comms, and per-agent skill directories. Moved 13 skills from `.claude/skills/` into agent-specific dirs and added 6 new skills (paper-research, python-production, live-testing, testable-code, academic-writing, latex-helper). Orchestrator skill count reduced from 19→6. | high | agent, infrastructure | to-test | Claude | Guy | 2026-04-13T16:25:00 | N | Y | Needs real-task validation in next session. |
| 172 | Exclusion-aware best-fit for orbital-property histograms (gap_sim) | `gap_sim` (10k-star simulation feeding Binary Orbital Properties histograms) was computed from the unmasked likelihood before grid exclusion, so exclusion sliders updated the heatmap best-fit but histograms stayed stale. Added `_find_best_model()` helper to `cadence.py` and moved grid-exclusion call before best-fit/gap_sim in both `_render_cadence_results` and `_render_cadence_results_langer`. `subtabs._render_analysis_plots` now prefers `ctx['best_model']` for histogram labels. | high | bias-correction, bug | to-test | Claude | Guy | 2026-04-13T16:25:00 | N | Y | All 16 error checks passed (static + py_compile + smoke 2D/3D/4D/all-NaN/Langer + render). |
| 96 | Validate NRES RVs + add STD-per-epoch graph | Verify the RVs derived from NRES spectra are genuine and reliable. Add a diagnostic plot showing the standard deviation of RVs within each epoch, to check for outlier epochs or instrumental systematics. Related to #51 (NRES analysis). | high | NRES, RV threshold | open | Claude | Tomer | 2026-03-10 | N | Y |  |
| 30 | Make CCF settings editable from webapp | The ccf_settings_with_global_lines.json should be easily editable from the CCF page | medium | webapp, CCF | open | Claude | Guy | 2026-03-01 | N | N |  |
| 7 | Publication-quality figures | Generate final plots for thesis/paper — CDF comparison, corner plot, orbital params | low | paper, plots | open | Claude | Guy | 2026-03-01 | N | Y |  |
| 32 | Add more reference papers | Add relevant papers used for overview and references to papers/ folder | low | paper | open | Claude | Guy | 2026-03-01 | N | N |  |
| 113 | Overnight agent git safety: git_commit_files + stash | Replaced `git_commit_all()` (which did `git add -A` sweeping user files) with `git_commit_files()` that only stages specified files. Added stash push/pop in finally block to preserve user uncommitted work. | high | agent, reliability, declined | open | Claude | Guy | 2026-03-12T12:20:00 | N | Y | [DECLINED 2026-04-23T14:19:16] I stopped working on the agent now |
| 114 | Git worktree isolation for overnight agent | Converted overnight_agent.py from branch-checkout to worktree isolation (`../agent-worktree/`). All `run_pipeline`, `run_freeform_task`, `agent_loop` now use worktrees. Added DONE status to agent_app. Added `[T]est` worktree option to `--stop` review. | high | agent, reliability | to-test | Claude | Guy | 2026-03-12T13:27:00 | N | Y |  |
| 130 | Live likelihood-vs-sigma graph disappears after run + mislabeled | In Dsilva cadence tab, the live likelihood vs sigma_single graph disappears when simulation completes (not persisted like heatmaps). Also, labels say "p-value" when showing likelihood. | high | bias-correction, bug, labels | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 1. Fixed: persist live_sigma_1d in session_state, re-render in done branch. |
| 131 | "p-value" labels used for non-p-value metrics throughout results | Throughout the results page, "p-value" is used in titles/axes/hover for CvM S-scores and Likelihood values which are NOT p-values. K-S p-values are correct; CvM should say "S-score"; Likelihood should say "Likelihood". | high | bias-correction, labels | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 1. Fixed in helpers.py, extras.py, analysis.py. |
| 132 | sigma_single missing from Scoring Method Comparison table | The Scoring Method Comparison table shows f_bin and pi but omits sigma_single even when it's a grid axis (cadence Dsilva with sigma scan). | high | bias-correction, bug | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 2. Fixed: added Best σ_single + 68% HDI σ_single columns. |
| 133 | Per-method sigma slider + score-vs-sigma + interactive Model Explorer | Each scoring method window needs: (1) its own sigma_single slider, (2) a score-vs-sigma graph (not just Likelihood), (3) Model Explorer should be interactive with parameter sliders for exploration. | high | bias-correction, feature | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 3. Fixed: (a) score-vs-sigma for all 4 methods, (b) sigma slider per expander, (c) interactive Model Explorer with 3 sliders. |
| 134 | Corner plots should include sigma_single as 3rd parameter | All corner plots ignore sigma_single, showing only f_bin × pi. When sigma_single is a grid axis, corner plots should be 3×3 including sigma. | high | bias-correction, feature | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 3. Fixed: extracted to corner_plots.py, renders 3×3 (pi×fbin×sigma) when sigma>1. |
| 136 | Parabolic fit ignores sigma_single (should be 3D/4D) | The 3D parabolic fit in scoring method windows doesn't include sigma_single as a model variable. sigma_grid and full ND arrays are not passed from method expander to _render_cvm_analysis. | high | bias-correction, bug | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 2. Fixed: pass sigma_grid + full ND arrays from method expander. Dsilva 4D: best logPmax slice. |
| 137 | Likelihood 2D interpolation graphs say "S" not "Likelihood" | In the Likelihood scoring window, the 2D interpolation heatmap titles and labels say "S" or "-logL" instead of "Likelihood". Should match what the graph actually shows. | high | bias-correction, labels | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 1. Fixed: added _stat_display variable, updated titles/success messages. |
| 138 | Best-fit summary table should be per-method, not shared at bottom | The best-fit summary table sits outside all scoring method windows and only shows K-S results. Each method should have its own summary table inside its window. | high | bias-correction, bug | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 4. Fixed: per-method table inside each expander, removed 203-line shared table from cadence.py. |
| 139 | CDF comparison at top with per-method toggleable shadows | The CDF comparison graph should be at the top above all scoring method windows (not inside K-S expander). Show best CDFs from all methods with error bands as shadows. Per-method toggles to show/hide each CDF. | high | bias-correction, feature | to-test | Guy | Guy | 2026-03-18T12:00:00 | N | Y | Group 4. Fixed: CDF moved to top of cadence results, per-method checkboxes, removed from K-S expander. |
| 140 | Fix likelihood f_bin=1 degeneracy — per-epoch error model for binaries | Binary RVs in simulation had ZERO noise (pure orbital). Face-on binaries got ΔRV≈0 exactly, making f_bin=1 cost-free. Fix: add per-epoch measurement noise from error model distribution to both singles and binaries. Separate error model selectors for singles vs binaries in UI. | critical | bias-correction, simulation, bug | to-test | Claude | Guy | 2026-03-18T16:30:00 | Y | Y | Root cause of likelihood always giving f_bin=1. Added _draw_measurement_noise(), error_model_single/binary fields to SimulationConfig, plumbed through all workers. |
| 141 | Likelihood vs sigma graph disappears after run completes | In Dsilva cadence tab, the live likelihood vs sigma_single graph disappears when simulation runs finish. Should persist. | high | bias-correction, bug | to-test | Guy | Guy | 2026-03-18T16:30:00 | N | Y | Duplicate of #130. Fixed 2026-03-21: runners_cadence.py live sigma graph 4D marginalization fix. |
| 142 | Fix p-value label misuse throughout results page | Many graphs incorrectly say 'p-value' when showing likelihood, CvM S-score, or other metrics. Review ALL graphs and fix titles/labels to match actual content. | high | bias-correction, bug | to-test | Guy | Guy | 2026-03-18T16:30:00 | N | Y | Duplicate of #131. Fixed 2026-03-21: colorbar + label overrides in analysis.py. |
| 144 | Per-method sigma_single slider + score vs sigma graph in each scoring window | Each scoring method expander should have its own sigma_single control and score-vs-sigma plot. Currently the Model Explorer window has no exploration ability. Make it useful. | high | bias-correction, feature | to-test | Guy | Guy | 2026-03-18T16:30:00 | N | Y | Duplicate of #133. Fixed 2026-03-21: per-method sigma slider + logPmax slider in analysis.py. |
| 147 | 3D parabolic fit must include sigma_single (4D fit) | The parabolic best-fit interpolation ignores sigma_single as a model variable. Should be a 4D fit (f_bin, pi, sigma_single, score). Already have _parabolic_min_3d — use it with all 3 axes. | high | bias-correction, feature | open | Guy | Guy | 2026-03-18T16:30:00 | N | Y |  |
| 151 | Playground f(T) must apply 4σ significance criterion | In the Playground (physics-based mode), the f(T) survival curve currently counts stars where ΔRV > threshold. It should also apply the 4σ significance filter: count only stars where (ΔRV > threshold) AND (ΔRV − 4σ_combined > 0). Without this, f(0) = 1.0 even with nonzero errors, which doesn't match the observed data that uses the significance criterion. Affects compute_physics_fraction_curve in compute.py. | high | rv-modeling, research, methodology | open | Claude | Guy | 2026-03-19T19:15:00 | N | Y | User noted: "even if the RV threshold is 0, it can't be 100% binarity because of the 4sigma criteria" |
| 152 | Plots page overhaul — add all missing notebook plots + restructure | Full plan in `plans/plots_page_overhaul.md`. Split xshooter.py (825 lines) into sub-tab modules. Add 9+ missing plots from notebooks: ΔRV scatter, piecewise fit+elbow, equiv thresholds, agreement ranking, epoch-strip dashboard, SNR requirements, survival function, PDF intersection, anchor points. Add sorting controls + toggles. Agent must: (1) do 2 discovery rounds first, (2) implement all phases, (3) run 3 improvement rounds, (4) run 3 error check passes. Skills pre-installed: developing-with-streamlit, ui-ux-pro-max, scientific-dashboard-design. | critical | webapp, plots, agent-task | to-test | Claude | Guy | 2026-03-19T19:17:53 | N | Y | [Completed 2026-03-20] Split xshooter.py 825→47 lines into 8 sub-tab modules + compute.py. Added 9 missing plots. 2 review rounds done. Full plan: plans/plots_page_overhaul.md |
| 160 | Mock data validation — single-point parameter recovery test for likelihood scoring | Create a "Validation" tab in the Bias Correction page that tests whether the likelihood scoring pipeline can recover known parameters. Workflow: (1) Set "true" parameters via sliders: f_bin, π (Dsilva only), σ_single, logP_max. (2) Generate mock observations by running the same cadence-aware simulation at the true parameters (full simulation, produces fake 25 ΔRV values). (3) Run a fresh grid search against the mock observations using the exact same pipeline (_run_cadence_bg). Use coarser grid defaults for speed (30 f_bin × 20 π × 500 N_sets). (4) Score the recovery with a weighted normalized distance metric: for each parameter compute abs(best_fit - true) / parameter_range (0=perfect, 1=worst), weight by number of grid points (denser grid = more trust), compute weighted mean as overall recovery score. (5) Display: big recovery score metric, per-parameter breakdown table (true vs recovered, distance, weight), likelihood heatmap with star=true + circle=recovered marked, CDF comparison (mock obs vs best-fit model). Support both Dsilva and Langer models. Include seed input for reproducibility. New files: app/bc/validation.py (~500 lines) and app/bc/render_validation.py (~300 lines). Wire into tab system via bc/__init__.py and 05_bias_correction.py. Reuse: _run_cadence_bg(), _single_grid_task_cadence_aware(), analysis._method_best_and_hdi(), make_heatmap_fig(), render_likelihood_cdf(). | high | bias-correction, research, validation | open | Guy | Guy | 2026-03-23 | N | Y |  |
| 165 | Review simulation parameters — grid ranges, resolution, N_sets | Go through all simulation parameters for both Dsilva and Langer cadence runs. Verify grid ranges are physically reasonable, resolution is sufficient, and N_sets provides convergence. | high | bias-correction, research | open | Guy | Guy | 2026-03-30T19:00:00 | N | Y | User task for 2026-03-31 |
| 170 | Fix IndexError in likelihood explanation when bins > 4 | `example_p` in `render_lk_fit.py:745` was hardcoded to 4 elements but `n_obs` can have 5+ bins. Replaced with `np.geomspace(0.60, 0.05, len(n_obs))`. | medium | bias-correction, bug | to-test | Claude | Guy | 2026-03-31T20:05:00 | N | Y | One-line fix, error-check passed. |
| 171 | Add L bins column to file browser result table | File browser in `file_ops.py` now shows the `likelihood_bin_edges` stored in each .npz file as a compact string (e.g., `[0, 46, 250, 650, ∞]`). | low | bias-correction, feature | to-test | Claude | Guy | 2026-03-31T20:19:00 | N | Y | Reads from existing stored data. |
| 167 | Model comparison: Dsilva vs Langer — which period model fits observations better? | Develop methodology to compare Dsilva (power-law period) vs Langer (Case A/B mixture period) models. Options: AIC/BIC comparison, Bayes factor, cross-validation, likelihood ratio test. Determine which model is more likely to represent reality. | high | bias-correction, research, statistics | open | Guy | Guy | 2026-03-30T19:00:00 | N | Y | User task for 2026-03-31. Key thesis question. |
| 169 | Autosave for bias correction simulations | Automatic periodic checkpointing every 120 seconds during cadence simulation runs (both Dsilva and Langer). Reuses existing _save_partial_cadence() and partial file format. Subsequent autosaves overwrite same file. | medium | bias-correction, feature | to-test | Claude | Guy | 2026-03-30T19:25:00 | N | Y | 6 lines in runners_cadence.py. Visual indicator not yet added. |
| 161 | Mock data validation — batch sweep across parameter space to map recovery quality | Extend the Validation tab (#160) with a batch mode that tests parameter recovery at multiple points spread across the parameter space. Goal: identify where in (f_bin, π, σ_single) space the bias correction pipeline recovers the true model well vs poorly. (1) Define a coarse grid of "true" parameter sets (e.g. 5 f_bin × 5 π × 3 σ = 75 test points). (2) For each true point, run the single-point recovery test from #160 (generate mock obs → grid search → score). (3) Collect all recovery scores into a results table indexed by (true_f_bin, true_π, true_σ). (4) Visualize as a "recovery quality heatmap" — 2D heatmap (f_bin vs π, marginalized over σ) colored by recovery score: green=good, red=poor. Shows at a glance where the pipeline is reliable. (5) Interpret for thesis: ideally recovery is uniformly good everywhere; if poor in some regions, check whether the real observations' best-fit lands in a reliable region. Computationally expensive (75 × full grid), so support: resumable checkpoints, progress bar (X/75 complete), option for sparser sweep first (3×3×1=9 points). Build on compute_recovery_score() from #160. Store batch results in .npz. Depends on #160. | high | bias-correction, research, validation | open | Guy | Guy | 2026-03-23 | N | Y | Depends on #160 |
| 187 | vlidation tab in bias correction is mambo jambo | after running it i found several VERY disturbing things. mock data: f_bin: 0.85, pi: 0.9, sigma_single: 5 and LogP_max: 4. 1) the last result shows the CDF comparison at the top of the page ok for the mock observation (btw change to mock observation in the validation tab), but the best simulated score gives a very flat graph, totally not a CDF and not the best CDF. 2) while the top most heatmap and 1D graph of max norm lilelihood of logP_max, shows that the best logP_max recovered is 6 and f_bin is 1 whith pi= 0.45, the summary table right below says f_bin = 0.9697 and the %68 HDI 0.9899 (both not 1), best pi to be = 2.333 with 68% HDI 1.727 (both not 0.45) and best LogP_max as nan (bug). thesse unconsistant results are not torelable and everything must be consistent with no bugs. 3) in the corener plots, where the margenalized plots show the errors as 68% of area under the curve, the errors shown for the HDI in the summary table at the top has 0 match, thats another inconsistensy I cannot tolerate. find which si correct and fix the other one. 4) setting the real mock data parameters in the model explorer results in a better looking CDF with a higher score. how could that be? the best model grid points should have been the higest score since the algorithm when through what i set manually and gave it a lower score.  5) the CDF sanity check graph shows all calculated CDFs really ubnder the mock data one, making it seem like the sanity check failed and the results makes no sense. 6) in the model explorer the data is truncated where the mock data RVs end, let it continue for other CDFs, I didnt tell you to truncate it | critical | bias-correction, bug, webapp, validation, testing, reliability, graphs | to-test | Guy | Guy | 2026-04-23T18:01:52 | Y | Y | Stages A–D + cleanup completed in 2026-04-23 21:38 sprint. (1) best-fit CDF reads stored `best_median_cdf` instead of re-simulating; "Observed" → "Mock Observation" via `_obs_label(result)` across 15+ files. (2) all 4 argmax params stored (`argmax_fbin/pi/sigma/logPmax`); `logP_max=NaN` bug fixed; marginal mode (`mode_*`) stripped from result dict and every reader; summary column renamed "Joint argmax". (3) corner-plot vlines now read joint argmax (matches summary table); honest-labels rule codified in `memory/feedback_honest_labels.md`. (4) grid `n_sets`+`grid_seed` stored and re-used by Explorer (no more seed/n_sets drift); regression test `scripts/test_grid_vs_explorer_score.py` shows |ΔlogL|=0.103 within 5.0 tolerance. (5) CDF Sanity Check rewritten — N=500 per draw, 16/84 percentile band from 50 draws, mock in black inside band. (6) Explorer x-axis extended across all traces (no more truncation at mock RV end). Bonus: A&A theming sweep added `_AA_OVERRIDES['title.font.color']` + applied to 7 plots that skipped it; mock_results/ persistence (`app/bc/validation_io.py` 567 lines, save/load/scan/promote mirroring cadence); error-distribution chooser ported into mock UI (mock noise drawn from chosen distribution via `_draw_measurement_noise`, byte-identical mock vs Explorer CDF verified by `scripts/test_explorer_mock_equal*.py`); pre-existing `xv` undefined bug at `render_lk_explorer_langer.py:230` patched. UNCOMMITTED — awaiting Guy's visual sign-off on (a) every validation-tab plot is white-bg A&A, (b) summary-table values match heatmap argmax + logP_max not NaN, (c) mock and Explorer CDFs overlay byte-for-byte at matching slider+seed, (d) saved validation run appears under mock_results/ and reloads, (e) home button on binary-fraction plot zooms out to full data range. Three white-on-white traces inside WORKING-flagged blocks (`render_lk_explorer.py:973,995`, `render_lk_explorer_langer.py:284`) deferred pending explicit user authorization. |
| 188 | RV modeling app: histogram fix + parametric summary + measurement-error model port | (1) Diagnosed sparse/spiky orbital-parameter 3×3 histogram on Simulate Binary RVs tab — `compute_physics_diagnostics` was being called with `n_sets=1` (~25 binaries) while the main RV generator uses `n_sim=100 000`. Fixed via `n_sets_hist = ceil(n_sim / n_per_set)` so histogram population matches main sim. (2) Added best-fit Parameter Summary for Parametric mode on Model Fitting tab: distribution names + per-parameter labels (from `_PARAM_META`) for both single and binary, plus N_epochs and Seed. Physics mode unchanged. (3) Ported single-star and binary-star measurement-error models from bias-correction webapp into Model Fitting tab Parametric mode: added `render_error_model_pair` call after distribution selectors; extended `compute_model_fraction_curve` with 6 new error kwargs (safe defaults); implemented per-star Bernoulli assignment + per-class intrinsic draws (singles: one tiled draw, binaries: per-epoch draws) + independent per-epoch noise via `_draw_measurement_noise`; implemented full 2-test detection on simulated ΔRV — `(dRV > t) AND (dRV − 4·σ_pair > 0)` with σ_pair = √2·σ_measure per class — matching observed-curve construction at `page.py:49-59`. Singles get ONE intrinsic draw per star tiled across epochs (behavior change, scientist-approved: single stars have no intrinsic epoch-to-epoch variation, ΔRV from noise alone). New kwargs default to `fixed / () / 0.0` so existing callers unaffected. QA + scientist fact-checks ✓ on all 7 core scientific points. | high | rv-modeling, feature | to-test | Claude | Guy | 2026-04-23T14:05:00 | N | Y | UNCOMMITTED — awaiting visual sign-off. Files: `rv_modeling_app/rv_modeling/tab_simulation.py:748-754`, `tab_fitting.py:227-258,308-316,356-394,395-408`, `compute.py:298-360`. Open caveat: for non-Gaussian noise distributions (gamma, weibull, lognormal, exponential, uniform), `sigma_measure` ≠ actual 1σ of `_draw_measurement_noise` output (code applies `abs(X)` then random sign), so σ_pair=√2·σ_measure and the 4σ criterion are miscalibrated. Exact for `fixed`/`normal`. User to decide: (a) accept caveat + stick to `fixed`/`normal`, or (b) compute σ_eff=√E[X²] per distribution. Three pre-existing unused imports flagged by pyflakes in `tab_fitting.py` (scipy.stats, scipy.optimize.minimize, shared_lite.COLOR_SINGLE) — not introduced today, cleanup candidate. |
| 189 | Standalone To-Do webapp: launch help + green-code-span + table-pipe-corruption fixes | Standalone To-Do webapp at `todo_app.py` (entry shares all logic with embedded page via `app/todo_core.py`). (1) Documented launch command: `conda run -n guyenv streamlit run todo_app.py --server.port 8502`; explained zsh `!`-history-expansion gotcha for the Hebrew `תואר שני!/` path (use single quotes or `cd` first). (2) Bug: descriptions rendered with bright green `<code>` spans (unreadable) because user-authored descriptions contain many backticks. Fix: HTML-escape the description and strip backticks before passing to `st.markdown` in `col_title` rendering. (3) Bug: task #185 showed priority as literal `"none"` plus description text bleeding into the narrow priority column. Root cause: description contained `int \ | none` — the embedded `|` broke naive `line.split('|')[1:-1]` parser, shifting all subsequent columns. Fix: hardened `_parse_table_rows` with regex `re.compile(r'(?<!\\)\|')` (split on `|` only when NOT preceded by `\`), then unescapes `\|` → `|` per cell; added `_escape_cell()` helper in `save_todos` to pre-escape every string cell across Open/Done/Deleted tables; surgically repaired the corrupted row in `TODO.md` (`int \ | none` → `int or None`, only row #185). /error-check all phases PASS — pyflakes, imports, parse test on real `TODO.md` parses 61/105/4 tasks, task #185 priority now correctly parses as `high`. Smoke test on port 8504 returned HTTP 200. | high | todo, webapp, bug | to-test | Claude | Guy | 2026-04-23T17:27:00 | N | Y | UNCOMMITTED — awaiting Guy's visual sign-off on rendered To-Do app (green code-spans gone + task #185 lays out normally with `high` priority). Scoped to To-Do app only — `app/todo_core.py` has no importers outside `todo_app.py` and `app/pages/10_todo.py`; bias-correction page untouched. Files: `app/todo_core.py` (added `import html`, `_CELL_SPLIT` regex + `_escape_cell` helper, pipe-aware parser, pipe-escaping writer, HTML-escape + backtick-strip on description render), `TODO.md` (one substring replace in row #185). |
| 195 | Likelihood Model Explorer ↔ grid score parity: deterministic n_bin, cached re-sim, CDF extension | Diagnosed both score paths side-by-side: Explorer's `_me_cdf_band` (validation_mode → `_sample_delta_rv_mock` with mock seed and forced `n_sets=1`; real-obs → `simulate_delta_rv_cadence_aware` with hardcoded `default_rng(42)`) vs grid worker `_single_grid_task_cadence_aware` (`default_rng(seed_base + idx_cell)`, `n_sets`, multinomial against `likelihood_bin_edges`). Confirmed CDF visualization bins (`bin_edges`, default 36-edge `np.arange(0, 360, 10)` or adaptive) are completely independent of likelihood scoring bins (`likelihood_bin_edges`, default Dsilva 4-bin `[0, 45.5, 250, 650, ∞]`). Three coder-agent batches: (1) forced deterministic `n_bin = round(N × f_bin)` in both `simulate_delta_rv_sample:715` and `simulate_delta_rv_cadence_aware:930`; added `_explorer_run_grid_pipeline` helper; gated Explorer score+CDF behind a 🔬 Run button; dropped `validation_mode` from `_me_cdf_band`; applied to both Dsilva and Langer twins. (2) Reversed direction (auto-update is fast enough): removed Run button; added cached wrapper `_explorer_run_grid_pipeline_cached` keyed on (f_bin, π, σ, logP_max, n_sets, seed_base+idx_cell, hashable cfg); promoted `_n_sets_me` to a user-editable `st.number_input` (default = `result.get('grid_n_sets', 1000)`, min=1, no max, step=100, persisted to `lk_explorer.n_sets` / `lk_explorer_langer.n_sets`); auto-recompute on every slider/number-input change with "Computing logL…" spinner only on cache miss. Global best metric still reads directly from `logL_raw[i_sig_best, i_fb_best, i_pi_best]`. (3) CDF truncation: appended `(pooled_max, 1.0)` final point to Explorer (current) and Best-fit (algorithm) traces in both files; removed `_x_candidates` block + hardcoded `xaxis_range=[0, _xmax_cdf*1.05]` so plotly autofocuses from data extents. Diagnosed mock binary count: verified deterministic code at `app/bc/validation.py:222` produces exactly `int(round(25 × 0.70)) = 18` across all seeds. Diagnosed banded 2D heatmap: per-set `n_bin = round(25 × f_bin)` is a step function of f_bin; n_sets averaging is so effective via LLN that the only structure left across nearby f_bin cells is the integer quantisation, producing horizontal bands ~0.04 wide. n_sets is NOT being ignored — it's working too well. | high | bias-correction, model-explorer, parity, bug | to-test | Claude | Guy | 2026-04-30T01:17:00 | N | Y | UNCOMMITTED — awaiting Guy's visual sign-off after rerun. Files: `wr_bias_simulation.py` (deterministic n_bin in two simulators), `app/bc/render_lk_explorer.py` (cached wrapper, n_sets number_input, CDF extension, removed hardcoded x-range), `app/bc/render_lk_explorer_langer.py` (Langer twin). Pending decision: switch from per-set deterministic n_bin to **aggregate-deterministic** floor/ceil distribution (`total_n_bin = round(n_sets × 25 × f_bin)` distributed across sets as floor or ceil) to restore smooth f_bin axis on heatmaps without losing exact-aggregate-fraction guarantee. |
| 194 | Three-stage binarity strategy memo + 5 validation runs queued | Captured Guy's proposed three-stage workflow as a project memory: Stage 1 = bias correction with no f_bin/threshold assumed, finds best (π, logP_max[, σ_single]); Stage 2 = use winning model to draw many systems (cadence-free) and fit a parametric distribution to binary RVs; Stage 3 = plug that distribution into the RV Modeling tool to solve for observed f_bin, threshold, and σ_single. Logged the open error-propagation question: passing a single best-fit point loses corner-plot uncertainty; two candidate framings (sample marginals → distribution-of-distributions, or track how Stage-2 PDF parameters scatter across Stage-1 posterior draws). Captured five validation simulations launched evening 2026-04-30 for next-day review: Tabs 1–2 (low/high f_bin extremes with σ_single pinned to truth), Tabs 3–4 (sensitivity to an extra histogram bin near the steep CDF slope), Tab 5 on port 8502 (σ_single included as a free search dimension). Linked the σ_single-search run (Tab 5 on :8502) to the three-stage memo as the decisive test for whether σ_single can live in Stage 1 or must be deferred to Stage 3. | high | bias-correction, planning, paper, validation | open | Claude | Guy | 2026-04-30T01:16:00 | N | Y | Memory-only artifact (no code edits). Created `memory/project_three_stage_binarity_strategy.md` and `memory/pending_validation_runs_2026_04_30.md`; both indexed in `MEMORY.md` Live State. Decisions: treat the three-stage chain as the target architecture for the paper's conclusion; defer σ_single-staging until Tab 5 results land; error-propagation framing left open — flagged for raising. Open: Tabs 1–2 recovery at f_bin extremes, Tabs 3–4 bin-placement sensitivity, Tab 5 σ_single recovery. |
| 193 | Paper writing session — figures inserted/rendered, table populated, LaTeX bugs, Shenar-style polish | (1) Inserted 13 figure environments across 5 section files; 11 PDFs rendered to `plots/` from real data via `pipeline/export_paper_figs.py` (cdf_obs_vs_sim, fbin_pi_heatmap, fbin_pi_marginals, langer_heatmap, peak_drv_per_star, agreement, bin_sensitivity, period_models, sample_map, ccf_profile, binary_examples — all A&A-styled, 1664-line export script). (2) Populated `paper/tables/star_sample.tex`: 25 stars × (Name, BAT99, RA, Dec, Group, Subtype, N_epochs, Source) + 4 footnotes (Brey 70a WN/WC transition, HD 269891 WN+B, RMC 140 WN+WC composite, Brey 83 WC5→WC4 reclassification). Resolved H2013 LMCe 584 ≡ L72 LH 41-1042 via specs.py + FITS-header verification (1.5″ pointing offset). Per-row epoch counts now reflect actual data (5/6/7 split: 3 + 15 + 7 = 25). BAT99 + spectral types looked up from Breysacher+1999 PDF (VizieR returning PostgreSQL errors); Foellmi+2003 + Schnurr+2008 added to `references.bib`. (3) Fixed 8 distinct LaTeX bugs causing "small + glued + no spaces" rendering: math-mode contamination in unit macros (`\kms`, `\Msun`, etc.), `\fbincorr^{...}_{...}` atom-binding malformations, `\tablefoot{}` without `\par` separators, and silent: `tables/star_sample.tex` was never `\input`'d. (4) Applied 7 Shenar-style fixes: deleted dead `abstract.tex`, populated empty Conclusions block, narrative Conclusions paragraphs (no enumerate), `\tablefoot{}` on `tab:emission_lines`, populated appendix placeholder, ESO programmes via `\esoProgrammes` macro. Methods section grew from ~150 to ~310 lines: added §Choice of primary RV line, §Line-agreement cross-check, §ΔRV threshold derivation, refined §Threshold-curve fit, §Spectroscopic morphology investigation. Discussion + Conclusions fully drafted (forward-looking, macro-driven, awaiting Phase-4 number lockdown). 7 commits to local main + 7 pushes to Overleaf (latest: 0a5a359). | high | paper, latex, figures | to-test | Claude | Guy | 2026-04-29T14:00:00 | N | Y | COMMITTED to main (5 commits) + 7 Overleaf pushes. Live state in `memory/current_focus.md`. New feedback rules saved: `feedback_writer_owns_tex_edits.md` (writer owns full .tex edit cycle), `feedback_no_todos_in_paper.md` (zero `% TODO` comments in paper LaTeX). Open: 17 TBD macros pending Phase 4 number lockdown; `binary_classification.tex` still empty; Acknowledgements `[funding source]` placeholder; sample-purity decision (HD 269891 + Brey 70a); user produces `fig:threshold_derivation` themselves with folded-normal model; Phase 5 final compile + ADS bibcode verification. |
| 192 | Validation tab mock-data overhaul: 2-criteria detection, dual error models, ΔRV error bars, deterministic count, persistence | Extended `_sample_delta_rv_mock` + `generate_mock_observations_detail` with optional `sigma_meas_binary`, `error_model_binary`, `error_params_binary` (defaults `None` → fall back to single side's values; preserves byte-identical RNG for Explorer's CDF sanity-check). Replaced single error-model selector with two-column Singles \| Binaries layout (mirrors Dsilva/Langer cadence tab pattern). Added horizontal ΔRV error bars on every dot in CDF + binary-fraction panels: σ_ΔRV = √(σ_min² + σ_max²) using per-epoch errors at min/max RV epochs (same formula as `02_spectrum.py`). Detection curves now apply BOTH criteria: `(ΔRV > t)` AND `(ΔRV − 4σ > 0)`; legend renamed to "Detected f_bin(threshold) — 2 criteria", "Missed binaries (failing 2-criteria)", "Single false positives". Star table gained `σ_ΔRV (km/s)` and `Detected (2-crit)` columns; coloured cells for "Yes". Reverted "Realised f_bin" from entire page after user pushback (caption mention, dotted horizontal line on binary-fraction panel, Panel A red dashed line, legend `(N)` count suffix); user: "drop it entirely... I talked about the entire page". Critical bug fix: mock binary count was Bernoulli-drawn (`rng.random(N) < f_bin`), so seed=42 + f_bin=0.7 + N=25 gave 13 binaries (2σ-low) instead of expected 17–18; switched to deterministic `n_bin = int(round(N × f_bin))` with `rng.choice` picking which stars are binary. Verified across 8 fbin values × 5 seeds: count is now exact. True Parameter sliders (f_bin, pi, sigma_single, logP_max) and Random seed now persist to disk via `SettingsManager.save([section, val_*])` on every change; read defaults from `grid_cadence_dsilva` / `grid_cadence_langer` config on load (independent per period model). | high | bias-correction, validation, ux, bug | to-test | Claude | Guy | 2026-04-29T13:58:00 | N | Y | UNCOMMITTED — awaiting Guy's visual sign-off. Files: `app/bc/validation.py` (function signatures + deterministic binary count), `app/bc/render_validation.py` (two error-model selectors, error bars, 2-criteria curves, removed all realised f_bin references, persistence wiring). New feedback memories saved: `feedback_mock_deterministic.md` (deterministic mock counts, validation page convention) and `feedback_ui_persist_on_change.md` (all UI controls must persist to disk on change). |
| 191 | Validation tab Sprint 4 Phases 5–8: regression diagnostics, 5-CDF visual consistency, truth-coded dots, Panel B alignment | Diagnosed and fixed user-reported regressions from end-of-day 2026-04-23 Sprint 4 work. Phase 5 (5 bugs): (1c) best-fit CDF flat at 0.12 — root cause was `render_shared.py:191` blindly squeezing leading axis (`p_arr[0]`) instead of size-1 axes → `best_vals['logPmax']=NaN` → fall-through to broken back-compat sim path with default `BinaryParameterConfig`; fixed by mirroring the cadence_langer pattern. (1e) Sanity Check `Unknown period_model: dsilva` — caller passed runner-mode tag `'dsilva'` as `period_model`, but `sample_logP` accepts only `'powerlaw'`/`'langer2020'`; translation fix in 3 callers (`render_lk_explorer.py:1357`, `render_lk.py:549`, `render_lk_explorer_langer.py:1062`). (1b) `argmax_logPmax` NaN — same root cause as 1c; squeeze fix cured it. (1b) HDI: per user instruction "show the maximum point in the marginalized graphs", added new "Marginal max" column; updated `feedback_honest_labels.md` to allow marginal max if labelled honestly. (1a) A&A theme: shared.make_heatmap_fig root edit fixed ~10 heatmaps; user authorised WORKING-flag edits inside Model Explorer (D17), corner plots (D14), parabola fits (D9). Phase 6: flagged diagnostic table as `# WORKING` ("perfect, mark it as working"); removed false bootstrap bands from mock-preview CDF + binary-fraction-vs-threshold ("feels like madeup useless confusing unrelated false data"); added single-source-of-truth CDF style constants (`_CDF_OBS_COLOR`, `_CDF_FIT_COLOR`, `_CDF_OBS_MARKER`); standardized 5 CDFs (mock-preview, best-fit comparison, sanity check, re-sim interp, model explorer) on teal step + dashed red sim. Phase 7 (after pushback "they are certainly not visually consistent"): added `validation_io.load_per_star_truth(result)` helper; truth-coded red/green per-star dot overlays on CDFs 2–5 (Dsilva) + 3 Langer twins matching the first CDF's pattern; replaced 14+ remaining hardcoded colors with constants. Phase 8 (after Panel B "way better than the rest"): aligned `_render_panel_b_cdf` colors to teal/red constants; added `shape='hv'` to sim trace + band fill + residual; explained that Panel B's bootstrap band IS computed differently (sampling uncertainty at N=25 vs grid sim variance) — kept methodology, only changed appearance. Round-5/6: standardized 9-panel CDF cross-method consistency with multi-method legend convention (mock = black solid step, grid-argmax best-fit = red `#D62728` dashed median + 16/84 shadow + faint draws, marginal-posterior best-fit NEW overlay = purple `#9467BD` dashed median + 16/84 shadow + faint draws); fixed dot-positioning bug (obs step was drawn from `_binned_cdf` while dots used raw star ΔRV — now both built from same sorted array). | critical | bias-correction, validation, plots, bug | to-test | Claude | Guy | 2026-04-28T10:28:00 | Y | Y | UNCOMMITTED — Sprint 4 still on `main`. Did NOT touch the three deferred white-on-white traces (`render_lk_explorer.py:973, :995`, `_langer.py:284`) per earlier instruction. Files modified: `app/bc/analysis.py`, `app/bc/render_shared.py`, `app/bc/render_validation.py`, `app/bc/render_lk_explorer.py`, `app/bc/render_lk_explorer_langer.py`, `app/bc/render_lk.py`, `app/bc/render_lk_fit.py` + `_langer.py`, `app/bc/validation_io.py`, `app/bc/validation.py`, `app/bc/runners_cadence.py`, `app/bc/cadence.py`, `app/bc/corner_plots.py`, `app/bc/likelihood_viz.py`, `app/shared.py`, `app/bc/render_shared_langer.py`. Test scripts all PASS. New: `memory/feedback_squeeze_axis.md` (never blindly drop a leading axis; squeeze ONLY size-1 axes); MEMORY.md critical rule + honest-labels relaxation. Pending visual sign-off across all 9 CDF panels in both Dsilva and Langer modes; user must visually verify Panel B step shape is now visible (t_grid=400 might still look smooth even with shape='hv'; coder flagged 50-point fallback if needed). |
| 190 | Bin Sensitivity sub-tab: revived save/load + autosave + persistence + plot polish + E050 deadlock fix | Revived prior chat's work on Bin Sensitivity save/load. Storage layer (`bin_sensitivity_storage.py`) was already done; tab-side UI was half-wired (two broken call sites: `_maybe_autosave_partial` undefined, `_render_results` signature mismatch). Final implementation mirrors cadence `_render_partial_table` / `_scan_partial_metadata` / `_scan_result_metadata` from `file_ops.py` verbatim: added `list_bs_partials()` to storage + `_scan_bs_partial_metadata` + `_scan_bs_result_metadata` + `_render_bs_partial_table` + `_render_bs_results_table` + shared `_hydrate_loaded_bs_run`. Partial/final separation, expander for partials, Load/Delete/Resume buttons matching cadence icons. Saved-runs UI moved out of `_render_results` (which only fires on active completed job) into `_render_bs_saved_runs_panel` called unconditionally at top of tab — so user can load prior partials after a browser refresh. Schemes-list persistence to `user_settings.json` under `bin_sensitivity.schemes` (unconditional `_persist_schemes(rows)` on every render of `_render_schemes_list`); mock-truth params persisted under `bin_sensitivity.mock_params`. Removed silent `except: pass` from persistence helpers — errors now surface via `st.toast(..., icon='⚠️')` + `print()` fallback. Added first-call-per-session debug `print('[bsn] _persist_schemes first call, N rows')` for terminal verification. **E050 fix:** diagnosed run hang — `_run_all_schemes_bg` was calling `rescore_scheme_cached` (`@st.cache_data`-decorated) from a `threading.Thread`; Streamlit's cache deadlocked on internal locks without a ScriptRunContext. Bypassed by calling uncached `rescore_scheme` directly, passing already-loaded `ctx` to avoid re-opening the .npz per scheme. Plot improvements: CDF Overlay shows two-line subplot titles with best-fit + HDI68 per scheme (Option A, U+2212 minus); Posterior Shapes shades 68% HDI region per scheme with matching legendgroup (line+shadow toggle together); all 6 plots got readable-text bump (ticks ≥12pt, titles ≥14pt, legend ≥14pt for 3+ subplots). E049 fix: `_apply_aa_axes(fig)` helper standardizes unscoped `update_xaxes`/`update_yaxes` (was: scoping bug caused subplot inconsistency between left/right columns). | high | bias-correction, bin-sensitivity, plots, bug | to-test | Claude | Guy | 2026-04-23T19:06:00 | N | Y | UNCOMMITTED — awaiting Guy's verification after next app launch: (1) 3 existing `.partial_*.npz` in `bin_sensitivity_results/` should appear in top-of-tab Saved runs expander; Load → render, Save result → promote. (2) Custom schemes should survive a hard browser refresh and the `[bsn] _persist_schemes` debug line should appear in streamlit terminal on first tab interaction. If schemes still don't stick after the bulletproof rewrite, the toast error will reveal the underlying exception. Files: `app/bc/bin_sensitivity.py` (792 → ~1270 lines), `app/bc/bin_sensitivity_storage.py` (added `list_bs_partials()` + self-test step), `app/bc/bin_sensitivity_scorer.py` (non-mock branch in `_run_all_schemes_bg` bypasses cache; NOTE comment on `rescore_scheme_cached`), `app/bc/bin_sensitivity_plots.py` (`_apply_readable_text()` helper; CDF subplot title annotations; HDI68 shaded bands; `_apply_aa_axes()` for E049). New: `memory/plot_preferences.md §2026-04-23` (readable text size minimums); `.claude/references/learnings.md` (3 new User Interaction rules); `COMMON_ERRORS.md` E050. |
| 196 | logP_max scan: fixed-value input box for the OFF state | Added a `logP_max (fixed)` `st.number_input` to `_render_logPmax_scan` (`app/bc/params.py`) that appears when the scan toggle is OFF. Persists under `[sec, 'logPmax_fixed']` via `sm.save` on change; default seeds from existing `f'{p}_logP_max'` session state on first render so the new widget initialises to whatever the orbital section already has. Modified Dsilva and Langer cadence tabs (`app/bc/cadence.py`) to rebuild `_bin_cfg = dataclasses.replace(_bin_cfg, logP_max=float(_*_logPmax_vals[0]))` after the scan expander returns, so the typed fixed value flows into the cadence-aware simulation. Did NOT touch `runners_cadence.py` (marked `# WORKING — do not change`). New widget key (`f'{p}_logPmax_fixed'`) instead of reusing the orbital section's `f'{p}_logP_max'` because Streamlit forbids duplicate keys; decoupling lets the scan expander act as the single source of truth for the simulation when the scan is OFF without sync gymnastics. /error-check returned PASS (5-phase, including project render test). User confirmed with "nice." | medium | bias-correction, ui, validation | to-test | Claude | Guy | 2026-04-30T16:31:00 | N | Y | UNCOMMITTED — awaiting visual sign-off when user next opens the bias-correction page. Files: `app/bc/params.py` (`_render_logPmax_scan` now seeds + renders the fixed-value input + caption + new return when scan OFF), `app/bc/cadence.py` (added `import dataclasses`, two `dataclasses.replace` calls — one per tab — to override `_bin_cfg.logP_max` after the scan expander). Coder agent did the code edits per `feedback_coder_does_edits.md`. |
| 197 | Single-star RV / σ / ΔRV peak-to-peak inspection plot | Built `scripts/plot_single_star_rvs.py` — loads observed RVs via `pipeline.load_observations.load_observed_delta_rvs(settings, ObservationManager())`, filters `is_binary is False & N≥2`, sorts by σ ascending, plots per-epoch RV scatter with error bars + ⟨RV⟩ tick + σ-diamonds blended onto the *left* RV axis (same units as RVs). Marks the lowest σ (Brey 90a, 1.02 ± 0.68) and largest σ (Brey 83, 11.27 ± 1.59). Implemented `std_uncertainty(rv, rv_err)` propagating per-epoch RV errors through the sample std via the closed-form Gaussian formula `err(s) = √(Σ d_i² σ_i²) / (s (N−1))` (derived from `∂s/∂x_i = (x_i − x̄) / [s (N−1)]`). Computed an inverse-variance weighted mean σ across the 15 single stars: **4.49 ± 0.17 km/s** (vs. unweighted 5.19 ± 0.69 km/s, population scatter 2.67 km/s). Flagged proactively to user that the propagated 0.17 km/s reflects only measurement uncertainty, not the much larger intrinsic σ-spread; the weighted-mean error is therefore optimistic if interpreted as "uncertainty on a typical single-star σ". Repurposed the right axis for ΔRV peak-to-peak triangles with `err(ΔRV_pp) = √(err_max² + err_min²)`. Right axis recoloured violet to visually pair with the violet ΔRV-pp triangles. Output: `plots/single_star_rvs.html` (interactive). PNG export skipped — `kaleido` not installed in `guyenv`. | medium | rv-analysis, plots, single-star | to-test | Claude | Guy | 2026-04-30T16:32:00 | N | Y | User iteratively verified the layout (two rounds: error bars + dashed ⟨σ⟩ band; then σ blended into left axis with ΔRV-pp on right). σ_intrinsic vs σ_measured caveat (4.49 ± 0.17 km/s weighted-mean candidate σ_single estimate vs 2.67 km/s star-to-star scatter) flagged for next bias-correction iteration. PNG export pending `kaleido` install if user wants paper-ready figures. Files: `scripts/plot_single_star_rvs.py`, `plots/single_star_rvs.html`. |
| 198 | Bias-correction CDF Comparison panel enrichment: per-star truth dots, per-rank gradient markers, mean line, marker-shape legend | Designed and executed enrichment of the "CDF Comparison: Observed vs Best-Fit Models" panel in both Dsilva (`render_shared.py`) and Langer (`render_shared_langer.py`) live renderers. Five chunks per the approved plan at `~/.claude/plans/in-the-bias-correction-purring-swing-agent-aefbb60d3bb5f0283.md`: (1) thread 4 new `best_*` arrays through `wr_bias_simulation.py:simulate_delta_rv_cadence_aware` → `_single_grid_task_cadence_aware` (11-tuple) → `_process_result` → `run_bias_grid_cadence_aware` and persist into runner result via `runners_cadence.py`. (2) Define `CDFBandResult` NamedTuple in `render_lk_explorer.py` and Langer twin so `_me_cdf_band` / `_me_cdf_band_langer` return the new median+mean+per-rank arrays alongside the legacy CDF band. (3-5) In both `render_shared.py` and `render_shared_langer.py`: render per-star truth dots on the BLACK observed CDF using `validation_io.load_per_star_truth(result)` for validation runs (red=single, green=binary; circles), or via `obs_detail['is_binary']` for real-obs runs; render per-rank median markers (squares) and mean markers (triangle-up) on each method's best-fit step line, snapped to step y via `np.searchsorted(step_x, dot_x, side='right') - 1`; color-encode markers by per-rank MC binary fraction via Plotly native `marker.colorscale=[[0, _CLR_SINGLE], [1, _CLR_BINARY]]`; emit a single colorbar (`MC binary fraction`, ticks 0% / 50% / 100%) on the FIRST rank-marker trace via a tracking flag dict; append three phantom legend traces (`Observation (circle)`, `Median rank (square)`, `Mean rank (triangle)`); extend caption to describe the red→green gradient and shape convention. Diagnostic step: replaced silent `except: pass` in chunk-level wrapper and chunk-3 fallback with `st.error(traceback)` / `st.warning(traceback)` so the actual exception surfaces in the UI during dev — to be reverted to silent only after visual approval per the existing "Silent except is a trap" learning. Round 3 surface-bug: `'numpy.ndarray' object has no attribute 'get'` from `app/shared.py:settings_hash` — `result.get('settings')` round-tripped through `np.savez` came back as a 0-d numpy object array (variant of E043; learning extended in `.claude/references/learnings.md`); fix detects `isinstance(_settings_raw, np.ndarray)` first and calls `.item()` then guards with `isinstance(_, dict)` before falling back to settings manager (mirrored in Langer file). | high | bias-correction, plots, cdf, validation | to-test | Claude | Guy | 2026-05-04T23:38:00 | N | Y | UNCOMMITTED — user explicitly deferred visual sign-off to next session ("its not perfect, we will continue tomorrow"). Files: `wr_bias_simulation.py`, `app/bc/runners_cadence.py`, `app/bc/render_lk_explorer.py`, `app/bc/render_lk_explorer_langer.py`, `app/bc/render_shared.py`, `app/bc/render_shared_langer.py`. Pending visual checks (next session): (a) red/green colored circles appear on the BLACK observed CDF for per-star binary classification, (b) squares ride dashed median lines exactly, (c) triangles ride dotted mean lines exactly, (d) colorbar reads "MC binary fraction" with red/gray/green at 0%/50%/100%, (e) three phantom legend entries appear at bottom. After visual approval: revert `st.error` and `st.warning` diagnostics back to silent (`pass`). All `/error-check` 5/5 PASS. |
| 199 | Likelihood Model Explorer Reset-to-best fix: joint argmax + slider quantisation bypass | Diagnosed two distinct bugs in `_render_lk_model_explorer()` (`app/bc/render_lk_explorer.py`) causing "Current Explorer logL ≠ Global best logL" after the user clicks Reset-to-best. **Bug A**: slider defaults `def_fb / def_x / def_sig` came from `_method_best_and_hdi(p_nd, [fbin_g, x_g], …)` — a 2D-marginalized argmax that disagrees with the joint argmax of the full N-D `logL_raw` cube. **Bug B**: when σ_single or logP_max wasn't a scanned axis (size-1 grid), Explorer fell back to `sigma_meas` (measurement noise) instead of `sigma_grid[0]` / `bin_cfg.logP_max` (the actual fixed value the grid was built with). **Round 1 fix**: moved the joint-argmax decomposition block from L1295-1325 to BEFORE slider rendering; used `_bf_*` values as slider defaults; fixed σ/lp non-scanned-axis fallbacks; rewrote caption to show joint-argmax + `(fixed)` annotations. All four dimensionality cases handled (4D, 3D σ-only, 4D-degenerate-σ for lp-only, 2D). Verified clean: py_compile OK, pyflakes only pre-existing warnings, `scripts/test_render.py` 5/5 PASS, smoke-tested joint-argmax decomposition on real Case-C grid `(15, 1, 49, 50)`. **Round 2 fix** (after user screenshot showed Current Explorer logL still ~0.04 off Global best despite caption matching): root cause = Streamlit `st.slider` quantises floats to implicit step `(max-min)/100` — see new COMMON_ERRORS entry **E045** (Streamlit slider implicit-step quantisation). Added `_just_reset` detection via `st.session_state['..._last_rc']` tracker, then after sliders render and `_eff_logPmax` resolved, override `me_fb / me_x / me_sig / me_logPmax / _eff_logPmax` with exact `_bf_*` floats (only when `_just_reset` AND `_logL_raw_arr` is non-empty). Subsequent renders pass slider values through untouched (no auto-snap, per user's explicit preference). | high | bias-correction, model-explorer, bug | to-test | Claude | Guy | 2026-05-04T19:09:00 | N | Y | UNCOMMITTED — user reported round-2 fix may not have closed the gap entirely ("i think its still the same") but ended the conversation before providing screenshot. Pending visual confirmation: load a 4D grid, click Reset-to-best, screenshot showing Current Explorer logL == Global best logL exactly. If still mismatched, next theory to check: cadence library precision through save/load round-trip, or whether `seed_base` is actually persisted in user's grid `.npz` (Explorer falls back to 1234 if absent). Files: `app/bc/render_lk_explorer.py` (only). Backup: `Backups/render_lk_explorer.py.bak`. Plan file: `~/.claude/plans/figure-out-how-does-keen-avalanche.md` (3 iterations). |
| 200 | Validation corner plot: green dashed "true input" markers on diagonals + 2D heatmaps | Added the user's hand-picked mock-input parameters (`true_fbin`, `true_pi`, `true_sigma`, `true_logPmax`) to validation corner plots: diagonal 1D marginals get a **green dashed vertical line** (`#16A34A`, dash=1.5px, annotation "True input" top-left so it doesn't collide with the existing red top-right "Joint argmax"); lower-triangle 2D heatmaps get a **green × marker** at `(true_x, true_y)` overlaid on the gold-star joint argmax. Truth values were already plumbed end-to-end (`render_validation.py:427` → `cadence.py:1233` → `runners_cadence.py:64` → `validation_io.py:343-347` saves into result npz with `is_validation=True`), so no upstream changes were needed. **THREE files were patched** because the Likelihood scoring tab has its own duplicated corner-plot implementation that bypasses the shared helper: `app/bc/corner_plots.py:_render_corner_plot` (used by K-S / CvM / weighted-CvM tabs, called from `analysis.py:1018-1019`), `app/bc/render_lk_fit.py:_render_lk_corner_plot` (Dsilva Likelihood tab, called from `render_lk.py:375-376`), and `app/bc/render_lk_fit_langer.py:_render_lk_corner_plot` (Langer Likelihood tab — same duplication). Each file got the same 4-edit pattern: `_add_1d_posterior` gains `true_value=None` + green `add_vline` block; `_add_2d_heatmap` gains `true_x=None, true_y=None` + green × Scatter; `_render_*_corner_plot` gains `_truth_keys` dict + `_is_validation` flag + `_truth_for(axis_name)` helper gated on `result.get('is_validation', False)`; caption gains a conditional "Green dashed line / green × marker: user-chosen input parameter for this mock run." clause only when validation mode is active AND at least one `true_*` value is finite. Updated WORKING-flagged comment banners in `render_lk_fit*.py` to "UPDATED 2026-05-06: added validation truth-line markers — user authorised". Non-validation runs and legacy npz without `true_*` keys silently skip the green elements (np.isfinite guard). `/error-check` 9/9 PASS twice (once per round): pyflakes NO_MATCHES, py_compile OK, signature inspection, smoke tests confirm gating (None/finite/NaN/partial-NaN), `scripts/test_render.py` 5/5 PASS. End-to-end smoke against a real validation .npz (`fbT0.70_piT-3.00_sigT2.0_logPT3.50_seed42_...npz`) rendered Likelihood corner plot to `/tmp/lk_corner_test.html` — found 12× `#16A34A` color and 3× "True input" annotations (one per displayed diagonal). | medium | bias-correction, validation, plots, corner | to-test | Claude | Guy | 2026-05-06T15:43:00 | N | Y | UNCOMMITTED — visual sign-off pending per `feedback_no_self_approve.md`. Files: `app/bc/corner_plots.py`, `app/bc/render_lk_fit.py`, `app/bc/render_lk_fit_langer.py`. Plan: `~/.claude/plans/in-the-bias-correction-quizzical-bear.md`. **Round 2 root cause**: orchestrator initially patched only `corner_plots.py` and missed the duplicate `_render_lk_corner_plot` in `render_lk_fit*.py` — green line never showed on user's Likelihood tab screenshot. QA agent diagnosed via grep + figure inspection that Likelihood tab calls a separate function. Lesson saved (TBD): when adding a feature to a "shared" plot helper, grep for ALL `_render_*_corner_plot` / `_add_1d_posterior` definitions across `app/bc/` because there may be duplicates flagged WORKING. To verify in webapp: open Bias Correction → Validation tab → Likelihood scoring tab → expand corner plot → confirm green dashed vlines on each diagonal panel at the chosen truth values + green × on each 2D heatmap. Also test K-S / CvM / Weighted tabs (same expectation). |
| 201 | π parameter explainer PDF: 6-page derivation of `sample_logP_powerlaw` | Built `scripts/make_powerlaw_explainer.py` — single-script generator that imports the production `sample_logP_powerlaw` and emits a self-contained 6-page PDF via `matplotlib.backends.backend_pdf.PdfPages`. Pages: (1) title + summary tracing slider → `generate_mock_observations` → `simulate_delta_rv_cadence_aware(pi=…)` → `sample_logP` → `sample_logP_powerlaw` → inverse-CDF over `[logP_min, logP_max]` → Kepler RVs → ΔRV → recovery grid; (2) derivation showing where the `π+1` exponent comes from in the inverse-CDF formula; (3) prior PDF curves for π ∈ {−2, −1, −0.5, 0, 1, 2}; (4) CDF curves with inverse-CDF construction at u=0.7 (general + π=−1 panels); (5) validation histograms (200k samples vs analytic, π ∈ {−2, 0, 2}, perfect overlap); (6) period-in-days view (π=0 Öpik vs π=−1 1/log P). A&A-style rcParams (white bg, black text, serif, viridis-like). Output: `docs/sample_logP_powerlaw_explained.pdf`. Hit two matplotlib mathtext gotchas during build, both logged: E052 (`\bigl`/`\bigr` family unsupported by mathtext, crashes at draw time) and E053 (`ax.text(..., wrap=True)` corrupts `$math$` segments — wrapping inserts newlines mid-formula). Caught a scientific misstatement during page-6 inspection: π=−1 caption originally said "uniform in P" but the prior `p(x) ∝ 1/x` with `x=log P` is uniform in `ln(log P)`, NOT uniform in P; rewrote the caption. New learning added to `.claude/references/learnings.md` Plot Rendering: "always rasterise + visually inspect every page of multi-page PDFs before declaring done" — triggered three times in this session. | low | docs, validation, paper-prep | to-test | Claude | Guy | 2026-05-07T17:34:00 | N | N | UNCOMMITTED. New files: `scripts/make_powerlaw_explainer.py`, `docs/sample_logP_powerlaw_explained.pdf` (`docs/` dir created). Plan file: `~/.claude/plans/in-the-validatio-tab-sorted-wreath.md`. Optional follow-ups offered (slider tooltip, `DOCUMENTATION.md` entry) — not requested. PDF opened in Preview after final inspection — user did not report issues. |
| 202 | Smooth pooled-empirical CDFs across all simulated-star plots | Three-round CDF Comparison panel iteration in `app/bc/render_shared.py`: (a) initial coarse per-rank step CDF + drop mean+triangle markers + height 400→600; (b) restored 16-84 percentile band shadow on the fine ΔRV grid; (c) replaced both line and band with smooth pooled empirical CDF. Final recipe extracted into shared helper `bc.helpers.smooth_pooled_cdf(pooled, n_sets, n_fine=500)` — returns `(sorted_pool, y_pool, x_fine, lo_fine, hi_fine)`. Sorts all `n_sets × n_stars` ΔRV samples for the smooth dashed line; computes per-draw empirical CDFs on a 500-point linspace via `np.searchsorted`, then 16/84 percentile across draws for the smooth band. Applied to **5 plot sites**: `render_shared.py` + `render_shared_langer.py` (mock-vs-best-fit, both period models), `render_lk_fit.py` + `render_lk_fit_langer.py` (Likelihood Best-Fit, both period models), `likelihood_viz.py` (alternate Likelihood Best-Fit panel). Brought `render_shared_langer.py` to parity with the powerlaw twin: dropped mean dotted CDF lines + triangle rank markers + "Mean rank (triangle)" phantom legend, bumped height 400→600, tightened caption. For the Likelihood Best-Fit panels, also switched the **observed** CDF to a true empirical step (`np.sort(obs_drv)`, `shape='hv'`) so it matches the canonical mock-observation black step in the other panels. Updated `memory/plot_preferences.md` — replaced the stale 2026-03-30 rule "all CDFs use shape='hv'" with a split rule (observed = `shape='hv'`, simulated = pooled empirical + smooth band via `smooth_pooled_cdf`). Added 2026-05-07 entry to the Feedback Log. `/error-check` ran after each round; all 5 render tests pass, no new pyflakes warnings. | high | bias-correction, plots, cdf | to-test | Claude | Guy | 2026-05-07T17:43:00 | N | Y | UNCOMMITTED — awaiting visual sign-off on all 5 panels (Dsilva + Langer twins for mock-vs-best-fit and Likelihood Best-Fit, plus alt Likelihood Best-Fit panel in `likelihood_viz.py`). Files: `app/bc/helpers.py` (new `smooth_pooled_cdf`), `app/bc/render_shared.py`, `app/bc/render_shared_langer.py`, `app/bc/render_lk_fit.py`, `app/bc/render_lk_fit_langer.py`, `app/bc/likelihood_viz.py`. Plan file: `~/.claude/plans/i-wanna-improve-thfirst-starry-dusk.md`. Pending follow-ups (next session): (a) Model Explorer CDF in `app/bc/render_lk_explorer.py` still uses coarse visualization bin grid — same fix; (b) "last CDF of Panel B" also still coarse — identify which file/panel. Coder sub-agents repeatedly stalled in plan-mode despite parent session being out of plan-mode → fell back to direct orchestrator edits. |
| 203 | Mock-data distribution verification + generic Tests page (wrong-shape attempt) | Verified parallel-Claude's claim that the Validation tab's mock-data generator pulls from a different distribution than the sliders. Created `scripts/test_mock_distribution.py` with 5 orthogonal cases: (1) singles with σ_meas=0 → recovered σ̂ matches input σ_single within 0.12%; (2) two-source convolution σ_meas=5 → recovered σ̂ matches √(σ_single² + σ_meas²) within 0.06%; (3) deterministic n_bin = round(N × f_bin); (4) f_bin extreme dispatch (0 / 1 / 0.5); (5) Fixed-vs-Log-normal error-model shape difference. All 5 tests PASS → claim REFUTED. Two-source observational model in `app/bc/validation.py:152-165` is implemented correctly. Likely confusion in the parallel session: observational RVs (signal + noise) vs intrinsic RVs only, or ΔRV peak-to-peak vs per-epoch RVs. Then misread the user's "I want a new GUI for verifying mock data" as an ask for a generic test-runner page: built `app/pages/13_tests_verification.py` + `app/pages/_tests_verification_helpers.py` (auto-discovers `scripts/test_*.py`, displays source, runs via threaded `subprocess.Popen` with `st.fragment(run_every=0.5)` live streaming, persists last-run results). User clarified: a "new GUI for X" means a dedicated interactive inspector with X-specific controls and live plots, NOT a generic CLI wrapper — saved as new feedback memory `feedback_new_gui_means_dedicated_interactive.md`. The Tests page is functional but is the wrong shape for the actual ask; user said "we will not do that now" — leaving it on disk. Next-session work: build the actual mock-data inspector (sliders for σ_single, σ_meas, f_bin, error_model, true_logPmax → live `bc.validation.generate_mock_observations_detail` → per-epoch RV histogram vs theoretical N(0, √(σ_single² + σ_meas²)), is_binary count vs round(N·f_bin), ΔRV CDF, KS verdict overlay). | medium | bias-correction, validation, tests, ux | to-test | Claude | Guy | 2026-05-07T17:46:00 | N | Y | UNCOMMITTED. New: `scripts/test_mock_distribution.py`, `app/pages/13_tests_verification.py`, `app/pages/_tests_verification_helpers.py`. Memory: new `memory/feedback_new_gui_means_dedicated_interactive.md` linked from MEMORY.md Critical Rules. Sub-agent plan-mode loop hit again (4 sub-agents stalled in plan despite parent being out) → fell back to direct `Write` calls; code is unchanged from coder's design. The Tests page is cosmetically clean and works, but the actual feature work is to build the mock-data inspector next session. |
| 204 | Validation Model Explorer CDF: smooth pooled empirical + 16/84 percentile band (Dsilva + Langer) | Closed the follow-up flagged in TODO 202: the Model Explorer CDF on the Validation tab still rendered as a coarse step CDF with no uncertainty band despite running `n_sets` MC iterations. Reused the existing `smooth_pooled_cdf(pooled, n_sets, n_fine=500)` helper at `app/bc/helpers.py:118` (already used by `render_shared.py:637-659` for the validation tab's other CDF panel). The Explorer (current params, dashed red) and best-fit overlay (dotted red, when checkbox ticked) now both display a smooth pooled empirical CDF plus a 16/84 percentile band; original step trace kept as the empty-pool fallback (`if _scdf is None`). Applied to both `app/bc/render_lk_explorer.py:1124` (Dsilva) and `app/bc/render_lk_explorer_langer.py:698` (Langer) — three hunks per file (import + Explorer trace block + best-fit overlay block). Reused the LOCAL `_hex_to_rgba` already defined at the top of each file instead of re-importing from `bc.helpers` — avoids shadowing/redefinition warning. Band convention 16/84 percentile matches project standard (Panel B, render_shared marginal/grid CDFs, `feedback_honest_labels.md` 68 % HDI rule). Apply to both current AND best-fit overlay traces for visual consistency: current = dashed red @ 0.20 fill opacity, best-fit = dotted red @ 0.12 fill opacity (so the two bands compose without illegibility). Dropped `shape='hv'` on the median line — that was the source of the staircase look. WORKING flag above `_render_lk_model_explorer` left intact: user explicitly authorised the change. `/error-check` PASS — pyflakes clean, py_compile clean, render test 5/5 PASS, all 6 downstream consumers (render_lk, render_lk_langer, render_validation, render_shared, render_shared_langer, validation) re-import cleanly. | high | bias-correction, model-explorer, plots, cdf | to-test | Claude | Guy | 2026-05-10T15:56:00 | N | Y | UNCOMMITTED — visual sign-off pending per `feedback_no_self_approve.md`. User must reload the webapp and confirm: smooth dashed red line + translucent red band on Explorer (current); dotted red + lighter band on best-fit overlay when checkbox ticked; both bands compose readably; band tracks slider movement; `vrect` bin overlay still visible behind new traces; Langer page identical. Files: `app/bc/render_lk_explorer.py`, `app/bc/render_lk_explorer_langer.py`. Plan: `~/.claude/plans/in-the-model-explorer-generic-hamster.md`. |
| 205 | Standalone `mock_inspector_app/` Streamlit webapp: Mock vs Model-Explorer pipelines side-by-side | Designed and built `mock_inspector_app/` — a NEW standalone Streamlit webapp (not a page in `app/`), launched on port 8503. Files: `__init__.py`, `settings.py`, `runner.py`, `inspector.py`, `app.py` (~2200 lines). Imports from existing project (`bc.helpers`, `wr_bias_simulation`, `pipeline.load_observations`) but writes to its own `mock_inspector_app/settings.json` (auto-created on first save). Compares two pipelines side-by-side at the same params/seed: **Mock Data** (`rng.choice` for binary indices, the validation-tab path) vs **Model Explorer** (`rng.permutation`, the explorer's re-sim path). Both call the same inner `simulate_delta_rv_cadence_aware` → byte-identical orbital draws; only the binary-index assignment differs. Per pipeline: ΔRV CDF (smooth pooled empirical + 16/84 band via `bc.helpers.smooth_pooled_cdf`), f_bin-vs-threshold curve, 3×3 orbital histograms grid (logP, e, q, K1, M1, M2, i_deg, ω_deg, T0_rad — matching the `render_shared_langer.py:1014` `render_orbital_histograms` convention). 8 iteration rounds: (1) initial side-by-side columns; (2) restructured to 3×3 overlay grid with Mock blue + Explorer red overlaid in each panel; (3) tried `multiprocessing.Pool` parallelisation but `mp.Pool(spawn)` blocked the Streamlit thread for 5–10 s on macOS — reverted to pure serial with inline 100-call progress throttle; (4) allowed N_iter=1 + removed observed-CDF trace; (5) added "🎯 Add single run" button with red/green per-star dots; (6) added step LINE through dots on CDF + same dots+step line on f_bin panel; (7) added seed input + accumulate multiple single runs + "🧹 Clear single runs" button; (8) added 16/84 std band on f_bin panel + user-controlled "Std band bins" `number_input` above both panels. Cross-tab divergence investigation: user reported new GUI gives different mock data than validation tab at same seed/params. First bisect was wrong (compared GUI defaults to GUI defaults); re-bisect found root cause: σ_meas default in new GUI = 5.0 km/s, in validation tab (`extras.py:578`) = 1.622 km/s. ΔRV is peak-to-peak of noisy measurements, so σ_meas DIRECTLY inflates each star's ΔRV; live bisect at σ_meas=5.0 vs 1.622 produces completely different sorted ΔRV arrays at the same seed. Audit revealed FIVE different binary-index sampling methods across the codebase (`rng.choice`, two `rng.permutation` sites, two `rng.random < f_bin` Bernoulli sites at `wr_bias_simulation.py:1073` and `:2132`). The Bernoulli sites violate the `feedback_mock_deterministic.md` rule and are flagged as a follow-up. All edits dispatched via the coder agent per project rule. `/error-check` ran clean after every edit. | high | mock-inspector, validation, gui, new-app | to-test | Claude | Guy | 2026-05-10T17:42:00 | N | Y | UNCOMMITTED — visual sign-off pending. New: `mock_inspector_app/__init__.py`, `mock_inspector_app/settings.py`, `mock_inspector_app/runner.py`, `mock_inspector_app/inspector.py`, `mock_inspector_app/app.py` (~2200 lines total). Auto-created on first save: `mock_inspector_app/settings.json`. No changes to existing project files (`app/**`, `wr_bias_simulation.py`, `pipeline/**`, `bc/**`) per "don't touch working code" rule. Open: (a) σ_meas default — change new GUI default 5.0 → 1.622 to match validation tab? user has not picked; (b) n_iter default — validation tab uses 1, new GUI uses 500; (c) Bernoulli f_bin sites in `wr_bias_simulation.py:1073, :2132` — violate the deterministic-mock rule, deferred; (d) multiprocessing for very large N_iter would need `concurrent.futures.ProcessPoolExecutor` + `st.fragment` background polling, not implemented. Launch: `conda run -n guyenv streamlit run mock_inspector_app/app.py --server.port 8503`. |
| 206 | Validation Model Explorer mock-vs-grid CDF mismatch — q_range default mismatch fix (E048 recurrence) | User reported: Model Explorer dashed CDF (at user-set truth params) visibly fits the mock observation worse than the algorithm best-fit dotted CDF, even though those exact truth params generated the mock. Three parallel investigators (coder, scientist, qa) launched. False starts: (1) `logP_max` plumbing bug at `cadence.py:1230-1236` — refuted by reading the saved npz (`logPmax_grid = [3.5]`, both paths correct); (2) "1.0-logL truth-cell reproducibility gap" — couldn't reproduce −39.15 from any seed in 0..1000, fresh code gave −38.12 ± 0.04; (3) "saved mock cannot be reproduced from its metadata" — true at the standalone-script level but the live UI reproduces the saved mock byte-identically (mismatch was a debug-script-context bug, not a real bug). The mismatch in (3) was the breadcrumb to the real bug at `app/bc/render_validation.py:351-357`: UI builds `BinaryParameterConfig(logP_min=0.15, logP_max=true_logPmax, period_model=period_model, e_model=e_model, e_max=0.9)` with only 5 fields explicitly set, leaving `q_range` at dataclass default `(0.1, 2.0)` per `wr_bias_simulation.py:318`. The grid uses the FULL `bin_cfg` from the orbital-params widget which is `q_range=(0.1, 4.0)` for this user's settings. Higher q ⇒ heavier companion ⇒ larger K1 ⇒ larger ΔRV. Mock samples a narrower ΔRV distribution than the grid evaluates against. Numerical confirmation: at truth (0.7, 3.0, 10.0, logP_max=3.5), mock smooth (q≤2) vs grid smooth (q≤4) differ by 0.05–0.09 in CDF across the entire ΔRV range — exactly the offset the user kept pointing at. Saved mock empirical CDF is consistent with q≤2 model (within 1σ for N=25); appears as a +2σ outlier vs the q≤4 model the Explorer plots. **Fix**: build `_base_bin_cfg` from `settings['grid_cadence_dsilva']['orbital']` (Dsilva) or `settings['grid_cadence_langer']` (Langer) — same source `cadence.py` reads — then `dataclasses.replace(_base_bin_cfg, logP_min=0.15, logP_max=true_logPmax, period_model=period_model, e_model=e_model, e_max=0.9)` for the truth-specific overrides. Override only truth-specific fields; everything else (q_range, mass_primary_*, langer_*, q_flipped) inherits from the user's orbital-params widget. Do NOT modify the grid worker side — the fix is one-sided (mock generator only). Same root cause as **E048** (re-sim helper builds fresh `BinaryParameterConfig` and discards the user's bin_cfg) at a new call site. `/error-check` 15/15 PASS. | high | bias-correction, validation, model-explorer, bug | to-test | Claude | Guy | 2026-05-10T17:45:00 | Y | Y | UNCOMMITTED — visual sign-off pending per `feedback_no_self_approve.md`. User to reload the webapp tomorrow, set truth + `q_range=(0.1, 4.0)`, Generate, and verify the mock empirical CDF max ΔRV is now in typical range for q≤4 (often >200; fresh code at seed=42 gives max=244). Validation grid run should land Explorer-at-truth logL within single-mock noise of the algorithm's best. Files: `app/bc/render_validation.py:347-404` (Dsilva and Langer branches), `COMMON_ERRORS.md` (E048 recurrence note appended), `.claude/references/learnings.md` (last_triggered → 2026-05-10 + new entry on running empirical comparison BEFORE repeating "single-mock noise"). Plan: `~/.claude/plans/in-the-model-explorer-replicated-tower.md`. Open: (a) does the fix also resolve the ~1.0 logL stored-vs-recomputed truth-cell gap? likely yes, won't know until a fresh validation run is done; (b) other call sites of `BinaryParameterConfig(...)` with subset-of-fields construction — broader audit deferred. |

## Done

| ID | Title | Description | Priority | Tags | Status | Added by | Suggested by | Date added | Urgent | Important | Date done | Notes |
|----|-------|-------------|----------|------|--------|----------|-------------|------------|--------|-----------|-----------|-------|
| 145 | Corner plots must include sigma_single as a model variable | Corner plots currently ignore sigma_single, focusing only on f_bin and pi. sigma_single is a grid variable — include it in the corner plot. | high | bias-correction, feature | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:46:37 | Duplicate of #134. Fixed 2026-03-21: corner_plots.py now includes logPmax axis. |
| 149 | Best-fit summary table should be per-method inside expanders | The shared best-fit summary table outside all expanders should move inside each scoring method expander, one table per method. Already addressed in #138. | high | bias-correction, feature | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:46:19 | Duplicate of #138 — already implemented. |
| 150 | CDF comparison at top with per-method toggle (duplicate of #139) | CDF graph should be at top above scoring windows, not at bottom. Show best CDFs for each method with error bars as shadow. Per-method toggles. Already addressed in #139. | high | bias-correction, feature | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:46:14 | Duplicate of #139 — already implemented. |
| 156 | Fix cadence checkpoint resume — use saved grid params | Cadence resume was using current UI grid params instead of checkpoint's saved params, causing wrong grid shape on resume. Fixed: override fbin_vals, pi_vals, sigma_vals, logPmax_scan_vals, n_sets from checkpoint. Also load all 8 scoring arrays (was only loading 2). | high | bias-correction, bug | done | Claude | Guy | 2026-03-22T19:10:00 | N | Y | 2026-04-23T17:45:54 | Fixed in cadence.py:511-559 |
| 157 | Simplify bias correction — remove weighted KS/CvM + non-cadence tabs | Removed weighted K-S and CvM scoring methods (keep K-S + Likelihood only). Removed non-cadence Dsilva and Langer tabs (keep cadence tabs + RV Errors + Compare). Deleted dsilva.py, langer.py, runners_dsilva.py, runners_langer.py (~2400 lines). SCORING_METHODS reduced from 4→2. Backup in Backups/bc_backup_20260322/. | high | bias-correction, refactor | done | Claude | Guy | 2026-03-22T19:10:00 | N | Y | 2026-04-23T17:45:51 | User-requested simplification |
| 158 | Hard-split graph rendering into K-S vs Likelihood files | Created 9 independent render files: render_shared.py, render_ks.py, render_ks_scoring.py, render_ks_fit.py, render_ks_explorer.py, render_lk.py, render_lk_scoring.py, render_lk_fit.py, render_lk_explorer.py. No code reuse between K-S and Likelihood (duplicated by design). All under 800 lines. | high | bias-correction, refactor | done | Claude | Guy | 2026-03-22T19:10:00 | N | Y | 2026-04-23T17:45:41 | User explicitly chose duplication over reuse for safety |
| 159 | Review GRAPHS_PER_METHOD.md — apply A1-A4 comments | Full graph review completed 2026-03-24: 21 graphs reviewed, 7 passed, 11 fixed, 4 removed. All changes applied. GRAPHS_PER_METHOD.md fully updated. | high | bias-correction, webapp | done | Claude | Guy | 2026-03-22T19:10:00 | N | Y | 2026-04-23T17:45:37 | [Completed 2026-03-24] Full review + implementation. App test deferred to 2026-03-25. |
| 162 | Test all likelihood graph changes — review every graph per GRAPHS_PER_METHOD.md | Round 4 (2026-03-29): All 17 graphs resolved. D9, D10, D15, D17, D18 approved WORKING. H2/H4 approved. Live polling logPmax 1D fallback added. Grid exclusion moved above heatmaps. "Observed" → "Simulated" binary fraction label. | high | bias-correction, testing, webapp | done | Claude | Guy | 2026-03-23T18:40:00 | Y | Y | 2026-04-23T17:45:28 | [Completed 2026-03-29] All 17/17 graphs approved, removed, or folded. |
| 164 | Cadence Langer graph review — D14/D15/D17 visual approval + D10/D18 fixes | D14 (corner plot), D15 (summary table), D17 (model explorer) fixed and set to TO-TEST. D10 (3D parabolic surface) and D18 (CDF sanity check) still BROKEN. User must visually approve TO-TEST graphs before WORKING flag. | high | bias-correction, webapp, testing | done | Claude | Guy | 2026-03-30T19:00:00 | Y | Y | 2026-04-23T17:45:22 | D14: axis ordering + constant σ exclusion. D15: constant σ row. D17: langer2020 sim, deduplicated slider, margins. |
| 163 | Graph review overhaul: removals, max-LK framing, 15+ fixes | Round 4 (2026-03-29): All remaining MODIFY targets resolved. D15 cadence-aware re-sim, D17 synced sliders + reset counter, D18 5-draw CDF fix, B2 logPmax 1D fallback, G1 grid exclusion moved, Langer 1D heatmap fallback. | high | bias-correction, webapp, refactor | done | Claude | Guy | 2026-03-24T19:27:00 | N | Y | 2026-04-23T17:45:19 | [Completed 2026-03-29] All graph review work done. |
| 166 | Evaluate likelihood bin division — reliability and alternatives | Assessed D'Silva+2023 Sec 4.2 binning (4 bins for 11 stars). With 25 stars, 5 bins justified. User chose `[0, 15, 45.5, 150, 300, ∞]` — detection threshold as physical edge, other edges at observed CDF structure. Added L bins column to file browser. Preset UI deferred. | high | bias-correction, research, statistics | done | Guy | Guy | 2026-03-30T19:00:00 | N | Y | 2026-04-23T17:44:57 | [Worked 2026-03-31] Binning methodology discussed, edges chosen, L bins column added to file_ops.py. Preset config UI not yet implemented. |
| 168 | Spectrum page improvements — absorption line search for non-RV binaries | Improve the spectrum/plots page to enable searching for absorption lines in WR spectra. Goal: (1) find SB1 signatures (potential dormant BH-WR), (2) find SB2 companions in stars classified as single. Related to #67. Implemented: absorption depth heatmap, epoch difference plot, LMC redshift correction, show-all-epochs, 3-tab restructuring, zoom history nav. | high | webapp, spectrum, research | done | Guy | Guy | 2026-03-30T19:00:00 | N | Y | 2026-04-23T17:44:44 | [Completed 2026-03-31] Full overhaul in spectrum_helpers.py + 02_spectrum.py. Relates to #67. |
| 22 | Create NRES analysis page | New page for NRES spectra: stitching, blaze correction, CCF on emission lines, RV std threshold determination | medium | webapp, NRES | done | Claude | Guy | 2026-03-01 | N | Y | 2026-04-23T17:43:57 |  |
| 135 | Manual likelihood bin edges selector | Add ability to manually choose the coarse ΔRV bin edges used by the multinomial likelihood score (currently hardcoded [0, 45.5, 250, 650, ∞]). | medium | bias-correction, feature | done | Guy | Guy | 2026-03-18T12:00:00 | N | Y | 2026-04-23T17:43:45 | Group 4. Fixed: shared _render_likelihood_bin_config in params.py, threshold-based or manual mode. |
| 143 | Add sigma_single column to Scoring Method Comparison table | The scoring method comparison table is missing sigma_single. Add it. | medium | bias-correction, feature | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:43:38 | Duplicate of #132. Fixed 2026-03-21. |
| 146 | Manual coarse bin edges for likelihood score (already done as #135) | Add ability to choose the coarse bin edges manually used by the likelihood score. See task #135. | medium | bias-correction, feature | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:43:33 | Duplicate of #135 — already implemented. |
| 148 | Likelihood 2D interpolation graphs say 'S' not 'likelihood' | In the likelihood expander, 2D interpolation graphs have 'S' in their labels instead of 'likelihood'. Fix labels to match actual content. | medium | bias-correction, bug | done | Guy | Guy | 2026-03-18T16:30:00 | N | Y | 2026-04-23T17:43:22 | Duplicate of #137. Fixed 2026-03-21: colorbar_title_override for Likelihood. |
| 182 | Corner-plot marginalization audit vs Dsilva 2022 | Scientist agent (paper-research) audited 1D/2D marginal computation in `app/bc/corner_plots.py`. Joint grid `result['likelihood'] = exp(logL_raw − logL_max)` is linear-likelihood max=1.0, NOT integral-normalized. 1D marginals at `corner_plots.py:197-201` use `np.nansum` over nuisance axes, then `_add_1d_posterior` at `:37-38` renormalizes via `np.trapezoid` so each curve integrates to 1. 2D marginals at `:204-223` use `np.nansum`; HDI contours at 68%/95% from cumulative mass. Compared to Dsilva 2022 §5.2: "Assuming flat priors... marginalised posterior likelihood." Under flat priors posterior ∝ likelihood → marginalizing posterior reduces to integrating linear likelihood over nuisance axes — exactly what code does. **Verdict: scientifically correct, matches Dsilva 2022.** | medium | bias-correction, audit, paper | done | Claude | Guy | 2026-04-19T23:55:00 | N | Y | 2026-04-23T14:44:51 | No code changes (research/audit only). Caveats flagged: (1) `nansum` assumes uniform grid spacing — fine currently; (2) `exp(logL−logL_max)` underflows to 0 far from max (same Dsilva white-zone behavior); (3) we plot 68%+95% contours, Dsilva plots 68% only — cosmetic. |
| 184 | Binning robustness literature research (no code) | Delegated literature search to paper-research agent on how bin count / bin placement affects binned-likelihood inference of `(f_bin, π)` for the N=25 WC sample. Identified direct precedent: Dsilva 2022 Sect. 5.2 used 4 bins at physically motivated edges for N=12–16 WR stars. Confirmed standard rules (Sturges, Rice, Scott) all give ~5–6 bins for N=25, so 10–20 bins would be noise-dominated (~50 % empty bins). Clarified Cash 1979 / Baker–Cousins 1984: binned likelihood is valid at any counts/bin but loses discriminating power at <~5 stars/bin. Designed 5-step robustness test protocol (bin-count sweep, edge-shift sweep, quantile vs physical edges, Anderson–Darling bin-free cross-check, leave-one-out jackknife). Wrote findings to `memory/binning_robustness_research.md` with full citation list (Cash, Baker–Cousins, Feigelson & Babu, Engmann & Cousineau, Dsilva 2020/2022, Sana 2012/2013, Kobulnicky & Fryer 2007). Indexed under "Detailed Memory Files" in `memory/MEMORY.md`. | medium | bias-correction, research, paper | done | Claude | Guy | 2026-04-20T16:42:00 | N | Y | 2026-04-23T14:44:47 | Research only — no code. Decision: keep ~5 bins in the paper and defend it by citing Dsilva 2022 Sect. 5.2 precedent. Do NOT increase to 10 or 20 bins — would guarantee ~50 % empty bins and noise-dominated posterior. Anderson–Darling cross-check identified as single highest-leverage robustness addition (bin-free, beats K-S at small N per Engmann & Cousineau 2011); not yet implemented — feeds #186 follow-up. Open question: whether to add Dirichlet(α=1) smoothing for zero-probability bins. |
| 186 | Bin-Sensitivity sub-tab (likelihood bin-choice robustness) — 4 new files, 6 A&A plots, mock-data validation | Built end-to-end over 4 coordinated rounds (scientist + plots + designer + coder + QA). New files in `app/bc/`: `bin_schemes.py` (263 lines, 8 scheme builders retained as library), `bin_sensitivity_scorer.py` (~793 lines, `SchemeResult` dataclass + MP-pool re-simulation with seed-stable reconstruction matching `runners_cadence.py:82-92` bit-identically + pitfall detector P1-P6 + K-S + HDI68), `bin_sensitivity_plots.py` (~746 lines, 6 pure Figure-returning plot builders all A&A-ready via `_academic_fig` from `app/plots/theme.py`), `bin_sensitivity.py` (~720 lines, tab renderer). Surgical edits to `__init__.py`, `helpers.py` (added `_BIN_SCHEME_COLORS` + `get_scheme_color`), `05_bias_correction.py` (tab registration). QA verified `_logL_one_scheme` matches `wr_bias_simulation.multinomial_log_likelihood` exactly (diff=0) and E048 full bin_cfg/period_model/cadence_weights/sigma_meas forwarding intact. UI scope narrowed per user to manual scheme rows only (locked `dsilva_default` row 0 + user-added rows with name + edges + delete button); parametric-family multiselects (`equal_width`, `log_spaced`, `quantile`, `anchored`, `freedman_diaconis`) removed from tab even though their builder functions are retained in `bin_schemes.py`. Mock-data mode added: 2-option radio + 5 number_inputs (f_bin_true=0.46, π_true=0.0, σ_true=15.0, log P_max_true=5.0, seed=42) using `generate_mock_observations()` from `validation.py`; truth overlays on plots #2 (gold star), #4 (green dashed lines), #6 (top-right annotation); `Δf_bin`/`Δπ` columns added to summary table in mock mode. One bug fixed mid-sprint (fragment polling didn't trigger main-script rerun when job flipped to done) via one-shot `st.rerun(scope='app')` in `_render_progress_fragment._poll()` — covered by existing E026. Plot #6 (bin-edge map) flagged STRONGLY APPROVED in `memory/plot_preferences.md` per user feedback. py_compile + pyflakes + `scripts/test_render.py` (5/0/0) clean. | high | bias-correction, research, robustness | done | Claude | Guy | 2026-04-20T19:22:00 | N | Y | 2026-04-23T14:44:12 | **UNCOMMITTED — awaiting user visual sign-off ~2026-04-22.** Test plan (from `memory/current_focus.md`): real-obs mode visual sweep → mock-obs mode visual sweep → truth overlays register on plots #2/#4/#6 → Δf_bin/Δπ columns appear in mock mode → all 6 plots render white-bg/serif/no-gridlines per A&A. On PASS, commit the 4 new bc files + `app/bc/__init__.py` + `app/bc/helpers.py` + `app/pages/05_bias_correction.py` + `memory/plot_preferences.md` + `memory/likelihood_bin_sensitivity.md` + `memory/current_focus.md`. Deferred: (a) multi-`n_bins` per family in one run, (b) `dsilva_shift_minus` scheme, (c) P3 + P5 auto-detectors, (d) quantile-family "from observed quantile" button — user said "ignore the gaps for now". |
| 128 | Binned multinomial likelihood (Dsilva+2023) — implementation + sigma sensitivity bug | Implemented multinomial_log_likelihood() following Dsilva+2023 Sec 4.2. Added as 3rd scoring option + CvM bonus. Likelihood heatmaps, corner plots (red), HDI68 columns all working. **BUG: likelihood flat across sigma_single** — needs investigation (pooling, bin sensitivity, or fundamental limitation). | critical | bias-correction, methodology | done | Claude | Tomer | 2026-03-16T19:56:00 | Y | Y | 2026-04-23T14:43:25 | Instructor flagged that p-values are not proper likelihoods for posteriors. Implementation done but sigma sensitivity unresolved. |
| 109 | RV Errors tab refinements: histogram fix, fit history, auto-fit, flat dist, scores | Fixed histogram x-axis cutoff (removed max(0,...)), added persistent fit history table, made Auto-Fit All persistent with best-fit PDF overlay, added Flat (uniform) distribution, added AIC/BIC/logL score explanations expander. | high | bias-correction, rv-errors | done | Claude | Guy | 2026-03-12T12:00:00 | N | Y | 2026-04-23T14:18:54 |  |
| 110 | Auto-commit hook for incremental rollback | PostToolUse hook on Edit/Write that auto-commits each file change with [AUTO] Save: filename message. Added to .claude/settings.json. | medium | automation, git | done | Claude | Guy | 2026-03-12T12:00:00 | N | N | 2026-04-23T14:18:39 |  |
| 105 | CDF sanity check in cadence tabs | Added 5-random-draw CDF sanity check plot in cadence tabs (both Dsilva and Langer). Generates 5 sets of 25 simulated stars at best-fit params with different seeds, overlaid on observed CDF. Verifies cadence-aware pipeline runs correctly. | medium | webapp, bias-correction | done | Claude | Guy | 2026-03-12T00:00:00 | N | Y | 2026-04-23T14:18:27 |  |
| 108 | RV Error Modeling tab + error model selector | RV Errors tab in Bias Correction page: explore per-epoch RV error distributions (single vs binary), with adjustable distribution parameters (Normal, Log-normal, Gamma, Weibull, Exponential), auto-fit with AIC/BIC/Q-Q, combined K-S test. Error model selector added to all 4 simulation tabs (Dsilva, Langer, Cadence x2) replacing fixed σ_measure input. Relates to #53. | high | webapp, research, bias-correction | done | Claude | Guy | 2026-03-12T00:00:00 | N | Y | 2026-04-23T14:18:15 |  |
| 103 | RV Modeling page improvements — follow-up on #52 | All 10 items implemented in 2026-03-17 rebuild: (1) nav order, (2) dashboard, (3) PLOTLY_THEME, (4) residuals under fit, (5) parameter playground, (6) χ²_red on plot, (7) binary ΔRV histogram, (8) Dsilva/Langer presets, (9) residuals fixed, (10) notebook approach replicated + improved. | high | webapp, research, bias-correction | done | Claude | Guy | 2026-03-11T16:34:35 | N | Y | 2026-04-23T14:18:12 | [Completed 2026-03-17] |
| 99 | Spectrum page: model comparison + classification table | Improve the Spectrum page (02_spectrum.py) with: 1) Overlay star model spectra (O, OB, different spins, metallicities, etc. from online catalogs) for comparison, with dashed lines marking important absorption line elements. 2) Classification table below the spectrum — per-star classification (SB1, SB2, free text, etc.) with notes field. 3) Easy classification workflow via dropdown menus and buttons to fill in the table. Related to #67 (SB2 signature page). [Done by overnight agent — needs review] | high | webapp, spectrum, classification | done | Guy | Guy | 2026-03-11T | N | Y | 2026-04-23T14:17:47 |  |
| 98 | Fix & convert Plots page — academic Plotly + bug fixes + per-plot selectors | Fixed nm→Å wavelength mismatch. Converted to white academic Plotly theme (A&A/ApJ style) with interactivity (zoom/pan/hover). Fixed StarClass epoch parsing bug (epoch10+) and load_property directory return bug. Added per-plot star/band/epoch selectors. Created auto-learn plot preferences (memory/plot_preferences.md). Related to #21, #26, #27 | critical | webapp, plots, bug | done | Claude | Guy | 2026-03-10 | Y | Y | 2026-04-23T14:17:33 |  |
| 93 | Account for RV measurement errors in the observed CDF | The observed CDF currently treats each measured ΔRV as exact (error-free). Investigate how to propagate RV measurement uncertainties into the CDF — e.g., binomial confidence intervals on the fraction per bin, or Monte-Carlo resampling within error bars. Consider whether/how these errors can be incorporated into the K-S statistic as weights or confidence bands. Implemented: σ_p2p significance criterion (ΔRV − 4σ > 0) applied to both simulated and observed binary fraction curves. | high | bias-correction, research | done | Claude | Tomer | 2026-03-10 | N | Y | 2026-04-23T14:17:13 | [Completed 2026-03-31] σ_p2p added to simulate_with_params(), significance criterion on all curves, Bartzakos +3 correction. |
| 95 | Verify Dsilva marginalized posterior uses true best σ_single | Tomer flagged that the marginalized posterior summary in the Dsilva tab might display the last-computed σ_single slice rather than the one with maximum K-S p-value. Check: (1) the GUI shows the overall best-fit model first, then updates all downstream graphs when the σ_single slider is moved; (2) the posterior marginalization properly weights by K-S score across σ_single slices (Tomer suspects it might assume a flat prior on σ_single). Related to #78 and #18. | high | bias-correction, bug | done | Claude | Tomer | 2026-03-10 | N | Y | 2026-04-23T14:15:52 |  |
| 79 | When running on a grid of LogP_max, and exploring using the slider, the graphs below wont show the data with the chosen LogP_max | Added 'Use current slice for analysis' toggle. When on, downstream graphs use slice best-fit instead of global argmax. | high | bias-correction | done | Guy | Guy | 2026-03-08 | N | Y | 2026-04-23T14:14:52 |  |
| 84 | Page state resets when switching pages | RV Modeling page: full SettingsManager persistence added (6 files, ~100+ widgets, user_settings.json). Bias Correction page already had persistence. Remaining: Spectrum page persistence unsolved, background process for simulations still needed. | high | bias-correction, webapp, bug | done | Guy | Guy | 2026-03-08 | Y | Y | 2026-04-23T14:14:48 | RV Modeling persistence added 2026-04-06. Spectrum page still open. |
| 73 | Add +- errors for the models outcome of the bias-correction simulation | I noticed you replaced that with saying the range they are within according to the 68% area under the curve rule, thats great but i still wanna see it also in error format. | low | bias-correction | done | Guy | Guy | 2026-03-08 | N | N | 2026-04-23T14:14:42 |  |
| 77 | Add percentage to progress bars | Add percentage to progress bars throughout the webapp (maybe if you created a single go to for processes, that would be easy), up the 3 digits after the dot. e.g. 56.356% and make sure it has high refresh rate | low | webapp, feature | done | Guy | Guy | 2026-03-08 | N | N | 2026-04-23T14:14:31 |  |
| 46 | after agent works on a feature, check that all previous features still work | Sometimes, after an agent works on a feature stuff break, for example, the Data alias folder got deleted and broke stuff. so besides of course checking that the new feature/bug fixes are implemented, there should be a maintained list of features in the webapp that should be checked everytime that it works. after it checked everything works you can merge the git branch, otherwise, you can check live what recently has been done to break it maybe by comparing the git versions which it last 100% worked on, and fix it before saving the changes | high | agent | done | Guy | Guy | 2026-03-05 | N | Y | 2026-04-23T14:14:28 |  |
| 52 | Statistical RV modeling page — f_bin vs DeltaRV threshold fitting | Full rebuild 2026-03-17: two-component mixture model with Empirical (simulation-based) + Gaussian (analytical) side-by-side. 4 tabs: Sample Fit, Fraction Recovery, Global Correction, Population Sim (placeholder). Parameter playground with instant sliders + orbital params. Preset buttons (Dsilva/Langer). Full error model selector. Split into app/rv_modeling/ package. | high | research, webapp, bias-correction | done | Guy | Guy | 2026-03-05 | N | Y | 2026-04-23T14:14:15 | [Rebuilt 2026-03-17] Full rewrite fixing all 5 rejection issues. Split into app/rv_modeling/ (4 modules). Population Sim tab still placeholder. |
| 67 | SB2 signature page | An important step I have not done in high quality is trying to find absorption lines in the WR stars spectra. The goals are 2: 1) find SB1, since my stars (should check in literature) won't show x-ray signatures of BHs, this would be exiting as followups to find a dormant BH-WR pairs. 2) maybe find a companion (SB2) from stars i classified as singles, so that would mean its a binaryand i should account for that. Core absorption search implemented in #168 (spectrum_helpers.py). | high | SB2 Signature page, webapp | done | Guy | Guy | 2026-03-05 | N | Y | 2026-04-23T14:14:02 | [Completed 2026-03-31] Absorption depth heatmap + epoch difference plot in spectrum_helpers.py. |
| 66 | Home page | I want the Home page to show the summary of the whole project, which was discussed in another task as well. In regard to what figures should be shown, I'll elaborate now, but note that I might write plots we didn't do yet, so leave an empty space there: 1) summary table of stars, with RVs and error,binarity, a cleaned flag/color on column, ability to sort in many ways, like found in my thesis work notebook. 2) observed binary fraction vs DeltaRV threshold. 3) k-s heatmap of the bias-correction. 4) the model fit to the observed binary fraction vs DeltaRV threshold. 5) The NRES star table of the RVs and the resulting std of the RVs. 6) The CDF of the DeltaRV compared to the best fitted model in the bias-correction | low | Home Page | done | Guy | Guy | 2026-03-05 | N | N | 2026-04-23T14:13:15 |  |
| 21 | Fix broken Plots page — implement from notebook | Implement all available plots from Plots.ipynb and StarClass.py plot methods in the webapp | high | webapp, plots, bug | done | Claude | Guy | 2026-03-01 | N | Y | 2026-04-23T14:12:58 |  |
| 6 | Test full end-to-end webapp run | Launch app and verify all pages work correctly, including bias correction with live heatmap | high | webapp, testing, bug | done | Claude | Guy | 2026-03-01 | N | Y | 2026-04-23T14:12:54 |  |
| 26 | Fix spectrum axis units to Angstrom | Spectrum browser shows nm but data should be in Angstrom — fix all axes and add preference to CLAUDE.md | high | webapp, spectrum, declined | done | Claude | Guy | 2026-03-01 | N | Y | 2026-04-23T14:12:51 | [DECLINED 2026-03-11T18:00:58] fits file hold it in nm, so always multiply by 10 when showing from them in Angstom |
| 29 | Auto-save state management | Automatic state saving with date/time dropdown list, assess ~100 state limit feasibility | medium | webapp, settings | done | Claude | Guy | 2026-03-01 | N | Y | 2026-04-23T14:12:37 |  |
| 31 | Create project status dashboard page | Documentation of all relevant plots and calculations, track implementation status (working, bugs, planned, not started) with an interactive checklist. That would be very cool to do on the home page. A summary of the workflow of that whole project (normalization, stitching, cleaning, CCF, adjustments, RV threshold, statistical model of RVs from binary simulations, bias correction, paper). and the status of the paper. | low | webapp, global, Home Page | done | Claude | Guy | 2026-03-01 | N | N | 2026-04-23T14:12:23 |  |
| 5 | 2D parameter histograms | Research whether 2D orbital parameter histograms (e.g. P vs e) add scientific value — confirm with Tomer | low | bias-correction, research | done | Claude | Tomer | 2026-03-01 | N | N | 2026-04-23T14:12:02 |  |
| 91 | Bin the simulated CDF in 10 km/s steps up to ~350 km/s | Instead of using raw RV values for the CDF, discretize into a grid with ~10 km/s bins ending around 350 km/s. This regularizes the CDF comparison and avoids noise from sparse high-RV tails. Related to #1 (CDF truncation). | high | bias-correction | done | Claude | Tomer | 2026-03-10 | N | Y | 2026-03-18T10:28:38 |  |
| 51 | measure DeltaRV threshold using the NRES data | Ive got stitched and blaze-corrected NRES data of 2 WC stars in the MW. I intially wanted to try and measure the RV using the same method of the CCF on x-shooter and assuming those stars are single/ long binary the std of the RV should represnt the RV variability of a lone WC stars close to my type (although the MW has a solar metallicity and the LMC has 0.5), which means DeltaRV about that should reject the option its due to a single WR stars, but has to be from a binary. This should be analysed easily in an 'NRES' page or 'DeltaRV threshold' page. and summarized when available in the home page. | high | RV threshold, NRES | done | Guy | Guy | 2026-03-05 | N | Y | 2026-03-18T10:28:01 |  |
| 50 | Parameters set by me get reset when switching pages. | The expected behaviour is that unless i close the webapp completely and open, i dont expect the parameters I've set to reset,  it should always cache, so it would feel natural | high | bias-correction, webapp, bug | done | Guy | Guy | 2026-03-05 | N | Y | 2026-03-18T10:27:56 |  |
| 45 | remember settings/parameters/toggles in all pages | i dont like it when i go to the To-Do page and the ranges i set for the simulation in the bias correction page are always reset. the webapp should feel like an application who lets me explore it while freely | high | bias-correction, bug, webapp | done | Guy | Guy | 2026-03-04 | N | Y | 2026-03-18T10:27:48 |  |
| 44 | bias correction should run in sleep mode | when i put my laptop in clemshell mode (fold it) it stops the simulation. it should run no matter what i do unless im stopping it using ctrl+c from terminal. | high | bias-correction, bug | done | Guy | Guy | 2026-03-04 | N | Y | 2026-03-18T10:27:44 |  |
| 92 | Per-star cadence simulation (25 stars × N_sets) with median CDF + error band | Simulate in sets of 25 stars: each set contains exactly one simulated star per real star in the sample, using that star's actual observation cadence (MJD timestamps from FITS headers). Run N_sets (user-configurable, default 10k) of these, so total simulated stars = 25 × N_sets. For each (f_bin, π) grid point, build the simulated CDF by taking the median binary fraction in each ΔRV bin across the N_sets realizations. Show the 68% posterior width as a shaded error band. Compute the K-S score against the median CDF, weighted by the per-bin standard deviation. This replaces the current approach of 10k independent stars with cadence-aware grouped sets. | critical | bias-correction | done | Claude | Tomer | 2026-03-10 | Y | Y | 2026-03-18T10:27:31 |  |
| 102 | follow up on task 101, add quick compare to the compare tab | Multi-select dataframe in Compare tab with orange highlighting on differing settings rows. | low | bias-correction, feature | done | Guy | Guy | 2026-03-11T15:57:47 | N | N | 2026-03-18T10:26:56 |  |
| 101 | loading previous results for bias correction | Clickable parameter table in Dsilva/Langer tabs to load saved results. Uses _scan_result_metadata() helper. | low | bias-correction, feature | done | Guy | Guy | 2026-03-11T15:53:07 | N | N | 2026-03-18T10:26:53 |  |
| 127 | Partial results table: precision + column alignment | Fixed: percentage now shows 1 decimal (.1f). Added ΔRV bin, ΔRV max, Scoring, Best f_bin columns to match full results table. | medium | bias-correction, feature | done | Claude | Guy | 2026-03-16T13:00:00 | N | N | 2026-03-18T10:26:42 |  |
| 126 | S-score titles should say "weighted S-score" | Partially done: updated _make_min_score_fig, _s_label, _stat_label, per-sigma table column, and caption. User noted more labels may need updating. | medium | bias-correction, labels | done | Claude | Guy | 2026-03-16T13:00:00 | N | N | 2026-03-18T10:26:30 |  |
| 121 | Dsilva cadence: 1D sigma_single plot → score instead of p-value | Fixed: added _make_min_score_fig() for weighted S-score plots. Live 1D sigma graph shows min S-score. Final results include 1D score vs sigma plot + Min weighted S column. | medium | bias-correction, feature | done | Guy | Guy | 2026-03-15T19:00:00 | N | Y | 2026-03-18T10:26:26 |  |
| 116 | EndConv/EnDay daily logging system | Created `/EndConv` slash command (`.claude/commands/EndConv.md`) that appends structured conversation summaries to `daily_logs/YYYY-MM-DD.md`. Updated `/EnDay` to read daily log as Step 1 before git inventory. | medium | automation, documentation | done | Claude | Guy | 2026-03-12T11:21:00 | N | N | 2026-03-18T10:26:21 |  |
| 115 | Standalone TODO webapp extraction | Extracted TODO logic from `app/pages/10_todo.py` into `app/todo_core.py` (shared module). Page is now 27-line wrapper. Created `todo_app.py` standalone entry point (port 8502). Both apps share same `TODO.md`. | medium | webapp, feature | done | Claude | Guy | 2026-03-12T13:40:00 | N | N | 2026-03-18T10:26:17 |  |
| 23 | Statistical RV modeling page | New page: model f_bin vs threshold by simulating RV pulls from binary orbital distributions and single-star Gaussians (from Thesis work.ipynb cells 83-89). Superseded by #52 — page fully rebuilt 2026-03-17. | medium | bias-correction, stats | done | Claude | Guy | 2026-03-01 | N | Y | 2026-03-18T10:26:03 | [Completed via #52 rebuild 2026-03-17] |
| 107 | Save & resume partial simulation runs | Cadence cancel handler now saves .partial.npz on cancel (Dsilva/Langer already had this). Shows 'progress saved' message. Existing partial load/resume UI in Dsilva/Langer tabs continues to work. | medium | webapp, bias-correction | done | Claude | Guy | 2026-03-12T00:00:00 | N | Y | 2026-03-18T10:25:55 |  |
| 106 | Methodology explainer expanders | Added collapsible methodology explainers to Langer and cadence tabs (Dsilva already had one). Explains step-by-step simulation logic, period model, K-S test, and cadence-aware modifications. | medium | webapp, bias-correction | done | Claude | Guy | 2026-03-12T00:00:00 | N | N | 2026-03-18T10:25:54 |  |
| 104 | Heatmap smoothing fix | Changed zsmooth=False to zsmooth='best' in shared.py make_heatmap_fig() for bicubic interpolation on all heatmaps. Also fixed comparison overlay heatmap. Zero performance cost (client-side). | medium | webapp, bias-correction | done | Claude | Guy | 2026-03-12T00:00:00 | N | N | 2026-03-18T10:25:46 |  |
| 129 | Split large webapp pages into subpackages | Audited all pages for file size. Split 3 pages exceeding 1000 lines into subpackages: 06_plots.py→app/plots/ (6 modules), 11_nres_analysis.py→app/nres/ (5 modules), 12_rv_modeling.py→app/rv_modeling/ (4 modules). Added ROOT export to shared.py. Added 800-line-max rule to CLAUDE.md. Bias correction split (app/bc/) handled separately. | high | webapp, refactor | done | Claude | Guy | 2026-03-17T19:21:00 | N | Y | 2026-03-18T10:25:43 | All modules under 825 lines. Needs webapp testing. |
| 125 | Live cadence heatmap stuck on first sigma_single | Fixed: live heatmap now shows CURRENT sigma being simulated (not best). Status text shows both current slice and overall best sigma. | high | bias-correction, bug | done | Claude | Guy | 2026-03-16T13:00:00 | N | Y | 2026-03-18T10:25:38 |  |
| 124 | NaN crashes in cadence results rendering (5 locations) | Fixed: added np.any(np.isfinite()) guards before all nanargmax/nanargmin calls in _render_cadence_results. All-NaN slices show '—' or skip gracefully. Also fixed _parabolic_min_1d/2d/3d. | high | bias-correction, bug | done | Claude | Guy | 2026-03-16T13:00:00 | N | Y | 2026-03-18T10:25:28 |  |
| 123 | Cadence save button not working (no toast, no file in table) | Fixed: removed st.rerun() after save (was killing toast). Replaced with st.success() message. File appears on next interaction. | high | bias-correction, bug | done | Claude | Guy | 2026-03-16T13:00:00 | N | Y | 2026-03-18T10:25:26 |  |
| 122 | Grid exclusion mask shape mismatch in cadence tabs | Fixed: exclusion masks now stored as VALUE SETS (not index arrays). Cadence section rebuilds masks from values against its own grid. Also guards all-NaN in _render_cvm_analysis. | high | bias-correction, bug | done | Claude | Guy | 2026-03-16T13:00:00 | N | Y | 2026-03-18T10:25:19 |  |
| 120 | Add posterior edge error bars to summary table | Fixed: cadence summary table now shows mode +hi/−lo format (matching Dsilva/Langer). Added σ_single row for multi-sigma runs. | high | bias-correction, feature | done | Guy | Guy | 2026-03-15T19:00:00 | N | Y | 2026-03-18T10:25:09 |  |
| 119 | BUG: cadence resume uses wrong scoring method | Fixed: scoring_method now saved in partial checkpoints and enforced on resume with warning if mismatch. Also saved in final .npz results. | high | bias-correction, bug | done | Guy | Guy | 2026-03-15T19:00:00 | Y | Y | 2026-03-18T10:24:54 |  |
| 118 | Cadence diagnostic histogram bin_cfg passthrough fix | Fixed `_render_cadence_results()` which was rebuilding `BinaryParameterConfig` from session_state with wrong defaults (missing langer_period_params, q_flipped, wrong e_model). Now passes the already-constructed `_bin_cfg` directly from the tab UI. | high | bias-correction, bug | done | Claude | Guy | 2026-03-12T18:40:00 | N | Y | 2026-03-18T10:24:48 |  |
| 117 | Inverse-variance weighted K-S scoring for cadence tabs | Replaced by CvM (Cramér–von Mises) inverse-variance-weighted scoring: S = Σ(F_obs - F_sim_median)²/σ²_i with empirical p-value from 10k simulation sets. Added to all 4 tabs (Dsilva, Langer, Cadence×2) with radio button UI. 3 CvM heatmaps (raw S, p-masked S, empirical p-value). Original weighted K-S approaches (weighted avg, weighted max, chi²) all failed — see DOCUMENTATION.md 2026-03-13 entry. | high | bias-correction, methodology | done | Claude | Tomer | 2026-03-10 | N | Y | 2026-03-18T10:24:36 |  |
| 112 | Cadence tabs: heatmap contours, save/load table, partial load/cleanup | Fixed cadence heatmap quality (removed duplicate star, contours now appear on final render). Added save/load results table to both cadence tabs. Added partial checkpoint load UI + cleanup on success. Cadence results now save with cadence_dsilva/cadence_langer prefix for proper filtering. | high | bias-correction, cadence | done | Claude | Guy | 2026-03-12T14:00:00 | N | Y | 2026-03-18T10:23:54 |  |
| 111 | Langer partial checkpoint UI + resume partial computation | Added Langer partial checkpoint detection + Load partial button (matching Dsilva). Both Dsilva and Langer now resume from .partial.npz checkpoint on Run (skip already-computed f_bin rows). | high | bias-correction, feature | done | Claude | Guy | 2026-03-12T12:00:00 | N | Y | 2026-03-18T10:23:38 |  |
| 100 | change the Date added in Sort by to 'Reccently added'. and add time stamp as well | title is enough | high | To Do, feature | done | Guy | Guy | 2026-03-11T14:41:28 | N | Y | 2026-03-18T10:23:05 |  |
| 83 | Live heatmap flicker during simulation | Reworked: replaced global st.rerun(scope='app') auto-refresh with per-tab @st.fragment(run_every=3) polling. Each tab now re-renders only its own live elements — no full-page rerun, no flicker. Also fixed Langer cadence display (f_bin×σ instead of f_bin×π) and np.empty→np.full(nan) bug. | high | bias-correction, bug | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-18T10:22:43 | [Reworked 2026-03-11] |
| 85 | same heatmap in the corner plots | Corner plots now use RdBu_r, zsmooth='best', 98th percentile clip. Langer corner also has contour lines now. | high | bias-correction, bug | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-18T10:22:41 |  |
| 80 | maximum p-vlaue summary split | Added two st.metric boxes: Current slice best vs Global best. | high | bias-correction, feature | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-18T10:22:37 |  |
| 42 | best f_bin and pi wont set to all other graphs after bias correction simulation run | Fixed: gap_sim now invalidated via fingerprint when result/slider changes. Added 'Use current slice' toggle. | high | bias-correction, bug | done | Guy | Guy | 2026-03-04 | N | Y | 2026-03-18T10:21:36 |  |
| 90 | after a bias correction run, write a summary of the best model value, with the errors | Summary table rewritten: one row per parameter, posterior mode ± 1σ (HDI68) errors shown clearly. | critical | bias-correction, bug | done | Guy | Guy | 2026-03-09T12:12:25 | Y | Y | 2026-03-18T10:21:18 |  |
| 78 | Langer heatmap result isnt a perfect match with the summarised result below heatmap | Fixed: summary now shows both Global argmax and Posterior mode (HDI68) in a clear table with separate p-values for each. Applied to both Dsilva and Langer tabs. | critical | bias-correction, bug | done | Guy | Guy | 2026-03-08 | Y | Y | 2026-03-18T10:21:05 |  |
| 53 | Model RV measurement errors from observations for bias correction | The bias correction currently uses a fixed sigma_measure=1.622 km/s for all stars/epochs (set in wr_bias_simulation.py SimulationConfig.sigma_measure, used at line ~1568). Real per-epoch RV errors are available in detail[star][rv_err] from load_observed_delta_rvs(). Task: (1) Collect all per-epoch RV errors across 25 stars, (2) Fit a parametric distribution (try log-normal, gamma, and normal — pick best fit by AIC/BIC), (3) Add a toggle in the bias correction page (05_bias_correction.py) to use Empirical error model instead of fixed sigma_measure, (4) When enabled, sample per-epoch errors from the fitted distribution instead of using fixed value. Small addition to wr_bias_simulation.py (add error_model field to SimulationConfig) and the bias correction page UI. | high | bias-correction | done | Guy | Guy | 2026-03-05 | N | Y | 2026-03-15T17:07:20 | [Modified 2026-03-15T17:07:18] |
| 76 | improvement in running the bias-correction runtime | Pool initializer pattern: shared data (cadence, obs) sent once per worker, not per task. Task args shrunk from full sim_cfg to (f_bin, pi, sigma, bin_cfg, model, seed). Note: we cant, it runs slower cuz binaries are harder to simulate so thats why it longer the more it went. | critical | bias-correction, declined | done | Guy | Guy | 2026-03-08 | Y | Y | 2026-03-15T12:43:50 | [Modified 2026-03-15T12:43:47] |
| 2 | Duplicate Dsilva/Langer tabs | Add ability to duplicate parameter tabs for side-by-side exploration of different grid configurations | critical | bias-correction, webapp | done | Claude | Tomer | 2026-03-01 | Y | Y | 2026-03-12T13:38:58 | [Modified 2026-03-12T13:38:56] |
| 24 | Set up Overleaf/LaTeX paper structure | Create paper/ directory with A&A format LaTeX skeleton, sync instructions for Overleaf, start drafting sections from DOCUMENTATION.md | high | paper | done | Claude | Guy | 2026-03-01 | N | Y | 2026-03-11T18:11:41 | [Modified 2026-03-11T18:11:37] |
| 28 | Toggle cleaned/contaminated stars in all plots | When possible, add toggle to show results with or without cleaned (less reliable) stars | medium | webapp, plots | done | Claude | Guy | 2026-03-01 | N | Y | 2026-03-11T18:04:12 |  |
| 27 | Add tabs to Plots page | Organize plots into tabs: RVs, Spectrum, RV Analysis, Emission Lines Comparison. Add cleaned/contaminated toggle | medium | webapp, plots | done | Claude | Guy | 2026-03-01 | N | Y | 2026-03-11T18:03:58 |  |
| 36 | TODO page bug fixes: compact edit, auto-priority, items in boxes | 3 fixes: single st.markdown for Eisenhower boxes, pencil edit button instead of expander, priority auto-derived from urgent/important flags | critical | webapp, GUI | done | Claude | Guy | 2026-03-01 | Y | Y | 2026-03-11T18:03:45 |  |
| 40 | clear Add new task after adding task | Re-fixed: form version counter pattern — incrementing creates fresh widget keys, reliably clearing all fields. | low | To Do, webapp, bug | done | Guy | Guy | 2026-03-03 | N | N | 2026-03-11T18:03:29 |  |
| 65 | Delete button for tasks | Added 🗑️ delete button per task, Deleted section with restore and permanent delete. | low | To Do, feature | done | Guy | Guy | 2026-03-05 | N | N | 2026-03-11T18:03:25 |  |
| 97 | Langer tab: direct q range & distribution controls | Replaced q presets (Dsilva/Langer flat/Langer Fig.3) with direct controls: distribution type selector (flat/gaussian/lognormal), adjustable q_min/q_max, and conditional μ/σ inputs. Gaussian clips to q_range instead of hardcoded bounds. | high | bias-correction, Langer | done | Claude | Guy | 2026-03-10 | N | Y | 2026-03-11T18:03:08 |  |
| 94 | Langer model: flexible parametric + empirical distributions for q & logP | Full distribution choice for q and logP: Gaussian, Log-normal, Reflected log-normal, Empirical (digitized from Langer Fig. 4 & 6), Flat. logP uses two-component mixture with per-component distribution choice. Empirical mode samples directly from digitized histogram bin heights. All params now persist to disk via on_change callbacks. | high | bias-correction, Langer | done | Claude | Tomer | 2026-03-10 | N | Y | 2026-03-11T18:03:00 |  |
| 39 | flip the agent log so the new log would be on top, | Reversed session blocks so newest appears first. | high | agent, webapp, bug | done | Guy | Guy | 2026-03-03 | N | Y | 2026-03-11T18:00:02 |  |
| 19 | Add f_bin vs sigma and pi vs sigma heatmaps | Added marginalized heatmaps after corner plot when sigma scan is active. | high | bias-correction | done | Claude | Guy | 2026-03-01 | N | Y | 2026-03-11T17:59:59 |  |
| 89 | save state/results easily to load easily later | Descriptive filenames + load dropdown implemented. Needs save button UX fix (auto-fill name, manual save). | critical | bias-correction, feature | done | Guy | Guy | 2026-03-09T11:43:37 | Y | Y | 2026-03-11T17:59:46 |  |
| 18 | Propagate best-fit model to all graphs | Best-fit argmax (f_bin, π, σ, logP_max) now shown in summary and used in all downstream analysis plots | critical | bias-correction | done | Claude | Guy | 2026-03-01 | Y | Y | 2026-03-11T17:59:26 |  |
| 37 | To-test workflow + restore completed tasks | Added to-test status with green badge, confirm button, full metadata in Done table, restore button to uncheck completed tasks | critical | webapp, GUI | done | Claude | Guy | 2026-03-01 | Y | Y | 2026-03-11T17:59:21 |  |
| 4 | Langer 2020 period model | Implement pipeline/langer_grid.py for the Langer et al. (2020) period distribution model. read through the paper (found in papers) and with the fact that the BH are the same as my WC stars for the period distributions (figure out if its case A or case B), find the figure that shows the distribution and mimic it into the model. of course ill see if it matches using the plotted histograms later | critical | bias-correction, pipeline | done | Claude | Tomer | 2026-03-01 | Y | Y | 2026-03-11T17:59:11 |  |
| 60 | Clicking a task found in the Eisenhower table jumps to it in edit mode in the To Dos page | Eisenhower tasks are now clickable st.buttons that open edit form and scroll to the task. | low | To Do, feature | done | Guy | Guy | 2026-03-05 | N | N | 2026-03-11T17:07:24 |  |
| 47 | Add integration with overleaf | through github i want claude to be connected to my overleaf for 2 things. 1) read through my thesis journal notes, to better understand my thesis. and load To Dos from the To Do list i have there. 2) so we could start writing the paper. | low | overleaf, paper, To Do | done | Guy | Guy | 2026-03-05 | N | N | 2026-03-11T17:06:57 |  |
| 81 | Add sorting for Open Tasks | Removed forced priority sort from save_todos() — file preserves insertion order, UI sort handles display. | low | To Do, feature, bug | done | Guy | Guy | 2026-03-08 | N | N | 2026-03-11T17:06:08 |  |
| 41 | fix bias-correction page | something broke and its not showing the histograms and anything after that. fix that with all what we did previously, and update it to the common errors so it wont hapen again | critical | bias-correction, bug | done | Guy | Guy | 2026-03-08 | Y | Y | 2026-03-09T12:38:24 |  |
| 72 | Time stamps for tasks | Full datetime (date+time) shown in task rows, datetime with seconds for date_added/date_done, modification timestamps in Notes column. | high | feature, To Do, declined | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-08T20:51:43 |  |
| 75 | Add drop down menu for To Do Tags, along with free text box | Multiselect of existing tags (sorted by frequency) + free text for new tags, in both Add and Edit forms. | high | To Do, feature | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-08T18:19:02 |  |
| 74 | Add 'X' button next to 'V' for completed tasks waiting for my approval | Added decline button with feedback form. Reason stored in Notes column, task returned to open with 'declined' tag. | high | To Do, feature | done | Guy | Guy | 2026-03-08 | N | Y | 2026-03-08T18:18:20 |  |
| 48 | Sigma slider snaps to exact values | Float options with format_func replace string labels; no more collisions | low | bias-correction | done | Claude | Guy | 2026-03-05 | N | N | 2026-03-08T18:17:31 |  |
| 69 | Heatmap stuck on last sigma after run | Post-run render now shows best-sigma heatmap immediately | high | bias-correction, bug | done | Claude | Guy | 2026-03-05 | N | Y | 2026-03-08 |  |
| 82 | Corner plot axes flipped vs main heatmap | Corner plot f_bin×π cell has swapped axes compared to the main K-S heatmap | high | bias-correction, bug | done | Claude | Guy | 2026-03-08 | N | Y | 2026-03-08 |  |
| 20 | Switch to matplotlib-style Plotly theming | White backgrounds, scientific fonts, traditional academic plot styling via Plotly (not pure matplotlib) | critical | graphs, webapp | done | Claude | Guy | 2026-03-01 | Y | Y | 2026-03-05 |  |
| 25 | Remove duplicate page menu in sidebar | Top page menu is ugly but functional, bottom one looks great — remove the top duplicate. CSS hide applied via stSidebarNav | critical | GUI, webapp | done | Claude | Guy | 2026-03-01 | Y | Y | 2026-03-03 |  |
| 38 | Fix agent webapp dark theme + page link paths + rate limit resilience |  | medium |  | done |  |  |  | N | N | 2026-03-03 |  |
| 33 | Eisenhower matrix + inline editing in To-Do page |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 8 | Add marginalization + corner plot |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 9 | Add M2, T0, omega histograms |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 10 | Verify K-S test scoring |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 11 | Verify q = M2/M1 |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 12 | Add compute_hdi68 helper |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 13 | Create GIT_LOG.md changelog |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 14 | Create TODO.md + webapp page |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 15 | Binary fraction vs threshold plot |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 16 | Create DOCUMENTATION.md |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 17 | Add reference papers (Dsilva, Langer) |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 34 | Common errors system + np.trapz fix |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |
| 35 | Documentation system with work log |  | medium |  | done |  |  |  | N | N | 2026-03-01 |  |

## Deleted

| ID | Title | Date deleted | Notes |
|----|-------|-------------|-------|
| 3 | Try logP_max = 4 | 2026-03-11T18:10:05 |  |
| 71 | Apply relevant changes on Dsilva tab to Langer tab | 2026-03-11T18:10:57 |  |
| 1 | CDF truncation at 350 km/s | 2026-03-11T18:11:09 |  |
| 54 | Randomize seeds for bias-correction simulation? | 2026-04-23T14:13:48 |  |
| 155 | Review CvM as primary scoring method | 2026-04-23T17:43:13 | Already implemented + integrated; review whether to promote as primary method |
| 154 | Integrate χ² weighted CDF scoring | 2026-04-23T17:43:14 | Function already exists, needs integration into grid scoring + UI toggle |
| 153 | Integrate inverse-variance weighted KS test | 2026-04-23T17:43:17 | Function already exists, needs integration into grid scoring + correct p-value |
